{
  "name": "receiz-offline-verifier",
  "private": true,
  "version": "85.0.0",
  "description": "Receiz offline verifier static app and public release surfaces",
  "scripts": {
    "lint": "node scripts/check-release-surfaces.mjs",
    "build": "node scripts/check-release-surfaces.mjs",
    "release:lock": "node scripts/check-release-surfaces.mjs"
  },
  "license": "MIT"
}
