# Receiz v113.5.0 Release Lock Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish and locally seal Receiz v113.5.0 as a stabilization-only coordinated distribution while preserving the frozen v113.0.0 authority layer and leaving external release actions unperformed.

**Architecture:** Keep the supplied v113.5 release records byte-matched in `releases/` and `docs/releases/`, add the standalone verifier’s required performance and invariant locks, and advance only package/runtime/documentation release identity. Preserve the existing Offline Studio full-width drop-surface correction and exact offline verifier labels, then qualify the exact candidate before one detailed commit and annotated tag.

**Tech Stack:** Markdown release evidence, static HTML/CSS offline proof instruments, Node.js release checks, pnpm scripts, service-worker version identity, and Git annotated release objects.

## Global Constraints

- Distribution version is `113.5.0`; release date is `2026-07-26`.
- The frozen ruleset, constitution registry/digest, artifact-law version, application operation matrix, compiler target, nine-tool MCP inventory, and compatibility range remain v113.0.0 / `>=113.0.0 <114.0.0`.
- No v113.5 authority layer is invented and PR #223 / the v114 development line remains excluded.
- Exact sealed artifact bytes, independently verified local truth, ownership proof, settlement finality, and deterministic first paint remain stronger than session, server, database, cache, projection, retry, and release state.
- Offline Record/Seal and Verify remain independently usable; local verification settles before optional append or network work.
- JPEG/HEIC canonical carrier handling remains fail-closed outside permitted transformations.
- Sports reward closeout remains one atomic terminal settlement.
- Durable proof memory remains first admission only, then append forever.
- Existing user changes to `apps/offline-record-seal.html` and `apps/offline-verifier.html` are release candidate inputs and must be preserved.
- Signed attestation, package publication, deployment, migration, production mutation/verification, GitHub release publication, and Git push remain external.

---

### Task 1: Complete the v113.5 release archive

**Files:** Create `releases/v113.5.0-performance-findings.md`, `releases/v113.5.0-invariant-register.md`, and their byte-matched `docs/releases/` copies; update supplied v113.5 records only where the generic standalone lock requires explicit links/headings/boundaries.

**Interfaces:** Consumes the attached release specification and twelve supplied evidence files; produces a complete canonical/archive record set without changing functional product behavior.

- [ ] Add performance findings for exact-byte offline proof flow, JPEG/HEIC canonical bases, local-first verification, retry/projection continuity, atomic reward closeout, first paint, and warning-free release quality.
- [ ] Add an invariant register separating distribution version from frozen v113 constitutional identity and locking all artifact, settlement, first-paint, and external-action boundaries.
- [ ] Add `Release-Lock Requirement` headings and cross-links required by `scripts/check-release-surfaces.mjs`.
- [ ] Preserve upstream release-range evidence while distinguishing the standalone verifier’s local Git baseline.

### Task 2: Align governed v113.5 release identity

**Files:** Modify `package.json`, `AGENTS.md`, root release notes/changelog, docs indexes/doctrine pointers, static verifier entrypoints, `site/index.html`, `site/sw.js`, and the release-checker date.

**Interfaces:** Produces coherent `113.5.0` distribution/runtime labels while the v113 ruleset and registry remain frozen.

- [ ] Advance current distribution and service-worker labels to `113.5.0`.
- [ ] Describe the stabilization fixes and explicit v114 exclusion without presenting them as new authority.
- [ ] Preserve the existing Offline Studio `display: block` drop-surface correction and verifier version change.
- [ ] Keep v113.0.0 as explicit constitutional and historical identity where required.

### Task 3: Qualify the exact standalone candidate

**Files:** Update and mirror `v113.5.0-checklist.md` only with checks actually supported by this repository.

**Interfaces:** Produces fresh local verification evidence and precise uncertainty boundaries.

- [ ] Run `node scripts/check_release_lock.mjs 113.5.0`, `pnpm lint`, `pnpm build`, and `pnpm release:lock 113.5.0`.
- [ ] Validate canonical/archive parity, all v113.5 relative links, frozen-v113 language, v114 exclusion, current labels, and exact candidate file inventory.
- [ ] Run `git diff --check` and confirm no unrelated or unstaged candidate changes.
- [ ] Keep signed attestation and every publication/deployment/production action unchecked.

### Task 4: Commit and tag the verified release

**Files:** Stage only planned v113.5 records and governed surfaces; create one detailed commit and annotated `v113.5.0` tag.

**Interfaces:** Produces local Git release objects without remote or production mutation.

- [ ] Rerun all gates against the exact staged index.
- [ ] Commit with changed surface, preserved primitive, risk boundary, checks, no downgrade, upstream/local history distinction, and external exclusions.
- [ ] Create annotated tag `v113.5.0` with stabilization/frozen-authority/external-boundary notes.
- [ ] Rerun release checks post-commit and verify clean tree, tag-to-HEAD identity, and branch-ahead count.

## Self-Review

- The plan covers every section of the attached release specification and the standalone checker contract.
- No placeholder, new authority layer, v114 inclusion, invented attestation, migration application, publication, deployment, or push is permitted.
- File and proof primitives remain named directly; no proof object, verification verdict, settlement state, first paint, or service-worker identity is downgraded.
