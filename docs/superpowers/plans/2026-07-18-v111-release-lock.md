# Receiz v111.0.0 Release Lock Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Publish and locally seal Receiz v111.0.0 artifact-derived authority hardening without performing GitHub attestation, package publication, deployment, database mutation, or Git push.

**Architecture:** Promote the supplied v111 evidence into byte-identical canonical and archive release trees, add the verifier-required performance and invariant locks, and advance release-identification surfaces only. Qualify the 43-law constitutional chain, `ARTIFACT-016` through `ARTIFACT-020`, archive parity, links, and the exact staged Git candidate before creating the local commit and tag.

**Tech Stack:** Markdown/JSON evidence, Node.js release checks, pnpm scripts, static HTML/service-worker version surfaces, Git commit and annotated tag.

## Global Constraints

- Release identity is `v111.0.0`, dated `2026-07-18`.
- Authority flows `law -> sealed proof object -> verified admission -> verified proof history -> deterministic recovery state -> verified capability -> atomic commit -> projection`.
- Every recovery-authoritative value derives from canonically reverified exact artifact bytes and independently verified stronger evidence roots.
- Structural admissions, callbacks, normalized graphs, receipts, server/session state, MCP memory, and AI explanations cannot create authority.
- Canonical identity requires domain-separated proof of private-key control.
- Divergent verified heads fail closed; recovery plans bind the complete authority basis.
- Historical runtime objects are not current operational authority; historical sealed artifacts remain evidence.
- MCP deterministic plan identity is distinct from consumable execution-attempt identity.
- Durable proof memory remains first admission only, then append forever.
- Receiz.com application/API behavior and database schema/migrations remain unchanged.
- GitHub attestation, publication, deployment, production data work, and Git push remain external.

---

### Task 1: Complete and mirror the v111 evidence archive

**Files:** Create `docs/releases/v111.0.0*`, `releases/v111.0.0-performance-findings.md`, and `releases/v111.0.0-invariant-register.md`; update supplied v111 Markdown with release-lock links/headings.

**Interfaces:** Consumes the eight supplied records; produces byte-identical canonical/archive evidence with explicit authority and external-action boundaries.

- [ ] Normalize release-local links and mirror every supplied record.
- [ ] Add performance findings for canonical reverification, independently rooted history, single-head failure, identity challenge, immutable zero-write failures, and fresh MCP attempts.
- [ ] Add an invariant register locking `ARTIFACT-016` through `ARTIFACT-020`, authority encoding, history/identity/plan/capability bases, and unchanged app/database scope.
- [ ] Add required release-lock headings and standalone-verifier local qualification items.

### Task 2: Align governed v111 identification surfaces

**Files:** Modify package/agent/root docs, documentation maps/doctrine, static verifier entrypoints, service worker, and release notes/changelog.

**Interfaces:** Consumes canonical v111 truth; produces coherent `111.0.0` identification without an application/API/database behavior claim.

- [ ] Advance all governed current-version pointers to `v111.0.0`.
- [ ] Describe artifact-derived authority, structural rejection, verified roots, identity control, complete plan basis, current-registry capability, immutable failures, and fresh MCP attempts.
- [ ] Preserve v110 and earlier only as explicit history; confirm direct primitive naming and no downgrade.

### Task 3: Qualify and record the standalone candidate

**Files:** Update and mirror `v111.0.0-checklist.md`.

**Interfaces:** Produces fresh local qualification evidence only.

- [ ] Run release lock, lint, build, and `pnpm release:lock 111.0.0`.
- [ ] Validate digest lineage, effective Kai, seal instant, 43 laws, `ARTIFACT-016`–`020`, and proof-object authority.
- [ ] Validate canonical/archive parity, relative links, stale v110 current-surface references, and diff integrity.
- [ ] Record only verified local checks, then rerun the full suite.

### Task 4: Commit and tag the exact verified candidate

**Files:** Stage only planned v111 records and governed surfaces; create one detailed commit and annotated `v111.0.0` tag.

**Interfaces:** Produces local Git release objects without remote mutation.

- [ ] Confirm no unstaged changes and no pre-existing v111 tag.
- [ ] Rerun full qualification against the exact index.
- [ ] Commit with changed surface, preserved primitive, risk boundary, checks, no downgrade, and external exclusions.
- [ ] Tag with constitutional seal and verify clean tree, tag-to-HEAD identity, release lock, and branch-ahead count.

## Self-Review

- Spec coverage includes every checklist claim, constitutional authority addition, external boundary, and durability law.
- No placeholders or inconsistent operation/law names remain.
