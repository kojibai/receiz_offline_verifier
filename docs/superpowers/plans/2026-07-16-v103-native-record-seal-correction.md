# Receiz v103.0.0 Native Record And Seal Continuity Correction Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Promote the v103 native continuity correction through the standalone offline verifier release lock.

**Architecture:** Restore native Profile Composer, Record, and Seal proof bundles as the verification primitive while keeping SDK/MCP/AI-skills as direct composition mechanics. Align public/verifier identity and the eight-document repository lock without carrying the rejected v102 universal portable-carrier gate forward.

**Tech Stack:** Markdown, static HTML, service-worker JavaScript, Node.js checks, pnpm, Git.

## Global Constraints

- Release identity is `v103.0.0` / `103.0.0`, dated `2026-07-16`.
- Changed surface: native Record/Seal verification and Profile showcase download continuity.
- Preserved primitive: historical and new native Receiz sealed proof bundles with canonical coordinates and Record ownership projection.
- v101 production behavior is the confirmed live baseline; v102's parallel-carrier requirement remains rejected.
- Durable proof memory remains first admission only, then append forever.

---

### Task 1: Align Release Surfaces

- [ ] Advance package, verifier labels, service-worker identity, release date, and current documentation pointers.
- [ ] Install supplied records canonically and add verifier-required performance findings, invariant register, cross-links, release-lock clauses, and local baseline.
- [ ] Run `node scripts/check_release_lock.mjs 103.0.0` successfully.

### Task 2: Verify and Hand Off

- [ ] Run `pnpm lint`, `pnpm build`, `pnpm release:lock 103.0.0`, `git diff --check`, and current-narrative scan successfully.
- [ ] Record only verifier-local checks actually executed and rerun the full gate set.
- [ ] Commit with a complete title/body and create local annotated tag `v103.0.0`; do not push.
