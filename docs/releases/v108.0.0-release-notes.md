# Receiz v108.0.0 release notes

## Proof-object-first correction

V108 makes the inherited authority direction explicit across every current surface:

```text
Receiz law and invariants
-> sealed proof object / deterministic proof state / verified local truth
-> accepted identity proof and admitted same-account binding
-> Receiz.com first-party reference behavior
-> SDK
-> MCP
-> AI skills
```

Receiz.com remains below proof truth and above developer rails only in dependency and conformance direction. The registry, threat model, auxiliary operation evidence, server state, and release evidence are not admission and are not proof authority.

## Profile save restored

- The first-party profile client is pinned to the exact pre-regression behavior from `a1081796e90a65124d952ce5907512999dd16cb0`.
- Inline rename and full save call `PATCH /api/profile/me` directly.
- The established account UID is preserved through rename, media composition, session refresh, and public-profile projection.
- Username availability remains advisory; commit-time uniqueness decides the winner.
- Zero `identity_keys` rows is a valid state for an already-admitted first-party account and cannot block a save.
- First-party code does not call `/api/receiz/identity/profile`, choose a latest key, or depend on SDK/MCP/AI identity selection.
- No developer-side authority prerequisite can gate or roll back the first-party result.

This corrects the authority inversion introduced by `9611cb8f57c587eae1b6ee8079ee6d7bd2a02637`; it does not invent a replacement profile flow.

## Truthful developer adapters

- The current developer profile update accepts authenticated profile intent only and composes the same established profile command behind Receiz.com's `PATCH /api/profile/me` route.
- The admitted account UID is preserved; no caller-selected key, receipt, head, or separate identity projection participates.
- SDK calls use the typed Receiz.com HTTP adapter, MCP executes through the SDK adapter, and AI skills adapt downward to that same operation.
- A registry row does not prove portable state. Portable-state verification requires the enclosing carried artifact or admitted verified block.

## Current developer outcomes under v108 law

Current SDK/MCP/skill surfaces expose neutral same-UID profile update and complete proof-object ownership composition. Profile callers provide intent only. Bearer callers provide the complete verified artifact only, and the result is a newly verified native Record -> Seal artifact. Proof objects are not confined to their originating platform: any lawful platform can verify and append authenticated ownership or history while preserving the same immutable object identity, payload, provenance root, prior history, and unknown application namespaces.

The two v107 SQL designs for identity-profile append state and generic bearer-claim storage were never applied. They are absent from the active migration chain and retained byte-for-byte only as non-deployable `.sql.txt` evidence under `docs/releases/historical/v107-never-applied-sql`. V108 does not require, advertise, or call their absent tables, bucket, or RPCs; it composes the developer outcomes from the established profile command and verified native artifact operations.

Historical v107 receipts and artifacts continue to verify against the law and digest they witnessed. Current v108 outcomes do not fabricate heads or receipts for the never-applied design. No migration rewrites sealed artifacts or causal history.

## First-party scope restored

The developer command queue is not mounted into the Receiz.com root runtime or main service worker. Existing profile, ownership, public-proof, PWA, and deterministic-first-paint behavior cannot be changed solely to conform to a developer rail. Independent continuity corrections that prevent stale PBI or public-profile projections from replacing newer verified truth remain protected.

## Developer documentation

`/developers` is the canonical maintained developer surface and states the proof-first direction directly. `/developers/v107` is a compatibility redirect to `/developers`; no version-branded v108 page is introduced.

## Release identity

The coordinated current version is `108.0.0`. The v108 registry was sealed at `2026-07-18T04:38:26.799Z` with real Kai-Klok pulse `13182786` and digest `126ca9283fee4ef4c398dbcb958e861cbea191724fdab8eb08df55ff0c14bb79`.

## Evidence boundary

The release notes describe the implemented and locked contract. Final repository qualification still requires the focused v108 contracts, actual desktop and mobile profile-mutation evidence, inherited release gates, the complete release freeze, and governance verification. Repository qualification is distinct from npm publication, production deployment, production database verification, and production signing.
