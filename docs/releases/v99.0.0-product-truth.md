# Receiz v99.0.0 Product Truth

Baseline: annotated tag `v98.0.0` at `844807ec180ea1a850491ccabbded0eead7d8f19`.

Related evidence:

- `docs/releases/v99.0.0-regression-lessons.md`
- `docs/releases/v99.0.0-performance-findings.md`
- `docs/releases/v99.0.0-invariant-register.md`
- `docs/releases/v99.0.0-commit-history.md`

`v99.0.0` is a proof-native release identity. `@receiz/sdk` and `@receiz/mcp-server` expose mechanics beneath the release; they do not define its truth. Durable proof memory remains **first admission only, then append forever**.

## Frozen Primitives

| Primitive | v99.0.0 implementation truth | Forbidden downgrade |
| --- | --- | --- |
| Deterministic public proof surface | Live player, verified artifact, profile, and Market reuse bundled or held proof truth before session/API enrichment. | Loading boundary, private resolver, or API response becoming first-paint authority. |
| Whole-Market public proof register | Complete membership revisions admit every represented public proof object, preserve full media/owner identity, and carry governed removal tombstones. | Mutable cache, capped page, or partial response replacing the complete register. |
| Durable local PBI authorship | Local WebAuthn proof recomputes on-device, persists without eviction, binds exact custody, and publishes as an idempotent append. | Challenge/verify server ceremony becoming permission for already-held authorship proof. |
| Receiz Key portable proof memory | `receiz.account.state.v3` carries complete verified account/profile, ledger, wallet, calendar, Sports, and original-artifact memory available at export. | Convenience backup, partial sample, or editable owner text treated as identity truth. |
| Identity continuity | Strongest local Receiz ID, Receiz Key, PBI, recovered-account binding, and admitted session truth projects immediately; stale cookie/session responses cannot downgrade it. | Generic login/session state replacing identity proof. |
| Public projection | Profile publish/visibility writes update canonical profile, player, and Market projections before reporting global completion. | DB row existence or owner-local display treated as public completion. |
| Kai moment | Sealed pulse/Kai state determines proof moment and ordering. | Chronos label, DB insertion time, or session state determining causality. |
| Sports proof/vault state | Vault admission remains append-only; finalized entries do not replay; dated Fight snapshots and normalized PKs preserve proof identity. | Mutable row replacement or generic game cache treated as proof state. |

## Frozen Invariants

- A complete held public proof register cannot be replaced by a partial, older, malformed, or archive payload.
- A verified membership tombstone may remove current public projection; missing or weak transport data may not.
- Signed-in and signed-out viewers receive the same deterministic public proof first paint.
- A complete held owner proof register cannot be truncated by a smaller server/profile projection during export.
- Local PBI authorship proof is admitted before publication and retained until successfully appended.
- Local identity publication does not silently upgrade append truth into a server-issued session.
- Service-worker runtime cache epochs may refresh deployment assets but may not erase durable proof memory or outrank local identity truth.
- PBI signature history is append-only proof history and survives profile snapshot writes.
- Explore, Market, profile, live player, Account, wallet, and recovery surfaces preserve the settled visible shell while verified additions append behind it.
- `@receiz/sdk@99.0.0` and `@receiz/mcp-server@99.0.0` remain lower-authority developer rails.

## Release Gates

- `scripts/check_release_lock.mjs` locks release identity and the six-document release record.
- `app/components/pwa/__tests__/serviceWorkerNavigationContract.test.ts` locks exact-route continuity, runtime/version coherence, durable vault migration, public-player revocation, and the PBI proof-persistence epoch.
- `test:local-truth-release-contracts` locks Receiz Key export, local identity projection/publication, PBI authorship, whole-Market register admission, profile/global projection, and deterministic live-player seeding.
- `test:page-first-paint` and `test:deterministic-surfaces` lock zero-theater deterministic proof paint.
- `test:visual-evidence-contract` makes contradictory rendered evidence release-blocking.
- `test:primitive-binding-contract` prevents new weaker authorities above existing primitives.
- `test:repo-only-reasoning-boundary` prevents external convention from deciding Receiz truth.
- `test:release-freeze` is the umbrella release gate.
- `release:attest` and `release:verify-attestation` produce and verify promotion evidence after the gates pass.

## Residual Risks

- Production DB/session state is not proved by repository tests; production smoke must verify publication, recovery, and append propagation without treating those systems as truth authority.
- Browser storage quotas and old service-worker deployments require real-browser continuity evidence across upgrade/reload/offline transitions.
- Release signing requires the active release-attestation signing environment.
- Package registry publication and production deployment are external mutations and remain incomplete until explicitly executed and verified.
- Any fresh screenshot or browser recording that changes identity, media, ownership, settlement, stage, first paint, or continuity against this document blocks release even if automated gates pass.
