# Receiz v113.5.0 Performance Findings

`v113.5.0` is a stabilization distribution over the frozen v113 authority layer. Exact sealed proof objects, verified local truth, deterministic first paint, and terminal settlement remain stronger than release, session, network, projection, retry, and cache mechanics.

## Findings

- Offline Record and Seal preserves exact source/trailer bytes, source format, download MIME, and exact-file save/share behavior without requiring a live Receiz identity.
- Canonical Signature V4 enrollment is local-first with bounded certificate expiry and optional authenticated ownership; enrollment mechanics do not redefine the sealed artifact.
- PNG sealing excludes mutable provenance and anchor chunks from the canonical basis.
- JPEG XMP, IFD1 v3, iOS Photos normalization, MakerNote v4, v5 basis, and v6 rational canonicalization accept only the explicitly permitted carrier transformations and otherwise fail closed.
- Native HEIC uses file detection and a canonical UUID proof channel for Record/Seal, exact save, and local verification.
- `/verify`, the installed PWA, and standalone verifiers settle local JPEG/HEIC/existing-carrier verdicts before network, session, server, or optional append work.
- Optional append runs through a durable outbox and cannot change a settled local verification verdict.
- Interrupted uploads reuse the already prepared proof object, and lagging public/profile projections cannot overwrite stronger prepared or saved proof truth.
- Sports reward closeout uses one durable terminal settlement with atomic closeout, retry safety, backpressure, and worker routing.
- Known proof truth and locked typography paint before hydration or service-worker rediscovery; versioned cache invalidation does not change settled product logic.
- Full-width Offline Studio file targets preserve complete Record, Seal, and Verify instruments rather than collapsing inline.
- Warning-free lint removes diagnostic noise without adding runtime branches, data flow, authority, or visible behavior.

## Performance Boundary

- Distribution version `113.5.0` does not create a new ruleset, registry, artifact law, matrix, compiler target, tool inventory, or compatibility interval.
- The frozen v113 constitutional registry digest remains `4c4aa85f9785d205dcf7e4e5109837a83f8c3bf8e166130ae7e87353f299c637`.
- PR #223 and v114 proof-composite work remain excluded.
- This standalone repository verifies static offline proof instruments and release records; it does not reproduce upstream package performance or production rollout evidence.

## Checks

```bash
node scripts/check_release_lock.mjs 113.5.0
pnpm lint
pnpm build
pnpm release:lock 113.5.0
git diff --check
```

## Release-Lock Requirement

`pnpm release:lock 113.5.0` must require this document. Without it, exact-byte offline proof flow, carrier normalization bounds, airplane-mode verdict ordering, retry/projection continuity, atomic settlement, deterministic first paint, drop-surface integrity, and warning-free release quality would lack an explicit scale boundary.
