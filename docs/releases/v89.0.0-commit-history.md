# Receiz v89.0.0 Commit History

Release date: June 13, 2026
Status: Required release-lock artifact for `v89.0.0`

## Purpose

This document locks the commit-history boundary for the Receiz `v89.0.0` release. It records the supplied broader-platform post-v88 ledger summary and the named regression/fix commits that define the foreground anchor publication boundary. It is release truth, not an activity feed.

## Local Repository Boundary

This offline verifier repository is currently cut from the local `v88.0.0` tag. The broader product implementation arc named by this release lives outside this static offline-verifier repository. The local release-prep work adds the `v89.0.0` publication artifacts, release locks, and version-surface alignment that point to the broader product truth.

## Supplied Broader-Platform Ledger

The supplied release brief defines the post-`v88.0.0` arc as:

- base: `v88.0.0` tag
- endpoint: profile-original anchor repair before the final `v89.0.0` release edits
- commit count: 169 commits
- changed files: 361 files
- insertions: 74,225
- deletions: 4,640

## Named Regression And Fix Commits

| Commit | Date | Release role |
| --- | --- | --- |
| `921a00395826f0a6336b6bcb83b1ba7e2ead007d` | June 8, 2026 | Introduced the regression by generalizing local signed-action/outbox behavior so foreground proof publication could be admitted to the generic outbox path. |
| `ad246053cfdf8510c57c01fc0c7901014270b3d0` | Post-regression repair | Fixed the boundary by excluding `/api/receiz/anchor` from local signed outbox admission. |

## Ledger Sections Preserved By This Release

- Profile publication and anchor repair
- Profile showcase/global sync and public profile market append refresh
- Visible local route snapshot refresh after visible original writes
- Sports Vault sealed image/manifest publication
- Compact QR publish and showcase publish separation
- Public owner vault routes and public event proofs
- Sports live/vault/FIFA/UFC/soccer/fight proof additions
- Pack claim/open retry, first-vault sync, and stale pending recovery repairs
- Wallet ledger receipts, wallet first-paint cookie, settled wallet RPC path, PBI upsert, and Receiz session continuity
- Service-worker/runtime version bumps and navigation behavior protection
- Lease cockpit, local verified lease lifecycle register, public marketplace handoff, and selected transaction room
- Sports proof read, SQL proof/source payload, user-live route, append discovery, wallet read, and profile read performance repairs

## Locked Commit-History Rule

The release may not describe the foreground anchor issue as a server-route readability failure alone. The preserved history boundary is:

```txt
foreground proof publication route
sealed/deterministic anchor response
profile original publication projection
public profile showcase/global sync/market projection
generic local signed-action outbox
```

The regression was weaker outbox admission into a stronger foreground anchor response path. The fix was route-specific exclusion of `/api/receiz/anchor` from local signed-action outbox admission.

## Release-Lock Requirement

`pnpm release:lock` must require this document for `v89.0.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the post-regression source of truth has not been made explicit.

## No Downgrade Statement

This commit-history artifact preserves the v89 proof boundary. It does not downgrade foreground proof publication to generic outbox replay, verified history to an activity feed, route truth to framework convention, public proof surfaces to share pages, or service-worker release truth to incidental cache behavior.

