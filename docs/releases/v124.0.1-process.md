# Receiz v124.0.1 Release Process — Reality Becomes Infrastructure

**The Production Runtime for Verified Civilization.** This process keeps the sealed proof object above package and deployment state.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk` and every coordinated package or deployment operation remain mechanics beneath the admitted sealed history.

## Goal

Publish one corrective V124 package/application set that closes every known developer scope and MCP/AI/Academy parity gap while preserving sealed proof object authority and the immutable V124 ruleset.

## What Worked

- Comparing every projection against the complete runtime inventory exposed omissions that grouped feature tests could not see.
- Separating package version `124.0.1` from ruleset version `124.0.0` preserved immutable law while making corrected bytes publishable.
- Host-held material/session references allowed MCP parity without leaking or reconstructing proof authority in model context.

## Order

1. Prove complete OIDC scope and MCP runtime inventories.
2. Generate AI and Academy projections from those exact inventories.
3. Run focused behavior, custody, parity, type, build, and release-law gates.
4. Commit the coherent `124.0.1` candidate.
5. Publish `@receiz/ai-skills`, then `@receiz/sdk`, then `@receiz/mcp-server`.
6. Resolve the Receiz Docs lock against those exact published coordinates before deployment.
7. Record publication, deployment, smoke, tag, and attestation evidence only after each operation succeeds.

## What Did Not Work

- Treating a new V124 subgroup as proof of whole-runtime parity left earlier generic tools outside machine registries.
- Updating the visible scope selector without updating every fallback emitter left valid capabilities unreachable from some setup paths.
- Treating missing database/anchor projections as proof failure let weaker infrastructure erase a valid public coordinate.

## Practical Rule For Future Work

Every new public SDK capability must close one end-to-end matrix in the same change: canonical scope, developer selector, token issue path, SDK operation, MCP adapter, AI machine registry, Academy projection, exact test, and release package. Special versioned groups remain immutable subsets of the complete current inventory.

## Standalone Qualification Boundary

1. Read `/Users/bjklock/Kai-Turah/receiz` without modifying it and bind the latest committed source candidate and final observed `origin/main` boundary `b4efb56c72780bd9b5013ef466fdea998701b96b`.
2. Preserve the existing standalone verifier and local Record/Seal proof primitives; change release markers and cache epochs only.
3. Carry the seven canonical `v124.0.1` release records into both archive locations with explicit standalone evidence boundaries.
4. Prove the frozen `v124.0.0` registry digest, 124-law inventory, predecessor digest, matrix digest, and compatibility interval remain unchanged.
5. Run the standalone surface checker, release lock, lint/build aliases, syntax checks, mirror checks, and whitespace checks.
6. Commit the standalone release from predecessor `35d3a440e2252dd8146e0b7f443824b19c0cf852`; create an annotated local tag only after the commit verifies.

Package publication, deployment, application-repository mutation, remote push, production smoke, and signed attestation are outside this standalone process.
