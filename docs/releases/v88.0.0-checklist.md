# Receiz v88.0.0 Checklist

Release date: June 3, 2026
Status: Release prep

## Release Scope

- Current documented release anchor in this repository before this cut: `v87.0.0`.
- Broader product trail referenced by this release: `v87.0.0` tag through `87.5.0` stable head.
- Post-`v87.0.0` arc: 90 commits, 195 changed files, 72,663 insertions, and 6,475 deletions before the final `v88.0.0` lock edits.
- This checklist governs the Live Sports Truth, Runtime Coherence, Public Live Cockpit, And Post-v87 Regression Lock cut and the outward version-surface alignment for this offline verifier repository.

## Required Release Artifacts

- `AGENTS.md`
- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/truthful-speed-invariants.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v87.0.0-product-truth.md`
- `docs/releases/v88.0.0.md`
- `docs/releases/v88.0.0-product-truth.md`
- `docs/releases/v88.0.0-checklist.md`
- `docs/releases/v88.0.0-process.md`
- `docs/releases/v88.0.0-regression-lessons.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- product-truth reference status markers updated for `v88.0.0`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`
- service-worker runtime marker updated in `site/sw.js`
- release lock requires the regression lessons document

## Canonical Product Gates

- Receiz remains named as a proof-native artifact system.
- Offline proof remains real proof and is not narrowed from `v87.0.0`.
- Compact proof token transport remains subordinate to the sealed proof bundle.
- Live Sports locked entries, individual player scores, field truth, reward state, and append truth remain live proof surfaces.
- `/baseball` remains a no-fork direct entry route to the canonical Sports baseball arena.
- Known selected-game and locked-entry truth paints before hydration.
- Hidden hydration appends verified additions only.
- Public Live Cockpit and `/live` rooms remain public witness/live proof surfaces.
- Market/Profile first paint preserves public proof object and canonical profile truth.
- Service-worker identity, public version labels, package version, runtime coherence defaults, and product truth move together.
- Explicit session clear/logout cannot resurrect stale signed-in first paint.
- Supabase timeout hardening stays tracked as DB policy/index/migration truth beneath Receiz product truth.
- Release output states changed surface, preserved primitive, risk boundary, checks run, and no downgrade.

## Focused Platform Verification

Release-prep commands expected in the broader platform workspace:

- `pnpm lint`
- `pnpm build`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm test:release-freeze`
- `pnpm test:page-first-paint`
- `pnpm test:value-loop-guard`
- `pnpm test:contacts-release-contracts`
- `pnpm test:signature-v4-server-remote`
- Sports live field layout and feed ownership contracts
- `/baseball` direct-route/no-fork contracts
- selected-game hydration and single-game proof projection contracts
- locked-entry first-paint and per-player score append contracts
- live cockpit total projection contracts
- finalized reward recovery contracts
- Public Live Cockpit and `/live` room public witness contracts
- market/profile first-paint and public proof object contracts
- service-worker version coherence and package fallback contracts
- session bootstrap durable-key clear/migration contracts
- notification delivery, push subscription, and chat call/message contracts
- Supabase timeout hardening migration and RLS guard contracts
- release lock, regression lessons, release attestation, and conformance history gates

## Offline Verifier Repository Gates

- root package metadata resolves to `v88.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v88.0.0`
- static service-worker registrations request `sw.js?v=88.0.0`
- `site/sw.js` carries runtime version `88.0.0`
- release docs and indexes point to the `v88.0.0` release artifacts
- product-truth reference docs are linked from the public docs map and carried forward for `v88.0.0`
- broader platform validation for `v88.0.0` is captured in the canonical release note, product-truth document, process document, checklist, and regression lessons
- verifier trust semantics remain deterministic and fail-closed
- `pnpm lint` has zero warnings/errors
- `pnpm build` completes
- `pnpm release:lock` confirms governed release surfaces and regression lessons
- whitespace diff check passes
- `pnpm release:attest` emits a signed attestation for the final commit before production shipment
- governed surfaces all show `v88.0.0`

## Release Rule

Do not ship `v88.0.0` unless the release gates in this checklist pass from the exact release candidate, `pnpm release:lock` confirms the release surfaces and regression lessons, whitespace diff passes, focused Sports, Market, Profile, Public Live Cockpit, runtime/session, notification/chat, Supabase timeout, security, and release contracts pass, production migrations named by the release are applied in the target environment, and release attestation is created in the production signing environment.

