# Receiz v89.0.0 Checklist

Release date: June 13, 2026
Status: Release prep

## Release Scope

- Current documented release anchor in this repository before this cut: `v88.0.0`.
- Broader product trail referenced by this release: `v88.0.0` tag through the profile-original anchor repair.
- Post-`v88.0.0` arc: 169 commits, 361 changed files, 74,225 insertions, and 4,640 deletions before the final `v89.0.0` lock edits.
- This checklist governs the Foreground Anchor Publication, Profile Global Sync, Sports Vault Public Proof, Wallet Runtime, And Post-v88 Regression Lock cut and the outward version-surface alignment for this offline verifier repository.

## Required Release Artifacts

- `AGENTS.md`
- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/truthful-speed-invariants.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v88.0.0-product-truth.md`
- `docs/releases/v89.0.0.md`
- `docs/releases/v89.0.0-product-truth.md`
- `docs/releases/v89.0.0-checklist.md`
- `docs/releases/v89.0.0-process.md`
- `docs/releases/v89.0.0-regression-lessons.md`
- `docs/releases/v89.0.0-commit-history.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- product-truth reference status markers updated for `v89.0.0`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`
- service-worker runtime marker updated in `site/sw.js`
- release lock requires the regression lessons and commit-history documents

## Canonical Product Gates

- Receiz remains named as a proof-native artifact system.
- Offline proof remains real proof and is not narrowed from `v87.0.0`.
- Live Sports, market/profile first-paint, runtime coherence, DB hardening, and regression lessons remain protected from `v88.0.0`.
- `/api/receiz/anchor` remains the foreground proof publication route.
- No broad `/api/receiz` outbox classifier re-admits foreground anchor publication.
- Anchor success and failure responses remain readable JSON before the composer reads them.
- Profile original writes append or refresh showcase truth, public profile market append truth, and visible local route snapshots.
- Sports Vault public proof publishing remains sealed-manifest first.
- Sports cards, packs, lineups, vault objects, event proofs, FIFA/UFC/fight additions, and public showcases remain proof objects.
- Receiz ID, PBI, wallet settlement, wallet ledger receipts, and durable runtime projection remain identity and settlement primitives.
- Lease cockpit, market ownership, sale, claim, and transaction room paths remain ownership and settlement primitives.
- Faster reads preserve stronger proof/local truth and append verified additions only.
- Service-worker identity, public version labels, package version, runtime coherence defaults, and product truth move together.
- Release output states changed surface, preserved primitive, risk boundary, checks run, and no downgrade.

## Focused Platform Verification

Release-prep commands expected in the broader platform workspace:

- `pnpm lint`
- `pnpm build`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm test:release-freeze`
- `pnpm test:page-first-paint`
- `pnpm test:value-loop-guard`
- `pnpm test:contacts-release-contracts`
- `pnpm test:signature-v4-server-remote`
- anchor foreground publication contract
- `/api/receiz/anchor` local signed-action outbox exclusion contract
- broad `/api/receiz` classifier guard against foreground anchor re-admission
- anchor success and error JSON readability contracts
- profile original publish continuity and first-view/live-player handoff contracts
- profile showcase/global sync/public profile market append projection contracts
- visible route snapshot refresh contracts
- Sports Vault sealed image/manifest publish contracts
- compact QR publish and showcase publish path contracts
- public owner vault route and public event proof contracts
- default username projection for brand-new accounts without saved profile username
- Sports live/vault/FIFA/UFC/fight proof contracts
- pack claim/open retry and pending recovery contracts
- wallet first-paint cookie, ledger receipt, and settled wallet RPC contracts
- PBI upsert and Receiz session continuity contracts
- service-worker navigation and runtime coherence contracts
- lease lifecycle register, marketplace handoff, transaction room, and market ownership contracts
- SQL proof/source payload and high-pressure user-live route contracts
- release lock, regression lessons, commit history, release attestation, and conformance history gates

## Offline Verifier Repository Gates

- root package metadata resolves to `v89.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v89.0.0`
- static service-worker registrations request `sw.js?v=89.0.0`
- `site/sw.js` carries runtime version `89.0.0`
- release docs and indexes point to the `v89.0.0` release artifacts
- product-truth reference docs are linked from the public docs map and carried forward for `v89.0.0`
- broader platform validation for `v89.0.0` is captured in the canonical release note, product-truth document, process document, checklist, regression lessons, and commit-history boundary
- verifier trust semantics remain deterministic and fail-closed
- `pnpm lint` has zero warnings/errors
- `pnpm build` completes
- `pnpm release:lock` confirms governed release surfaces, regression lessons, and commit history
- whitespace diff check passes
- `pnpm release:attest` emits a signed attestation for the final commit before production shipment
- governed surfaces all show `v89.0.0`

## Release Rule

Do not ship `v89.0.0` unless the release gates in this checklist pass from the exact release candidate, `pnpm release:lock` confirms the release surfaces, regression lessons, and commit history, whitespace diff passes, focused anchor/outbox, Profile, Sports Vault, Sports, Market, Wallet, runtime/session, DB pressure, security, and release contracts pass, production migrations named by the release are applied in the target environment, and release attestation is created in the production signing environment.

