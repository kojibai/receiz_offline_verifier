# Receiz v121.0.0 Commit History

Repository boundary commit: the v121.0.0 qualification commit containing this record.

Baseline: `64cb18cdf` for the settled publication experience, with the lawful refinements through `972faab93` retained.

Commit count at the v121.0.0 qualification boundary: 5362.

Included implementation refinements:

- `64cb18cdf` — background session publication and route projection baseline.
- `78307cdb5` — settled profile proof identity protection.
- `e3ae90ef1` — sealed record media hashes bound to profile proofs.
- `d9690b7d2` — document seal proof manifest.
- `ee03e1338` — offline Groth16 notice shape preservation.
- `972faab93` — media hash preservation and live projection refinement.

The final v121 release commit records coordinated metadata, registry, documentation, package artifacts, and qualification evidence without rewriting those implementation commits.

The release retains `@receiz/sdk` durable proof memory as **first admission only, then append forever**.

## Standalone archive boundary

- Immutable standalone baseline: `v120.0.0` at
  `09dda2dcf2ff478138fd3760f6186baa9a15b2d3`.
- Coordinated application source observed on pushed `origin/main`:
  `030af97a9b4ced783351d9d13ce1602553021b24`.
- The standalone v121 commit and annotated tag are created only after its own
  release gates pass; application and standalone histories remain distinct.
