# Receiz v120.0.0 Product Truth

Developer surface: `@receiz/sdk@120.0.0`. Durable proof-memory law: first admission only, then append forever. Registry digest: `0728651789b26e1d10c1991ec1c06c1ea4a576f0c6520537b250b171f8857073`. Operation matrix digest: `1c779ee5ade4b877ae9c6922ab02ba96fffffeb7580f1cf105a59fbb4424f351`.

V120 appends from the immutable v119 registry digest `49c167a437ec7c0e486412dd62c54af4abdf94eda1ebc18d263a027d105cecd9`. It does not reinterpret or rewrite a v119 artifact.

## Frozen Primitives

The enclosing sealed proof object remains the strongest truth. A living subject has immutable `subjectId`, `proofObjectId`, `subjectType`, `ownerReceizId`, `identityDigest`, `genesisDigest`, and `createdAtKai`. Ownership and current application are append-only state; neither rewrites identity or prior history.

Every subject artifact preserves byte-exact `identity`, `canonicalState`, `selfModel`, `memory`, `relationships`, `inventory`, `abilities`, `condition`, `worldPosition`, `mandates`, `eventHistory`, `projections`, and `applicationNamespaces` namespaces. Unknown namespaces remain byte-for-byte unchanged through append, export, import, reconciliation, and transfer.

## Frozen Invariants

Subject identity, sealed proof bytes, exact Kai ordering, append-only ownership history, unknown namespaces, admitted event citations, atomic multi-subject heads, deterministic replay, and settled first paint cannot be replaced by a model response, projection, cache, database row, server response, MCP result, or latest snapshot. A failed decision performs zero writes.

## Complete proof brain

The subject brain is not a prompt containing a fixed number of objects. Its law is:

```text
canonical head
-> entire content-addressed proof history
-> Kai/Merkle/Fibonacci indexes
-> retrieve relevant references
-> resolve exact primary proof-object bytes
-> verify digests and head binding
-> reason
-> answer with provenance
```

The 96-object value is only the maximum reasoning window. It is never a history limit. Long-form text remains part of the exact primary object. Indexes, excerpts, summaries, embeddings, and model context locate truth but never become proof authority. Missing exact bytes fail closed.

## Twin and world separation

The subject Twin may speak, observe supplied facts, cite proof memory, propose structured intentions, and emit performance timing. Its provider, model, version, prompt digest, context digest, and output digest are recorded for reproducibility. None of that admits an event.

Every consequential action follows:

```text
propose intent
-> construct typed command
-> validate exact heads
-> verify owner capability or active bounded mandate
-> decide with the deterministic reducer
-> atomically append event
-> return independently verifiable receipt
-> rebuild projections
```

A rejected decision writes zero subject heads, world heads, events, projections, or Kai coordinates. Retry identity returns the original receipt and cannot duplicate the event.

## Atomic social and inventory state

Meetings, mutual relationships, battles, gifts, trades, and other multi-subject actions stage all affected artifacts against exact expected heads. Admission advances every participant and the world together. Stale, unauthorized, malformed, or conflicting input advances none.

Factual subject memory cites admitted event IDs. Relationship values and inventory ownership change only through admitted events. Derived memory summaries and projections can always be rebuilt through an exact head.

## Autonomous mandates and runtime

One owner capability activates one exact mandate digest. Every autonomous lease rechecks current owner, active status, exact subject/world heads, expiry, action, targets, target owners, region, battle policy, inventory action, item value, daily exposure, approval threshold, minimum interval, daily count, provider allowlist, safety policy, application, and tenant.

Pause, revocation, expiration, ownership transfer, stale head, model timeout, malformed intent, or disallowed provider produces no invented event. Catch-up is deterministically ordered and bounded.

The reference emulator serializes a tamper-evident durable snapshot containing exact proof-brain append logs, subjects, world events and heads, receipts, idempotency keys, mandates, leases, transfers, and Kai. After a crash, only an expired lease is reclaimable, and restored admission identity prevents duplicate events.

## Bearer ownership transition

A transfer instrument binds exact subject and ownership heads, recipient mode, expiry, claim capability, inventory disposition, prior-owner conversation policy, registry, and reducer. Issuance places the subject in pending-transfer custody and blocks conflicting action. Claim independently verifies exact instrument bytes and current heads before one atomic ownership transition.

The transition preserves identity, genesis, complete history, public memory, relationships, transferred inventory, provenance, and unknown namespaces. It revokes former-owner mandates and their activating capabilities, cancels queued or leased jobs, and blocks former-owner private Twin access. Autonomy remains stopped until the new owner explicitly authorizes a new mandate.

## Profile and long-form continuity

The released `bjklock` first-paint profile contains 469 complete showcase proof objects. The profile subject brain admits all complete objects, including exact long-form text, at the exact composite showcase head. The visible first paint and an opened reader are settled truth; background narration inventory may warm only for a future interaction.

Narration audio is a derived exact-digest artifact. A play control is truthful only after matching audio is ready. Narration and Twin retrieval share the same primary-object/head/digest law; narration failure cannot alter canonical state.

The SDK exports a Draft 2020-12 schema for every v120 living-subject schema identifier and packages the complete bundle at `@receiz/sdk/schemas/v120/living-subjects`.

## Release-blocking failures

- Treating 96 objects as canonical subject history.
- Letting model text, performance, an index, MCP output, a receipt, a row, or a snapshot admit an event.
- Returning latest-snapshot-wins reconciliation.
- Factual memory without event citations.
- One-sided multi-subject effects.
- Any failed decision that advances Kai or state.
- Mandate execution after revocation, expiry, owner change, or scope breach.
- Transfer that changes identity, drops an unknown namespace, ambiguously handles inventory, or leaves former-owner authority active.
- Profile first paint with fewer complete objects than the released proof bundle.
- Narration UI advertising audio that is not already ready for the exact long-form digest.

## Release Gates

The v120 identity lock, living-subject conformance, SDK/MCP/AI parity, focused profile/Twin/narration contracts, package checks, Academy archive validation, governance controls, production build, and visible browser evidence must pass on the same candidate tree before repository qualification. Publication and deployment remain later evidence.

## Residual Risks

Production currently contains no ready long-form narration derivative, so the proof reader truthfully has no Play control. Creating one requires explicit authorization to disclose the exact source proof text to the configured TTS provider and to write the derivative/readiness rows. Package publication, deployment, production smoke, and signed attestation remain separate evidence and are never inferred from repository qualification.
