# Receiz v108.0.0 Release Lock Checklist

Release records:

- [docs/releases/v108.0.0-product-truth.md](v108.0.0-product-truth.md)
- [docs/releases/v108.0.0-regression-lessons.md](v108.0.0-regression-lessons.md)
- [docs/releases/v108.0.0-performance-findings.md](v108.0.0-performance-findings.md)
- [docs/releases/v108.0.0-invariant-register.md](v108.0.0-invariant-register.md)
- [docs/releases/v108.0.0-commit-history.md](v108.0.0-commit-history.md)

The public developer SDK surface is `@receiz/sdk`, and durable proof memory remains first admission only, then append forever.

## Locked implementation

- [x] Profile clients match the exact `a1081796e90a65124d952ce5907512999dd16cb0` pre-regression baseline.
- [x] Inline rename and full profile save call `PATCH /api/profile/me` under the already-admitted same account UID.
- [x] First-party profile code has no developer-operation, SDK, MCP, AI, or database-key prerequisite.
- [x] Username uniqueness is coordinated at commit and the same account's session/profile/public projection is refreshed.
- [x] Zero `identity_keys` rows and commit-time username conflict behavior pass the local database integration.
- [x] Native Record -> Seal embeds signed owner, record, payload, claim, path, and Signature V4 continuity.
- [x] Complete-artifact verification occurs before payload extraction, including canonical carrier-byte comparison.
- [x] SDK artifact custody is runtime-issued, exact-byte, verify-before-extract, and payload-substitution resistant.
- [x] Current SDK profile update accepts authenticated profile intent only and returns the same-UID result.
- [x] Current SDK bearer ownership accepts only a verified complete artifact and returns a new verified native artifact.
- [x] The two never-applied SQL designs are absent from `supabase/migrations`, preserved only as non-deployable evidence, and forbidden from current runtime/capability dependencies.
- [x] `/developers` renders only current unversioned behavior; `/developers/v107` redirects and no `/developers/v108` route exists.
- [x] Default MCP registry/dispatch and built package pass current profile/bearer outcome contracts.
- [x] Active AI skills and built distribution pass the current proof-object continuity contracts.
- [x] Current v108 release docs, threat model, package capability inventory, and generated distributions contain no retired operation semantics.
- [x] Final constitution registry contains all final v108 laws and is sealed once with the real Kai-Klok pulse at that exact instant.

## Required release evidence

- [x] Desktop inline rename evidence confirms success, unchanged account UID, zero `identity_keys` prerequisite, and zero requests to `/api/receiz/identity/profile`.
- [x] Mobile full-save evidence confirms success, unchanged account UID, zero `identity_keys` prerequisite, and matching owner/canonical public profile projections.
- [x] The final visual-evidence index contains at least two captured artifacts with verified SHA-256 digests and no contradictory screenshot, recording, DOM snapshot, or network trace.
- [x] SDK, MCP, AI skills, developer surface, native artifact, native ownership, migration-boundary, and semantic current-surface gates pass.
- [x] Inherited proof, ownership, settlement, offline, first-paint, package, and conformance gates pass without making historical developer mechanics current authority.
- [x] `pnpm test:release-freeze` has final recorded passing output on the release tree.
- [x] Governance, evidence, and artifact-signature controls have final recorded passing output.
- [ ] Release attestation is created from the final tree and verifies successfully. Local release metadata and Ed25519 signing values are currently empty; they must be supplied by the authorized release signer.

## External release boundary

- [ ] Final v108 repository commit created on `main` after all checks pass.
- [ ] npm publication is separately authorized and performed.
- [ ] Production deployment is separately authorized and performed.
- [ ] Production database changes are separately authorized; the retired SQL designs are never applied.

Unchecked external actions are not implied by repository completion.

## Independent Verifier Local Qualification

- [x] `node scripts/check_release_lock.mjs 108.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 108.0.0`
- [x] Supplied JSON validation, registry digest/previous-digest/effective-Kai parity, and canonical/archive byte comparison
- [x] Current-surface stale-narrative scan, v108 link scan, and `git diff --check`

These checks qualify this standalone verifier checkout only. They do not create the pending production attestation, publish packages, deploy production, mutate or verify a production database, push Git refs, or establish authenticated production-browser evidence.
