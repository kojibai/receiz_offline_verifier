# Receiz v126.0.0 Migration

The SQL registry admission migration is required for hosts running the existing v125 lawful-action SQL functions before accepting v126 issuance. It admits the exact v126 successor alongside v125 without rewriting historical receipts, sealed objects, original creation pulses, or ownership history.

1. Apply `supabase/migrations/20260908021500_v126_lawful_action_registry_successor.sql` transactionally. Its exact-definition guards reject an unexpected existing function body. Do not replay superseded historical migrations.
2. Install the coordinated `126.0.0` SDK, MCP and AI skills packages after their npm publication is verified; refresh the Academy lockfile from actual registry integrity records.
3. Deploy the application and Academy from the qualified release commit, then verify public proof, Showcase first paint and the PWA against the deployed version.
4. Preserve historical wire names and sealed coordinates. Use the current compiler registry for new admissions and existing compatibility readers for historical proof objects.

The SQL migration passed isolated PostgreSQL tests for retries, source custody, ownership appends, tampering and large sources. Production application has not been performed as part of this release preparation.
