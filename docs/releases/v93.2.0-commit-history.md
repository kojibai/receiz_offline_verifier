# Receiz v93.2.0 Commit History

Release date: 2026-06-26

Baseline: `v93.1.0` release-lock working tree after `@receiz/sdk@93.1.0` publication.

Commit count: pending local release slice.

This release records the SDK developer-experience work added after `v93.1.0`:

- bumped active app and SDK release surfaces to `93.2.0`
- added SDK runtime schema exports
- added asset and Sports card projection helpers
- added append-only SDK proof memory register
- added proof memory/projection SDK quickstart docs
- added SDK tests for schema exports, projections, and duplicate-safe proof memory

The historical `v93.1.0` release documents remain the durable proof memory release history.

## Release-Lock Requirement

`pnpm release:lock 93.2.0` must require this document for `v93.2.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the SDK slice baseline, publication boundary, and mechanical ledger would not be explicit release truth.
