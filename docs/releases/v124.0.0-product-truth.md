# Receiz v124.0.0 Product Truth — Reality Becomes Infrastructure

**The Production Runtime for Verified Civilization.** The sealed proof object is storage and continuity authority. Exact authenticated heads authorize accepted progression. V124 infrastructure coordinates these primitives; it does not replace them.

Durable proof memory remains first admission only, then append forever across `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills`.

## Frozen Primitives

1. `receizKaiNow()` derives `{ pulse, uPulse }` from elapsed time since `GENESIS_EPOCH_MS = 1715323541888` with `PULSE_SECONDS = 3 + sqrt(5)` through the existing phi-exact ties-to-even bridge. Current UTC supplies the observation; pulses since genesis supply the coordinate and ordering law.
2. Proof-authority challenges preserve V123 whole-pulse `issuedAtKai` semantics. A newly versioned field is required for explicit micro-pulse issuance or expiry.
3. Runtime responses are request-bound and authenticated by stronger proof/head evidence. A caller-selected `baseUrl`, transport success, JSON shape, database row, SDK value, or TLS connection cannot establish Receiz authority.
4. Durable plans bind application audience, exact inputs, authority evidence, expected heads, operation identity, and idempotency key before execution.
5. Exactly-once execution resolves to committed, zero-write, or unknown. Unknown is not permission to retry without recovery.
6. Replay, checkpoints, additions, and namespace projections enter application callbacks only after exact schema, discriminator, status, signature/proof, predecessor, head, expiry, and revocation validation.
7. Private additions are filtered by verified access before hydration; hidden coordinates and recipient membership are not leaked by transport responses.
8. World, inventory, material, Settlement, and Reserve effects advance every declared domain or none. A rejected transaction writes zero accepted state.
9. Recipient resolution is privacy-safe, rate-limited, consent-bound, and non-enumerable. A resolver result is a transfer coordinate beneath the enclosing ownership/transfer proof.
10. Capability descriptors report whether every required production dependency is operational with evidence. Method presence or configuration alone is insufficient.

## Frozen Invariants

- Receiz law and invariant register
- sealed artifact truth and enclosing proof object
- deterministic proof-object state and verified local truth
- verified append/register block and exact authenticated head
- authenticated snapshots and server append
- database/session/UI projection

No V124 runtime representation may travel upward through that hierarchy. Supabase or another hosted store may synchronize and recover durable state; it does not become proof, storage, ownership, identity, Settlement, Reserve, or continuity authority.

## Universal boundary

V124 adds no consumer-branded code path, application registration, principal admission, public authenticator, parallel signature scheme, or new issuer. Existing application/principal admission remains authoritative. The production runtime primitives accept declared application/audience coordinates and fail closed when the existing authority chain cannot authenticate them.

## Release Gates

- Coordinated SDK, MCP, AI Skills, constitutional-verifier, registry, compatibility, and package identity must remain exact at `124.0.0`.
- Private-envelope, authenticated-runtime, replay, checkpoint, exactly-once execution, atomic world/value, and recipient-privacy adversaries must fail closed.
- Release lock, deterministic surfaces, package qualification, documentation closure, governance evidence, and the complete release freeze must pass without weakening historical V123 law.
- Publication, migration, deployment, smoke, tag, and signed attestation must be recorded only from their exact external evidence.

## Residual Risks

Implementation in the repository is not package publication, migration application, deployment, production smoke, scale, adoption, tag, or signed attestation evidence. Those external stages remain unperformed or unverified in the V124 candidate record.
