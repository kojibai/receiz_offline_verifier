# Receiz v110.0.0 Commit History

The `@receiz/sdk` durable proof memory law remains **first admission only, then append forever**.

Baseline: `9d23332d8` (v109 local offline verification release).

Commit count: 12 implementation/design/release-lock commits before the final release-evidence commit.

- `21f689dbc` — v110 unified admission/recovery design.
- `e9b281c6f` — executable v110 implementation plan.
- `82b61b8bd` — artifact admission contracts and structured errors.
- `3186a691a` — verified deterministic proof history.
- `e39e53a19` — unified local artifact admission.
- `c41230adf` — MCP artifact-seal planner active-registry fix.
- `ee7ec9e77` — read-only recovery planning and `admitAndRecover`.
- `27b4fde6c` — capability-bound atomic recovery commit.
- `b609f90f0` — client surface and multi-application convergence.
- `46684678c` — MCP admission/recovery orchestration.
- `177398c45` — coordinated v110 package, registry, AI, generated distribution, documentation, and visible version-surface release.
- `13574cfad` — artifact registry gate bound to the active v110 law set while preserving historical fixture checks.

The coordinated release advances package, registry, AI, generated distribution, release-document, and visible identification surfaces to v110 without changing Receiz.com or database logic.

The upstream hashes above describe the coordinated Receiz.com/package workspace. This standalone offline-verifier release starts from local annotated tag `v109.0.0` at commit `9f51282c9c9d1f3ee288930b95556ed387d39e4b`; its final local v110 commit is recorded by this repository's Git history.

## Release-Lock Requirement

`pnpm release:lock 110.0.0` must require this commit-history record for `v110.0.0` and later release cuts. Upstream implementation history and standalone verifier history remain distinct while preserving one constitutional digest and the same sealed-artifact authority boundary.
