# Receiz v70.0.0 Checklist

Release date: April 24, 2026
Status: Release prep

## Release Scope

- Current documented release anchor in this repository: `v66.0.0`
- Broader product baseline referenced by this release: `v60.0.0` through final `v69.9.x` stabilization
- This checklist governs the `v70.0.0` public alpha documentation cut and outward version-surface alignment

## Required Release Artifacts

- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v70.0.0.md`
- `docs/releases/v70.0.0-product-truth.md`
- `docs/releases/v70.0.0-checklist.md`
- `docs/releases/v70.0.0-process.md`
- `docs/releases/v70.0.0-growth-from-v60.md`
- `docs/releases/v70.0.0-invariant-register.md`
- `docs/releases/v70.0.0-docs-manifest.md`
- extra public docs accounted for in the docs manifest, including `docs/FORMAT.md`, `docs/ARCHITECTURE.md`, `docs/COMPATIBILITY.md`, `docs/HARDENING.md`, `docs/THREAT_MODEL.md`, `docs/DEPLOYMENT.md`, `docs/COPY_GUIDE.md`, `docs/receiz-standard-v1.md`, `docs/presencebound-canonical-realm.md`, `docs/conformance/README.md`, `docs/schemas/README.md`, and `docs/governance/README.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`

## Database Requirements

- no new database migration is required by this offline verifier repository documentation cut
- existing production schema requirements from prior value-loop, world/profile, identity, and recovery releases still apply in the broader platform workspace

## Canonical Product Gates

- Receiz ID is the primary everyday login rail.
- `/signin` is a pass-through route, not a destination page.
- Existing local Receiz ID accounts go directly to the managed profile.
- Cold devices create a local Receiz ID and continue directly to the new managed profile.
- PBI, keyfile, email, and magic-link paths remain recovery and interoperability rails.
- PBI/keyfile recovery binds back to local Receiz ID truth when proof verifies the identity root.
- `/<username>` is the profile.
- `/<username>?manage=1` is the same profile with owner powers layered on top.
- Owner data may append controls and hidden panels, but it cannot redefine first-paint identity, showcase, media, or shell.
- Settlement remains proof-native value.
- Reserve remains funded value for external rails, notes, sends, positions, and wire transfer.
- Notes stay Reserve-only.
- Certificates and shares bridge live proof-native value into buyer-funded Reserve.
- Market sells preserve funded/unfunded causality and patch visible truth from the known trade result.
- Receiz ID, PBI/keyfile proof, route snapshots, wallet graphs, market positions, twin state, and proof bundles are real local truth.
- Server work publishes, mirrors, mints cookies, recovers, discovers, and syncs verified additions.
- The user does not wait for network permission when the device already holds stronger proof.
- Stale runtime, stale service-worker, and stale session states recover through local proof or server resync.
- Hard runtime recovery returns to the route the user was on instead of dumping them to home.
- Live twins cannot fall back to a profile picture as an identity substitute.
- Twin images stay locked to the current per-user/per-twin image.
- Mind trail ZIP downloads package every file with Receiz proof material so the downloaded bundle verifies as a proof bundle.
- Home hero proof-card PWA corrections are scoped to standalone PWA display only.

## Focused Platform Verification

Release-prep commands expected in the broader platform workspace:

- `pnpm exec tsc --noEmit --pretty false`
- `pnpm test:deterministic-surfaces`
- `pnpm test:release-freeze`
- `pnpm release:lock`

Focused suites expected in the broader platform workspace:

- Receiz ID existing-account pass-through tests
- cold-device local Receiz ID creation tests
- `/signin` pass-through route tests
- PBI/keyfile recovery binding tests
- email and magic-link interoperability tests
- canonical public profile and owner-mode profile-law tests
- Settlement/Reserve account-lane and causality tests
- Reserve-only note issuance tests
- certificate funding and sale-credit causality tests
- market funded/unfunded sale split tests
- local truth preservation tests for snapshots, wallet graphs, market positions, twin state, and proof bundles
- stale runtime route-preserving recovery tests
- service-worker verified-snapshot persistence tests
- live twin identity image-lock tests
- mind trail ZIP proof-bundle verification tests
- standalone-PWA-only home hero proof-card tests

## Offline Verifier Repository Gates

- root package metadata resolves to `v70.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v70.0.0`
- current release docs and indexes point to the `v70.0.0` release artifacts
- product-truth reference docs are linked from the public docs map
- broader platform validation for `v70.0.0` is captured in the canonical release note, product-truth document, growth record, process document, checklist, and invariant register
- extra public documentation is captured in `docs/releases/v70.0.0-docs-manifest.md`
- verifier trust semantics remain unchanged relative to `v60.0.0`
- no verifier proof-format or producer payload migration is implied by these repository release/documentation updates alone

## Release Rule

Do not ship `v70.0.0` unless the release gates in this checklist pass from the exact release candidate and the governed surfaces all show `v70.0.0`.
