# Receiz v70.0.0 - Public Alpha Release

Release date: April 24, 2026

## Summary

`v70.0.0` is the first public alpha release cut of Receiz as an end-to-end product system.

This release packages the work from `v60.0.0` through the final `v69.9.x` stabilization line into one governed public release. It does not redefine the product at the last second. It locks the laws already proven in the code:

> Identity is local. Proof is in the file. Verified truth is append-only. `/<username>` is canonical profile truth. Settlement and Reserve preserve funded causality. The network propagates and appends; it does not get to downgrade stronger proof already held by the device.

For this offline verifier repository, the release work is publication of the canonical `v70.0.0` artifacts, public product-truth reference documents, and outward version-surface alignment. The verifier proof-format and producer-payload contract remain unchanged.

## Product-Truth Reference

- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v70.0.0-invariant-register.md`
- `docs/releases/v70.0.0-product-truth.md`
- `docs/releases/v70.0.0-growth-from-v60.md`
- `docs/releases/v70.0.0-process.md`

## Supporting Release Documentation

The extra public docs included in this release surface are tracked in `docs/releases/v70.0.0-docs-manifest.md`, including verifier format, architecture, compatibility, hardening, threat model, deployment, copy guidance, conformance, schemas, governance, PresenceBound, and the public Receiz standard.

## What This Release Locks

### One-click Receiz ID entry

- Receiz ID is the primary everyday login rail.
- `/signin` is a pass-through route, not a destination page.
- Existing local Receiz ID accounts go directly to the managed profile.
- Cold devices create a local Receiz ID and continue directly to the new managed profile.
- PBI, keyfile, email, and magic-link paths remain recovery and interoperability rails.

### Canonical profile truth

- `/<username>` is the profile.
- `/<username>?manage=1` is the same profile with owner powers layered on top.
- Owner data may append controls and hidden panels, but it cannot redefine first-paint identity, showcase, media, or shell.

### Complete proof-native value loop

- Settlement remains proof-native value.
- Reserve remains funded value for external rails, notes, sends, positions, and wire transfer.
- Notes stay Reserve-only.
- Certificates and shares bridge live proof-native value into buyer-funded Reserve.
- Market sells preserve funded/unfunded causality and patch visible truth from the known trade result.

### Local truth production baseline

- Receiz ID, PBI/keyfile proof, route snapshots, wallet graphs, market positions, twin state, and proof bundles are real local truth.
- Server work still runs. It publishes, mirrors, mints cookies, recovers, discovers, and syncs verified additions.
- The user does not wait for network permission when the device already holds stronger proof.

### Runtime and offline resilience

- Stale runtime, stale service-worker, and stale session states recover through local proof or server resync.
- Hard runtime recovery returns to the route the user was on instead of dumping them to home.
- Offline verifier surfaces, service-worker defaults, package metadata, and public version badges move together to `v70.0.0`.

### World, live player, and twin identity

- Live twins cannot fall back to a profile picture as an identity substitute.
- Twin images stay locked to the current per-user/per-twin image.
- Mind trail ZIP downloads package every file with Receiz proof material so the downloaded bundle verifies as a proof bundle.
- Home hero proof-card PWA corrections are scoped to standalone PWA display only.

## Since v60.0.0

The full plain-language growth record is in `docs/releases/v70.0.0-growth-from-v60.md`.

In short, Receiz moved from a strong live proof product into a complete public operating surface:

- live proof front door
- holder-aware market truth
- historical continuity governance
- Settlement/Reserve value loop
- one-click Receiz ID login
- local-first/server-always recovery
- profile-law restoration
- conformance isolation
- custom-domain and in-app browser visual normalization
- truthful logout final state
- follow-login continuation
- route-preserving stale-runtime recovery
- proof-sealed world trail bundles
- v70 invariant register

## Verifier Impact

- Current shipped verifier, studio, and settlement entrypoints are marked `v70.0.0`.
- Repository release/docs surfaces now align to `v70.0.0`.
- Root package metadata resolves to `v70.0.0`.
- No verifier proof-format or producer payload migration is implied by this release update.
- Verifier trust semantics remain unchanged relative to `v60.0.0`.

## Preserved Verifier Contract

- Trusted-signature success still requires verified `signatureV4`.
- Trusted-signature failure semantics remain fail-closed:
  - invalid present `signatureV4` -> `Trusted signature invalid`
  - unavailable present `signatureV4` -> `Trusted signature unavailable`
  - missing `signatureV4` -> `Trusted signature missing. Expected signatureV4.`
- Effective anchor context remains required.
- Groth16 fields remain required and only real `g16:` proof payloads are accepted.
- Carrier extraction/normalization for PNG, PDF, SVG, JSON, trailer, and `.receizbundle` remains unchanged.
- Package ZIP/folder manifest verification paths remain unchanged.
- Canonical identity derivation and artifact-binding checks remain fail-closed.

## Release Rule

Do not ship `v70.0.0` unless the release gates in `docs/releases/v70.0.0-checklist.md` pass from the exact release candidate and the governed surfaces all show `v70.0.0`.
