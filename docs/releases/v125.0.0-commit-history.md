# Receiz v125.0.0 Commit History

**Reality-Grade Infrastructure — Source + Law + Sequence + Object + Evidence. Then the Reader.**

- Baseline: `a32399e29609bf290ac9d8250db941ee0ff1f52b`
- Final implementation boundary before release-document closure: `381019f38`
- Commit count: `125` above baseline before this release-document commit
- Repository delta above baseline before this release-document commit: `763 files changed, 93,438 insertions, 6,326 deletions`

The coordinated `@receiz/sdk` surface preserves durable proof memory as **first admission only, then append forever**.

## Implementation arcs

1. **Proof-native economy.** Glyph Settlement projection, lawful-action constitution, one-membership custody, wallet/market execution, and sender/receiver edge settlement.
2. **Showcase continuity.** Exact accepted heads, immediate local append visibility, deterministic first paint, sealed media continuity, identity filtering, and no replacement-object flash.
3. **Identity continuity.** Receiz ID recovery, exact multi-account switching, canonical creation pulses, Creation Sigil persistence, and verified identity-genesis projection.
4. **Framework-neutral infrastructure.** Generated application contracts, authority-safe adapters, recursive known-proof preservation, sealed generator metadata, and external-app conformance.
5. **Attestation and credentials.** Closed schemas, authorized role grants, verified evidence, causal reducers, actor proof, evaluation law, issuance, lifecycle transitions, and time-scoped revocation.
6. **Temporal authority.** Persisted KKS coordinates, strict monotonic live issuance, KaiSigil proof verification, Chronos subordination, legacy compatibility, and temporal conformance.
7. **Coordinated distribution.** One generated release identity across SDK, MCP, AI Skills, HTTP/OpenAPI/OIDC, package docs, built artifacts, and release locks.
8. **Production closure.** Eight ordered schema migrations, wide-screen proof-surface continuity, compiler fixes, MCP dist regeneration, profile legacy recovery, and release gate hardening.

## Post-publication operational coordinates

The published package evidence and implementation census above retain their original source boundary. Fresh provider evidence in the [September 5 operational update](v125.0.0-operational-update-2026-09-05.md) confirms mobile source-custody repair `c4ea6f15a37f2cb60af100b909591632d45321ab` deployed, superseding the earlier b8 deployment observation. The user confirms mobile sealing fixed live. It is not retroactively part of the original published package bytes.

The applied SQL rollback, release-document commit, Git tag, Docs deployment and signed attestation are separate coordinates. This record does not infer any unobserved result from another coordinate.

## Standalone Archive Boundary

The standalone v125 release starts from predecessor `325a38c44361de7550fa7fd406ee387dbc86cecd`, the commit targeted by its annotated `v124.1.0` tag. It imports the exact v125 verifier and Offline Studio from read-only application commit `ddca6196ad9f1e845d28dc6796de4d2818c7e4d3`, archives all 26 canonical source records, aligns local release surfaces, qualifies the static distribution, and records one standalone release commit and annotated tag.

This additional standalone chronology does not modify or rewrite the application history described above. It creates no package-publication, database, deployment, production-smoke, or signed-attestation evidence, and it performs no remote push.
