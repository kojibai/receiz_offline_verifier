# Receiz v124.0.1 — Reality Becomes Infrastructure

Release date: August 23, 2026

**The Production Runtime for Verified Civilization.** `v124.0.1` is the coordinated corrective release for the V124 runtime. It completes developer, MCP, AI, Academy, identity-import, and portable receipt continuity without changing the immutable V124 constitutional ruleset or allowing package, server, database, session, or model state to outrank a sealed proof object.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk`, MCP, AI, server, and database mechanics may verify, execute, synchronize, or recover beneath that admitted proof history; they cannot replace it.

## Corrective Release Outcome

- Every lawful delegated Receiz capability is selectable from the universal developer OIDC surface and available to delegated agent-token setup.
- The public MCP package exposes direct adapters for all 22 V124 SDK operations and publishes the complete current MCP inventory to AI and Academy projections.
- V124 handle, session, sealed material, replay candidate, and private-additions custody remain closed beneath canonical SDK/server verification.
- Saved receipt coordinates remain independently resolvable when database and anchor projections are unavailable, while altered coordinates still fail closed.
- The Academy identity workbench imports an Identity Record, Identity Seal, or Receiz Key directly; account redirection is not the identity authority path.
- The pre-existing V124 registry digest, `RECEIZ_RULESET_VERSION = "124.0.0"`, constitutional schemas, and machine-law identifiers remain byte-unchanged. New `.v124` MCP carrier identifiers describe additive, subordinate package representations; they do not amend the constitutional registry or reinterpret an existing field.

## Release Boundaries

`@receiz/ai-skills`, `@receiz/sdk`, and `@receiz/mcp-server` publish at `124.0.1` in dependency order. Their compatibility range remains `>=124.0.0 <125.0.0`, and their ruleset binding remains exactly `124.0.0`.

Publication, deployment, production smoke, Git tagging, and signed attestation remain separate evidence dimensions. This repository record does not claim any of those external operations before their exact evidence exists.

## Authority

The sealed proof object and Receiz identity artifact remain source authority. OIDC scopes, access tokens, MCP references, SDK objects, durable journal rows, sessions, and Academy projections authorize, transport, synchronize, recover, or display beneath that stronger truth; none becomes identity or proof authority.

Release law is frozen in [product truth](v124.0.1-product-truth.md), the no-new-migration adoption boundary is explicit in [migration](v124.0.1-migration.md), regression prevention is frozen in [regression lessons](v124.0.1-regression-lessons.md), and repository provenance is bounded in [commit history](v124.0.1-commit-history.md).

Canonical paths: `docs/releases/v124.0.1-product-truth.md`, `docs/releases/v124.0.1-migration.md`, `docs/releases/v124.0.1-regression-lessons.md`, and `docs/releases/v124.0.1-commit-history.md`.

## Standalone Repository Boundary

This standalone repository projects the verified-file surfaces, release documentation, and cache identity at `124.0.1`. It does not vendor the application, SDK, MCP server, AI Skills, Academy, OIDC routes, or receipt service implementation described above.

The source application repository was read without modification. Its latest committed candidate and final observed `origin/main` boundary for this archive are `b4efb56c72780bd9b5013ef466fdea998701b96b`. The standalone predecessor is `35d3a440e2252dd8146e0b7f443824b19c0cf852` at annotated tag `v124.0.0`.

The standalone package and visible release identity advance to `124.0.1`. The immutable V124 ruleset remains `124.0.0`, registry digest remains `d02429151b0bcebdaeb89485792e377afc55130f9a25e07982c1c88221314247`, application-operation matrix digest remains `540d1c1bf39f1b288b257c79a6e020bdcc5e587fc9b7dbf6b7aaa5d082e20ad5`, and compatibility remains `>=124.0.0 <125.0.0`.

No application-repository write, package publication, production deployment, database mutation, remote push, production smoke, or signed attestation is performed or inferred by this standalone release lock.
