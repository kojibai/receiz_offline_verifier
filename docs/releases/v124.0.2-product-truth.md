# Receiz v124.0.2 Product Truth — Reality Becomes Infrastructure

Release date: August 23, 2026

**The Production Runtime for Verified Civilization.** Package identity is `124.0.2`; the immutable constitutional ruleset remains `124.0.0`.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk` cannot replace the admitted sealed proof object.

## Frozen Primitives

- The sealed proof object, its exact native bytes, proof history, and authenticated heads remain authority.
- A developer domain may verify and render carried material, but its page is only a presentation surface.
- The SDK and MCP may verify, reconstruct, and project; neither becomes artifact authority.
- An identity glyph uses the exact sealed account creation pulse. Device or wall-clock state cannot replace that coordinate.
- Historical sealed artifacts remain byte-identical and verifiable.
- V125 monotonic head-relative temporal continuation is not represented as complete in `124.0.2`.

## Frozen Invariants

- Package identity is `124.0.2`; constitutional ruleset identity remains `124.0.0`.
- SDK and MCP compatibility remains `>=124.0.0 <125.0.0`.
- `rma2` retains fully inline exact custody; `rmc1` retains a bounded public head over exact ordered append custody.
- Reconstruction verifies complete capsule commitments and the enclosing sealed proof object before native payload extraction.
- Profile Composer publication cannot replace a carried proof coordinate with storage-only video state.
- The canonical standalone offline verifier runtime remains byte-identical inside Offline Studio.

## Release Gates

- `pnpm test:release-freeze`
- `pnpm test:v124-release-lock`
- `pnpm test:offline-record-seal-freeze`
- `pnpm test:primitive-binding-contract`
- `pnpm test:source-authority-contract`
- `pnpm test:repo-only-reasoning-boundary`
- profile composer, composite material transport, SDK, MCP, AI Skills, typecheck, build, and visual-evidence contracts

## Residual Risks

- The supplied 76 MB sealed video has visually confirmed `/v` playback, but a newly posted public-profile cold-load playback has not yet been captured as visual evidence.
- Coordinated package publication and immutable `v124.0.2` Git tagging remain pending external operations.
- The full release freeze must pass before release qualification; no partial gate run substitutes for that result.

The pre-existing V124 registry digest is unchanged. These are additive, subordinate package representations compatible with `>=124.0.0 <125.0.0`.

## Standalone Projection

The standalone `apps/offline-verifier.html` is byte-identical to the canonical application source with SHA-256 `1737d982c7f5149b68369cea9e55d34c2a81f1321767fb8b52eff3873cb0c4d9`. The standalone `apps/offline-record-seal.html` is byte-identical to the canonical application source with SHA-256 `3fb9592540e6026cd172d52df7c117417e8f02105a8d13010dcf09fc4df4d723`.

The changed offline-verification surface accepts the new `rpb2/nativeRecordSeal` compact projection and adds proof-URL material reconstruction. The implemented primitive is still the enclosing sealed proof object. URL fragments, IndexedDB append segments, QR transport, SDK/MCP projections, and presentation domains remain weaker transport or display mechanics. Reconstruction fails closed unless segment commitments, capsule binding, canonical verifier result, and route binding all agree.
