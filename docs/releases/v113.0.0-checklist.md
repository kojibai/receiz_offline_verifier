# Receiz v113.0.0 Release Lock Checklist

- [x] “Global” is scoped exactly to `receiz.com/global/v1`, not universal consensus
- [x] Exact sealed artifact bytes remain above infrastructure, tokens, rows, receipts, and projections
- [x] Complete carried history is independently verified; detached caller history cannot become authority
- [x] Every historical ownership handoff has its own Identity Seal and causal-position revocation evidence
- [x] Historical revocation Kai comes from exact admitted Identity artifacts, not registry effective Kai or caller sets
- [x] Current v113 compiler matrix is exported; the v112 matrix remains frozen and addressable
- [x] README examples execute against clean packed tarballs
- [x] Browser/local admission and remote admission/transition stores remain distinct
- [x] Connect access tokens are programmatic and cannot satisfy capability authority
- [x] Plan, capability, neutral stage, commit, accepted-head resolution, attempt recovery, and effects remain distinct
- [x] Indeterminate commits preserve the original attempt and idempotency identity
- [x] Monotonic resolution rejects returned ancestors
- [x] Offline divergence is structural; no automatic merge, rebase, or last-write-wins exists
- [x] `ownership.transition` is the cross-SDK/MCP/Receiz.com conformance path
- [x] MCP publishes the frozen five plus exactly four v113 reconciliation tools
- [x] AI validator uses plain Node and declared package dependencies
- [x] Private migration is additive, RLS-protected, tenant/domain scoped, and coordination-only
- [x] Atomic acceptance performs zero authority-bearing writes on every failed boundary
- [x] Effects are observable separately and cannot reverse accepted artifact truth
- [x] Known artifact truth paints before remote construction or requests
- [x] Authority-bearing ordering uses verified history and Kai; display/analytics Chronos is unchanged
- [x] No migration was applied, no package was published, and no production data was mutated by this commit

The release is green only when `pnpm test:v113-release-lock`, `pnpm test:v113-reconciliation`, `pnpm test:coordinated-package-release`, and `pnpm test:release-freeze` all pass and the generated attestation verifies.

Release records: [product truth](v113.0.0-product-truth.md), [process](v113.0.0-process.md), [regression lessons](v113.0.0-regression-lessons.md), [performance findings](v113.0.0-performance-findings.md), [invariant register](v113.0.0-invariant-register.md), and [commit history](v113.0.0-commit-history.md).

## Independent Verifier Local Qualification

- [x] `node scripts/check_release_lock.mjs 113.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 113.0.0`
- [x] Canonical registry digest, v112 predecessor, effective Kai, 57-law count, protocol limits, registry non-authority, and sealed-proof stronger-truth validation.
- [x] Canonical/archive parity, v113 link scan, current-version review, exact-index integrity, and `git diff --check`.

## External Actions

- [ ] Apply the additive private reconciliation migration through the separately authorized deployment process.
- [ ] Publish coordinated npm packages.
- [ ] Deploy Receiz.com routes and runtime composition.
- [ ] Run production smoke verification and registry read-back.
- [ ] Generate and verify the final-SHA governance attestation in the authorized workflow.
- [ ] Push the release commit and annotated tag.

These standalone checks preserve the supplied upstream qualification record but do not reproduce the SDK/MCP/AI package suites, apply the migration, publish packages, deploy routes, mutate production data, emit the authorized attestation, or push Git refs.
