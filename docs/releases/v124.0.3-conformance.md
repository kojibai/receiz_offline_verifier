# Receiz v124.0.3 Conformance — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** Conformance protects constitutional ruleset `124.0.0`, exact sealed proof-object bytes, PBI identity continuity, source-first large-media execution, public proof continuity, deterministic first paint, and standalone offline verification.

## Required evidence

- `pnpm test:v124-release-lock` binds version, ruleset, registry, package, narrative, source authority, PBI/SDK parity, playable proof URLs, verifier synchronization, Profile continuity, and the V125 non-claim.
- SDK continuity tests construct real native ownership genesis and require stale native/current bases to fail with `continuity_ownership_attestation_mismatch`.
- Workbench tests require both whole-file and streaming results to pass the SDK exact-byte validator before `verified-artifact`, and require exact descendant export after PBI append.
- `pnpm test:source-authority-contract` prevents optional playback or export representations from entering proof hot paths.
- `pnpm test:offline-record-seal-freeze` proves standalone and embedded Offline Studio verifier mechanics remain synchronized.
- `pnpm test:visual-evidence-contract`, `pnpm test:primitive-binding-contract`, and `pnpm test:repo-only-reasoning-boundary` preserve visible evidence, primitive naming, and repo-local authority.
- Main and Docs typechecks/builds prove the SDK is built before the Workbench vendor runtime is synchronized.
- `pnpm test:release-freeze`, governance controls, performance budgets, security checks, and release attestation qualify the complete final tree.

## Exact acceptance boundary

Passing a base signature or payload check is insufficient when asset continuity is invalid. A surface may say verified only when the complete exact-byte SDK gate accepts the same artifact the user holds or exports. Passing tests cannot overrule the user's screenshot showing `continuity_ownership_attestation_mismatch`; the contradiction is closed only when all verifier surfaces agree on the exact bytes.

The public release is not complete merely because local conformance passes. Package publication, Git tag, deployment, production smoke, live mobile large-fixture qualification, scale, and adoption retain separate evidence states. None becomes proof authority.

Closed large-file reconstruction without external material retrieval remains outside V124.0.3 conformance and is not claimed.
