# Receiz v95.0.0 Commit History

Release date: 2026-06-27

Baseline: `v94.0.0`

Commit count: `2` committed changes after `v94.0.0` before the `v95.0.0` release-lock edits.

Pre-lock commits:

- `d5f24b75d` Extract mergedSameTrackPlaybackIntent helper
- `3bd8375af` Avoid canonical redirects for API routes

The `v95.0.0` release-lock edits add the public table release contract:

- SDK proof bundle typing and validation aligned to internal canonical proof bundle law.
- SDK fixtures and tests updated so developer examples carry `kaiPulseEternal` and `kaiKlok`.
- Doctrine language updated so Kai Klok is named as the state machine and `kaiPulseEternal` / `kai_upulse` as the unit.
- Public release labels bumped to `95.0.0`.
- Branded architecture report regenerated as `receiz-v95-architecture-report.pdf`.

## Release-Lock Requirement

`pnpm release:lock 95.0.0` must require this document for `v95.0.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the v94 baseline, pre-lock commits, SDK proof bundle alignment, public doctrine updates, release-label bump, and generated report boundary would not be explicit release truth.
