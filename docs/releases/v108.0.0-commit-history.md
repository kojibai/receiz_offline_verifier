# Receiz v108.0.0 Commit History

Baseline: tagged v107.0.0 repository state at `02968f686`

Commit count: 10 recorded correction commits before the final v108 qualification commit

The baseline and correction hashes in this document describe the upstream Receiz.com implementation history. This independent verifier repository starts its v108 release-surface promotion from local annotated tag `v107.0.0` at commit `901f9d9809080c0ce57427dcb27817e5c3f64754`. Neither repository history outranks sealed proof truth.

## Historical anchors

- `a1081796e90a65124d952ce5907512999dd16cb0` is the exact working profile baseline.
- `9611cb8f57c587eae1b6ee8079ee6d7bd2a02637` introduced the first-party profile regression.
- `02968f686` is the tagged v107.0.0 repository baseline from which the v108 correction sequence proceeds.

## Recorded correction sequence

The following commits are present in repository history before the final v108 qualification record:

- `03a71e219` restores Receiz.com profile-save authority.
- `4b416ec6d` completes the Receiz.com profile-authority contract.
- `52f4e81da` hardens the profile-authority regression matcher.
- `42612c964` isolates v107 developer rails from established Receiz.com behavior.
- `dc81d4838` preserves the service-worker activation contract.
- `fb5f3bcfc` rebinds developer profile adapters beneath Receiz.com behavior.
- `8b7f62ef8` pins the exact pre-regression profile client behavior.
- `0fc4fe36d` preserves stable explicit-key SDK error semantics.
- `4cb2caa95` makes `/developers` the canonical proof-first surface.
- `cd5268e44` hardens v108 developer profile adapters and their boundary contracts.

These hashes document repository mechanics. They do not outrank sealed proof objects, accepted identity proof, visible mutation evidence, or release gates.

## Final-history boundary

No prospective commit hash or tag is asserted as fact. The final v108.0.0 repository history is the actual Git range recorded after the release tree passes focused gates, non-contradictory browser evidence, the full release freeze, and governance verification. This document must not invent the identity of a release-evidence commit before that commit exists.

Repository history also does not assert npm publication, production deployment, production database verification, or production signing. Those are separate externally authorized actions with separate evidence.

`@receiz/sdk` commit identity remains mechanics beneath proof truth. Receiz proof memory remains first admission only, then append forever.

## Release-Lock Requirement

`pnpm release:lock 108.0.0` must require this document for `v108.0.0` and later release cuts. A release that omits the actual historical baseline and correction sequence cannot claim that Receiz.com profile behavior, native proof-object custody, developer adapters, developer documentation, service-worker continuity, registry sealing, and qualification evidence were coordinated beneath stronger proof truth.
