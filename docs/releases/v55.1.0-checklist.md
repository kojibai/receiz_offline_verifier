# Receiz v55.1.0 Checklist

Release date: April 16, 2026
Status: Release prep

## Release Scope

- Current documented release anchor: `v55.0.0`
- No dedicated `v55.0.1` release note or tag exists; this checklist governs the full post-`v55.0.0` delta rolled into `v55.1.0`

## Required Release Artifacts

- `docs/releases/v55.1.0.md`
- `docs/releases/v55.1.0-product-truth.md`
- `docs/releases/v55.1.0-checklist.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`

## Database Requirements

- `supabase/migrations/20260416025923_market_nonentitled_usd_execution.sql`
- `supabase/migrations/20260416041200_market_prediction_nonentitled_usd_execution.sql`

## Focused Local Verification

- `pnpm exec tsc --noEmit`
- `node --test app/lib/profile/__tests__/profileRouteContinuityContract.test.ts app/lib/profile/__tests__/composerPublishContinuityContract.test.ts`
- `node --test app/components/layout/__tests__/bottomBarMarketNavigationContract.test.ts app/lib/account/__tests__/accountRouteFeedbackContract.test.ts`
- `node --test app/lib/market/__tests__/marketRouteHydrationContract.test.ts app/lib/market/__tests__/marketValuePillContract.test.ts app/lib/market/__tests__/marketTradeContinuationContract.test.ts`
- `node --test app/lib/market/__tests__/shareTradeUsdSettlementContract.test.ts app/lib/market/__tests__/predictionTradeUsdSettlementContract.test.ts`
- targeted eslint across the touched profile, layout, market, and release-contract surfaces during release prep

## Product Gates Before Tag

- confirm passkey auth, session bootstrap propagation, and magic-link fallback all operate on the same market/chart continuation path
- confirm Stripe embedded checkout returns through the exact pending trade continuation path for both chart and desk funding
- confirm `/profile` direct load, search navigation, logged-in navigation, and publish flows stay fully hydrated without shell fallback or second settle
- confirm chart and market `Buy` resume the exact order after funding, and closing funding after passkey still leaves the new account signed in
- confirm chart `Sell` only appears when the viewer has a real held position in that asset and uses USD receive input instead of raw share input
- confirm live-player back-card copy fits in frame and public twin presence is deterministic when the route conditions are valid
- confirm public version badges, offline verifier surfaces, service worker defaults, and release docs all move together when the final `v55.1.0` version cut is made

## Offline Verifier Repository Gates

- current shipped verifier, studio, and settlement entrypoints display `v55.1.0`
- current release docs and indexes point to the `v55.1.0` release artifacts
- verifier trust semantics remain unchanged relative to `v55.0.0`
- no verifier proof-format or producer payload migration is implied by these repository release/documentation updates alone
