# Receiz v124.1.0 Commit History

**Reality Becomes Infrastructure — The Production Runtime for Verified Civilization.**

Baseline: `3356776a7312c6be2bf4d69b35aa316ed97b7b80` (`Cohere carried media and portable market state`)

Commit count: 1 coordinated v124.1.0 release commit is planned above the repair baseline; its exact SHA is established only after commit.

The baseline contains the application repairs: exact EPUB opening, deterministic large-material publication, settlement eviction, profile count reconciliation, immediate signature affordance, and portable market custody. The coordinated release commit binds the version, capability inventory, generated AI Skills, MCP descriptions, SDK guide, release law, and Receiz Docs projection.

The public `@receiz/sdk` release surface preserves durable proof memory as **first admission only, then append forever**; MCP and AI Skills expose the same capability map without creating a second authority.

No Git tag, package publication, deployment, production smoke, or signed attestation is inferred from repository commits.

## Standalone Archive Boundary

The standalone implementation sequence after annotated tag `v124.0.3` contains four commits:

- `ffbe7eb` — `Add nativeRecordSeal and file-trailer rpb2 support`
- `a0d991c` — `Add nativeRecordSeal support & artifact preview`
- `e70d140` — `Update offline-record-seal.html`
- `b9ef7e2` — `Update offline-record-seal.html`

`b9ef7e2a0dcb8701fcdfbd49c17c2826fbf6a70e` is the standalone predecessor for this release. The application V124.1.0 release commit and observed `origin/main` are `924e687d6a031c9bb4ca358fc7f92ea3de1066f5`; its committed release source is additionally bound by exact digests.

The standalone `v124.1.0` commit and annotated local tag are established by the release sequence after qualification. They do not imply coordinated package publication, an application deployment, remote push, production smoke, scale evidence, adoption evidence, or signed attestation.
