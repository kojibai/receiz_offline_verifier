# Receiz v87.0.0 Checklist

Release date: May 28, 2026
Status: Release prep

## Release Scope

- Current documented release anchor in this repository before this cut: `v86.0.0`.
- Broader product trail referenced by this release: `v86.0.0` tag through current release candidate.
- Post-`v86.0.0` arc: 17 commits, 77 changed files, 6,903 insertions, and 455 deletions before the final `v87.0.0` lock edits.
- This checklist governs the Offline Proof Links, QR Proof Transport, And Post-v86 Public Proof Lock cut and the outward version-surface alignment for this offline verifier repository.

## Required Release Artifacts

- `AGENTS.md`
- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/truthful-speed-invariants.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v86.0.0-product-truth.md`
- `docs/releases/v87.0.0.md`
- `docs/releases/v87.0.0-product-truth.md`
- `docs/releases/v87.0.0-checklist.md`
- `docs/releases/v87.0.0-process.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- product-truth reference status markers updated for `v87.0.0`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`
- service-worker runtime marker updated in `site/sw.js`

## Canonical Product Gates

- Receiz remains named as a proof-native artifact system.
- Offline proof remains real proof when a sealed proof bundle carries the proof material needed to verify itself.
- Compact proof token transport remains subordinate to the sealed proof bundle.
- `/v` verifies compact offline-generated signed proof bundles as `Proof` only when Signature V4 and route binding are valid.
- Coordinate-only evidence remains historical compatibility evidence.
- Profile composer publication, database rediscovery, session state, server lookup, analytics, SEO, and framework convention remain weaker than sealed proof truth.
- Offline moment QR transport projects the proof-bearing link already carried by the seal result.
- Sports single-card QR/value surfaces remain public proof surfaces.
- World Estate remains a World identity/public witness surface.
- Market/Profile asset loading and first-paint contracts preserve public proof objects.
- Service-worker identity, public version labels, package version, and product truth move together.
- Hidden hydration appends verified additions only.
- Release output states changed surface, preserved primitive, risk boundary, checks run, and no downgrade.

## Focused Platform Verification

Release-prep commands expected in the broader platform workspace:

- `pnpm lint`
- `pnpm build`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm test:release-freeze`
- `pnpm test:page-first-paint`
- `pnpm test:value-loop-guard`
- `pnpm test:contacts-release-contracts`
- `pnpm test:signature-v4-server-remote`
- compact offline proof-link encoding/decoding contracts
- `/v` proof-token route binding and Signature V4 verification contracts
- legacy `rpb1:` token decode contracts
- coordinate-only historical fallback contracts
- offline moment QR proof-bearing URL contracts
- focused Sports single-card QR/value surface contracts
- focused Market/Profile public proof object and first-paint contracts
- focused World Estate identity/public witness contracts
- RLS migration and browser storage contracts
- service-worker version coherence, release lock, release attestation, and conformance history gates

## Offline Verifier Repository Gates

- root package metadata resolves to `v87.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v87.0.0`
- static service-worker registrations request `sw.js?v=87.0.0`
- `site/sw.js` carries runtime version `87.0.0`
- release docs and indexes point to the `v87.0.0` release artifacts
- product-truth reference docs are linked from the public docs map and carried forward for `v87.0.0`
- broader platform validation for `v87.0.0` is captured in the canonical release note, product-truth document, process document, and checklist
- verifier trust semantics remain deterministic and fail-closed
- `pnpm lint` has zero warnings/errors
- `pnpm build` completes
- `pnpm release:lock` confirms governed release surfaces
- whitespace diff check passes
- `pnpm release:attest` emits a signed attestation for the final commit before production shipment
- governed surfaces all show `v87.0.0`

## Release Rule

Do not ship `v87.0.0` unless the release gates in this checklist pass from the exact release candidate, `pnpm release:lock` confirms the release surfaces, whitespace diff passes, compact offline proof-link and `/v` proof-token checks pass before production promotion, focused Sports, Market, Profile, World Estate, security, and release contracts pass, production migrations named by the release are applied in the target environment, and release attestation is created in the production signing environment.
