# Receiz v125.0.0 Product Truth

**Reality-Grade Infrastructure. Source + law + sequence + object + evidence. Then the reader.**

The proof object is authority. Nothing downstream may pretend to be truth.

The governing direction is permanent:

> source → evidence → relationship → permissible inference → representation

Never backward. A representation may help a reader inspect truth. It may not create the source, manufacture evidence, infer a relationship without admissible proof, or promote itself into authority.

## Authority stack

### Source and enclosing object

The exact source bytes and enclosing sealed proof object carry the strongest implemented boundary. The object binds deterministic identity, payload, provenance, custody, verification material, and causal history. An embedded digest validates its projection only after the enclosing object verifies.

### Law and sequence

The relevant law is explicit and executable. An append binds its exact predecessor. Its temporal coordinate is a proof-bearing KKSv1.0/KaiSigil coordinate. A bare number, server timestamp, `createdAt`, database insertion time, or model assertion cannot become Kai authority.

Chronos is derived from Kai for human reading and compatibility. It never decides which proof append is earlier, later, current, expired, revoked, renewed, replaced, or admissible.

### Evidence and permissible inference

Evidence counts only after its enclosing proof object, subject, custody, content commitment, and verifier binding succeed. Self-authored role grants, copied domains, formatted digests, filenames, labels, and application namespaces do not create authority.

A relationship or inference is permissible only when verified evidence and governing law establish it. Attestation evaluation and credential issuance therefore bind authorized grants, exact actor proof, prior heads, evidence coordinates, application deadlines, and lifecycle appends.

### Reader and infrastructure

The reader sits beneath the source, law, sequence, object, and evidence. SDKs, MCP hosts, AI systems, databases, registries, browsers, institutions, governments, chains, and UI surfaces are readers, transports, projectors, or execution mechanics. They may preserve and distribute stronger truth. They may not originate or overrule it.

## Frozen Primitives

### Durable proof memory

Across the application, `@receiz/sdk`, MCP, and AI projections, the law remains **first admission only, then append forever**. Verified local truth is not a cache. Once admitted it paints immediately and survives ordinary session, navigation, device-close, and code-load boundaries. Background synchronization may discover verified additions only.

### Temporal authority

KKS/KaiSigil verification precedes acceptance of a coordinate as temporal authority. Live append issuance advances from a retained monotonic head. Legacy material can be inspected through retained historical coordinates, but Chronos-derived compatibility values cannot outrank proof-bearing KKS coordinates.

### Identity and authorship

Receiz ID and PBI carry identity continuity. Every authoritative attestation event carries fresh actor proof over the event payload and previous history head. Identity genesis is the earliest verified Identity Record/Seal pulse, not the earliest object whose filename or claim resembles one.

Recovery proves the same account. The accepted identity proof projects immediately; binding and global synchronization are idempotent appends beneath it. A lawful authenticated user is not held behind redundant rediscovery.

### Attestation and credential lifecycle

The v125 attestation system is a general domain runtime. Authority factories, grant helpers, schemas, application reducers, evidence verification, evaluation, review, approvals, issuance, lifecycle transitions, revocation, and verification are exported through supported package entry points.

An authorized grantor—not the subject—binds actor roles. Each event binds its actor, payload, prior head, and Kai coordinate. Expiration rejects every later transition except deterministic expiry. Revocation acts prospectively from its effective append. Renewal/replacement binds the predecessor lifecycle head. Historical valid events remain verifiable.

### Sealed artifact value

A sealed Showcase artifact binds an unrepeatable moment, media or payload, identity, provenance, ownership continuity, and Kai pulse. Its current value advances from that immutable pulse through the established deterministic Glyph projector. Recognition at v125 activation uses the value already accrued at current Kai; it does not create a new event.

Any reader can independently reproduce that value at any admitted Kai coordinate. The verified object supplies the canonical seed; the verified coordinate supplies the valuation pulse; `receiz.glyph-settlement-projector.v125` supplies the frozen policy; integer micro-Phi supplies the exact result. The complete inputs, source paths, rounding rule, projector digest, and known-answer vector are published in [Recompute Any Sealed Showcase Value](v125.0.0-showcase-value-reproduction.md). USD is a subordinate quote projection, never the proof-native authority.

### Lawful-action value

A lawful-action law is carried by an enclosing sealed artifact and bound to the v125 registry. A completed action is separately carried by an enclosing sealed artifact and embeds the existing `receiz.portable-execution-authority.v124` transition evidence.

Verification order is fixed: enclosing artifact; portable ownership; payload extraction; law; embedded transition; actor and subject heads; causal parents; commitments; witness; Kai coordinates; one-membership admission; current-Kai projection. A caller-supplied completion verdict, amount, valuation coordinate, institutional exemption, or self-hashable namespace is rejected.

### Wallet and edge transaction truth

Settlement is transferable proof-native Phi. Reserve is the legacy funded rail. Account Value composes both without making display state authoritative.

External users can send and receive Settlement and can send and receive Reserve through the same two-sided edge-verification law.

The sender seals the exact debit successor and the receiver seals or has already published the exact receiving successor. Both edges verify the enclosing proof, prior head, rail, amount, peer coordinate, and committed successor. The server and database are subordinate global synchronization, compare-and-swap, indexing, distribution, replay, and recovery mechanics. Delayed, missing, repeated, or out-of-order synchronization cannot duplicate or redefine the movement.

### Continuous public proof surfaces

Profile, Showcase, live player, proof media, wallet, market, history, and World surfaces reuse known verified truth on first paint. A locally sealed append is never missing to the person who created it. Server acceptance extends the same object; it does not create a replacement object. Dark proof surfaces retain their exact atmosphere at wide widths.

## Frozen Invariants

- Source truth and the enclosing sealed proof object outrank every representation.
- First admission only, then append forever.
- Chronos describes; proof-bearing KKS/KaiSigil coordinates order.
- One proof object has one causal identity and one current spendable membership.
- Known verified local truth paints immediately; synchronization can only append verified additions.
- Exact micro-Phi value is recomputed from the verified sealed seed and admitted valuation coordinate.

## Release Gates

The repository release freeze, coordinated package gates, temporal-authority and Showcase-value conformance, production build, governance controls, projection integrity, schema observation, registry evidence, Git tag, Docs publication, and signed attestation are recorded as separate checks. A passing weaker check cannot replace a stronger missing one.

## Release evidence law

Repository qualification, package publication, migration, deployment, public smoke, immutable Git tag, and signed attestation remain separate facts. A passing test does not prove deployment; a registry response does not prove the proof object; a deployment does not prove an attestation. Each record names only its directly observed boundary.

## Residual Risks

- Final updated-tree release qualification has not been established by this documentation update; the September 4 aggregate result remains historical evidence.
- The scoped September 5 disaster-recovery restore/replay exercise is complete. The governance evidence register still points to March; cadence maintenance and signature verification are pending, with no governance pass claimed.
- The eight-migration SQL rollback is recorded as applied; source-authority restoration review is in progress. Provider evidence confirms c4 deployed, and the user confirms mobile sealing fixed live; other viewing paths require their own verification. See the [operational update](v125.0.0-operational-update-2026-09-05.md).
- Current public Docs route, immutable Git tag and signed release attestation status require separately recorded evidence; the September 4 observations are not fresh checks.
- Authenticated production mutation, broad device qualification, scale, and adoption are not inferred from source, schema, package, or public HTTP evidence.

## Standalone Projection

The standalone distribution projects the exact v125 verifier bytes at SHA-256 `7d9f40257d38fb9c72ed8248a9fddcf2602b9365e317f343b64c2530f95e7735` and the exact Offline Record and Seal Studio bytes at SHA-256 `2346f79a4ecdd80b25028c34910511000f6dbe28c30f7fd277eace40869afde1`. The enclosing sealed proof object remains authority; this archive, its UI, and its documentation remain readers beneath that object.

The source application repository was inspected at `ddca6196ad9f1e845d28dc6796de4d2818c7e4d3` without modification. Known verified file truth paints first. Background or network work may only append verified additions, and no standalone surface downgrades a proof object to a card, an ownership state to a badge, a Settlement primitive to a balance display, or a public proof surface to a share page.
