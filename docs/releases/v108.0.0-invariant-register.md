# Receiz v108.0.0 Invariant Register

`v108.0.0` restores the literal proof-object-first authority order. Receiz law, sealed proof objects, deterministic proof state, verified local truth, accepted identity proof, and admitted same-account continuity remain above Receiz.com reference behavior, SDK, MCP, AI skills, registry, server, database, session, and release evidence.

## Locked Invariants

1. Receiz remains a proof-native artifact system. Product truth is carried by files, identities, vault objects, ownership states, settlement states, public witness surfaces, verification routes, complete sealed artifacts, and append-only history.
2. Authority order is exact: Receiz law; sealed proof objects/deterministic proof state/verified local truth; accepted identity proof/admitted same-account binding; Receiz.com first-party reference behavior; SDK; MCP; AI skills.
3. Receiz.com is the first-party reference behavior, not proof authority. Server, database, session, key registry, constitutional registry, developer rails, documentation, and attestation remain weaker mechanics or evidence.
4. Kai Klok remains the proof-object state machine. The v108 registry seal uses real canonical `kaiPulseEternal` whole pulse `13182786` at `2026-07-18T04:38:26.799Z`; a release label is never an effective Kai value.
5. Durable proof memory remains first admission only, then append forever. Hidden work may only append verified additions after the known head.
6. Deterministic first paint projects known proof, identity, ownership, settlement, profile, market, ledger, transaction, public graph, portable state, and archive truth before weaker work.
7. Inline rename and full profile save use `PATCH /api/profile/me`, derive the actor from the admitted account, preserve the UID, coordinate uniqueness at commit, and refresh the same session/profile/public projection after success.
8. Missing, delayed, or failed `identity_keys` projection cannot block the save, select another identity, or fabricate `identity_key_mismatch`; developer endpoints, SDK, MCP, and AI never precede the first-party operation.
9. Current developer profile update accepts authenticated profile intent only and composes the same neutral first-party command; it cannot accept caller-selected identity authority, heads, receipts, or keys.
10. Native proof-object creation performs authenticated Record before Seal with owner, record, payload, claim, path, Signature V4, and carrier continuity; exact complete artifact bytes remain the custody object.
11. Payload extraction and bearer ownership occur only after enclosing-artifact verification. Ownership derives carried custody, binds the authenticated owner, appends history, and returns a newly verified complete native artifact.
12. A proof object remains portable across lawful platforms when immutable identity, payload, provenance root, prior history, unknown namespaces, and complete verification are preserved.
13. Historical v107 source, schema names, receipts, heads, and artifacts remain witnessed history only; they are not current defaults, capability inventory, or authority.
14. The never-applied v107 SQL designs remain absent from active migrations and runtime dependencies and exist only as explicitly non-deployable `.sql.txt` historical evidence.
15. Developer queues remain developer tooling and cannot remount or redefine first-party service workers, deterministic first paint, PWA runtime, or settled proof surfaces.
16. `/developers` is the maintained unversioned developer reference; `/developers/v107` redirects to it, and no `/developers/v108` product page exists.
17. Current packages, ruleset, registry, runtime labels, service worker, verifier, AI manifests, and maintained documentation align at `108.0.0` with declared registry digest `126ca9283fee4ef4c398dbcb958e861cbea191724fdab8eb08df55ff0c14bb79`.
18. Production signing, npm publication, deployment, production database mutation/verification, and authenticated production evidence require separate authorization and cannot be inferred from repository qualification.

## Protected Surfaces

- `package.json`
- `AGENTS.md`
- `README.md`
- `RELEASE_NOTES.md`
- `CHANGELOG.md`
- `docs/README.md`
- `docs/FORMAT.md`
- `docs/governance/README.md`
- `docs/releases/v108.0.0*`
- `site/index.html`
- `site/sw.js`
- `apps/offline-verifier.html`
- `apps/offline-record-seal.html`
- `apps/offline-settlement.html`
- `apps/offline-sports-card-verifier.html`

## Checks

```bash
node scripts/check_release_lock.mjs 108.0.0
pnpm release:lock 108.0.0
git diff --check
```

## Release-Lock Requirement

`pnpm release:lock 108.0.0` must require this document for `v108.0.0` and later release cuts. A release that omits the invariant register is incomplete because proof-object-first authority, same-UID profile continuity, zero-key-row tolerance, native Record-before-Seal custody, verify-before-extract ownership, real Kai-Klok sealing, historical v107 isolation, non-deployable SQL boundaries, unversioned developer guidance, and coordinated verifier identity would not be explicit release truth.
