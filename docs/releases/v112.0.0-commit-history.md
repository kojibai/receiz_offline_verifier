# Receiz v112.0.0 Commit History

Public developer surface: `@receiz/sdk` v112.0.0. Durable proof memory remains **first admission only, then append forever**.

Upstream implementation baseline: dirty-repository head accepted by the user before v112 integration, followed by merge commit `06113e505` on the upstream `main` line. Standalone verifier baseline: release commit `0004938c7dc4de5fbedd7ba079920dba44038f05` (`v111.0.0`).

Commit count: final count is recorded by the completed v112 integration commit after all release gates pass.

The v112 line was built as reviewed, bounded commits covering:

- binding architecture, compatibility matrix, and executable law objects;
- exact-byte artifact verification/admission and verified identity/history custody;
- portable transition, plan, capability, seal/stage, and independent commit boundaries;
- derived effects, namespace preservation/reducers, named-domain atomicity, and reporting-only receipts;
- asynchronous admission store and IndexedDB browser durability without proof-object storage;
- v111 containment and deterministic replay equivalence;
- five typed MCP artifact coordination tools with generic mutation denial;
- canonical unsigned registry JSON/digest/generated SDK identity;
- coordinated `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` v112 identities;
- clean-room package qualification, release documentation, and final freeze evidence.

The registry lock is deterministic and unsigned. No native `.receiz` or signed wrapper is required or claimed. The release adds no product HTTP route and no database migration. v111 exact-byte verification/readmission remains compatible while historical runtime authority remains contained.

Upstream Task 14 records the full release freeze and desktop/mobile visual evidence passing on 2026-07-19. The standalone verifier release commit is created only after its exact staged candidate passes local qualification. The signed governance attestation is generated after push by the existing GitHub Environment workflow and therefore remains outside the local commit.

Commit review tracks exact-byte artifact truth; verification/admission separation; verified actor/history; transition-plan-command separation; namespace preservation/reducers; registry chain exactness; neutral staging; independent commit verification; named-domain atomicity; receipt non-authority; v111 compatibility; no new product HTTP route; no migration; checks; visual evidence; and exact uncertainty boundaries. After local commit, only push-bound external actions and the final-SHA governance attestation remain open.

## Release-Lock Requirement

`pnpm release:lock 112.0.0` must require this commit-history boundary so upstream implementation history, standalone release identity, visual evidence, and post-push attestation are never collapsed into one unsupported claim.
