# Receiz v76.0.0 Checklist

Release date: May 6, 2026
Status: Release prep

## Release Scope

- Current documented release anchor in this repository before this cut: `v75.0.0`
- Broader product baseline referenced by this release: tag `v75.0.0` at commit `bae14f0cd`, as supplied by the broader release brief.
- Broader release candidate referenced by this release: commit `876f70c7b` before the `v76.0.0` release bump, plus governed v76 version and release-doc updates.
- Broader release scope before the release bump: 46 commits, 177 changed files, 28,359 insertions, and 1,002 deletions, as supplied by the broader release brief.
- This checklist governs the `v76.0.0` value-production release cut and the outward version-surface alignment for this offline verifier repository.

## Required Release Artifacts

- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v70.0.0-invariant-register.md`
- `docs/releases/v75.0.0.md`
- `docs/releases/v75.0.0-product-truth.md`
- `docs/releases/v75.0.0-checklist.md`
- `docs/releases/v75.0.0-process.md`
- `docs/releases/v76.0.0.md`
- `docs/releases/v76.0.0-product-truth.md`
- `docs/releases/v76.0.0-checklist.md`
- `docs/releases/v76.0.0-process.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`

## Database Requirements

- no new database migration is required by this offline verifier repository documentation cut
- existing production schema requirements from prior world, market, proof, wallet, and status releases still apply in the broader platform workspace

## Canonical Product Gates

- World Concierge keeps direct response continuity while agentic behavior runs via the Agents SDK.
- World toolbelt actions remain available as business artifacts (lead research, contact enrichment, CRM sync, draft/send follow-up, campaign workflows, classification, payment/booking path generation, proof ledger, handoff, Sales Brain routing).
- Public trace is summary-only at user-facing level.
- Campaign follow-up defaults to email unless a valid connected webhook destination is configured.
- Legacy invalid webhook campaigns can heal back to email.
- World missions expose visible outcome sequence and next commercial action.
- Revenue attribution records and autopilot brief outputs remain tied to mission action truth.
- Market Twin persists mandate state, authority settings, order caps, daily caps, profit sweep, and append-only intent rows.
- Market Twin repair migration preserves missing authority columns created by older create-table guards.
- Market replay uses deterministic snapshot history and computes win-rate over actual replayed outcomes.
- Public Proof exposes observe/create/resolve/claim/verify/seal/dispute/regulatory routes and registry discovery safely.
- Public Proof UI remains mobile-first and avoids raw internal trace exposure.
- Wallet settlement handles non-subscription reserve/payment credit correctly and keeps running reserve balance behavior stable.
- Public/developer Status only includes safe routes and excludes admin, webhook, internal-worker, private-session-only, and privileged-issuer surfaces.
- Auth/profile continuity respects local/strong proof over stale transport echoes.
- Release-surface lockstep includes package metadata, readmes, changelog/release notes, and shipped HTML version markers.

## Focused Platform Verification

Release-prep commands expected in the broader platform workspace:

- `pnpm lint`
- `pnpm build`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm test:deterministic-surfaces`
- `pnpm test:release-freeze`
- `pnpm release:lock`

Focused suites expected in the broader platform workspace:

- Concierge continuity and handoff-path continuity suites
- World mission, toolbelt, Sales Brain, revenue attribution, and autopilot brief suites
- campaign follow-up default and repair suites
- Market Twin authority persistence, mandate checks, migration repair, replay integrity, and win-rate suites
- Public Proof observe/claim/verify/seal/dispute and registry suites
- Wallet reserve settlement / payment credit suites
- Status inventory route safety suites
- Public profile/auth continuity suites
- v75->v76 release-contract coverage suites

## Offline Verifier Repository Gates

- root package metadata resolves to `v76.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v76.0.0`
- release docs and indexes point to the `v76.0.0` release artifacts
- product-truth reference docs are linked from the public docs map
- broader platform validation for `v76.0.0` is captured in the canonical release note, product-truth document, process document, and checklist
- verifier trust semantics remain unchanged relative to `v60.0.0`
- no verifier proof-format or producer payload migration is implied by these repository release/documentation updates alone
- `pnpm lint` has zero warnings/errors
- `pnpm build` completes
- governed surfaces all show `v76.0.0`

## Release Rule

Do not ship `v76.0.0` unless the release gates in this checklist pass from the exact release candidate, `pnpm lint` has zero warnings/errors, `pnpm build` completes, and the governed surfaces all show `v76.0.0`.
