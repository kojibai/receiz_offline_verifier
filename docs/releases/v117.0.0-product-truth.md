# Receiz v117.0.0 Product Truth

## Release primitive

v117 is an additive release of the Receiz proof-native artifact system. Its registry inherits every v116 law unchanged. The new digest identifies the coordinated release; it does not manufacture proof authority or replace sealed artifact truth.

## Frozen Primitives

Sealed proof objects, Receiz ID/PBI identity continuity, ownership and custody, proof-native settlement, carried provenance, native capture, authorship appends, durable proof memory, and public witness surfaces remain direct implemented primitives.

## Frozen Invariants

Known verified truth paints immediately. Durable proof memory is first admission only, then append forever. Weaker DB, session, network, SEO, metadata, and UI state may append verified additions but cannot replace stronger truth.

## Truth hierarchy preserved

1. Sealed artifact and embedded proof truth remain authoritative.
2. Deterministic proof object and verified durable local truth paint immediately.
3. Verified register, server append, database, session, cache, metadata, and SEO remain weaker layers.
4. Weaker payloads may append verified additions but cannot replace, erase, or delay stronger known truth.

## Surface contracts

- Contacts is a composite proof object, not cache or generic contact state.
- Profile, Market, Wallet, Sports, `/t/`, and `/v/` are proof, ownership, settlement, or public proof surfaces according to the primitives they carry.
- Sports prepared native save uses the existing inspected sealed artifact; source extraction changes no ceremony, timing, digest, message, or outcome.
- `/t/` is the transaction public proof presentation. `/v/` is an adaptable public witness moment carrying “This happened” branding.
- A transaction Moment already carries its seal. Collect downloads it directly and never creates a second authority layer.
- SEO and share-image work are metadata mechanics only and never enter deterministic first paint.
- The isolated profile DB conformance URL exists only for destructive integration fixtures; it is not product truth and its absence does not prevent later gates from running.

## Release identity

- Version and ruleset: `117.0.0`
- Registry predecessor: v116 digest `9bf61fcf4541edf565bb2ded252e35a976a3ca7c9176dea0f1ffac74ce192a80`
- Registry digest: `598ee0fa4dc31b8394fdd2b7b8fe713c8ee8c4b33e6ecdd92101a1a93d8787a8`
- Operation matrix digest: `6c71f54e2fd709f69ce35e8cf3112d9066b5b108b9d610768d9b16ad38d5cab5`
- Current compatible package range: `>=117.0.0 <118.0.0`

## Release Gates

The v117 release lock, coordinated package gate, full release freeze, governance controls, build, browser evidence, and attestation are required evidence.

## Residual Risks

The destructive isolated profile-database fixture remains unavailable without its local test URL and service key. That unavailable infrastructure does not change product truth and does not prevent the remaining gates from running. Production DB state is not verified by the local release lock.

No v118 orchestration proposal is part of this product truth.

The `@receiz/sdk` v117 contract keeps durable proof memory **first admission only, then append forever**.
