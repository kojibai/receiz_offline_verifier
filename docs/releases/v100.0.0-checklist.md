# Receiz v100.0.0 Release Lock Checklist

Release documents:

- `docs/releases/v100.0.0-product-truth.md`
- `docs/releases/v100.0.0-process.md`
- `docs/releases/v100.0.0-regression-lessons.md`
- `docs/releases/v100.0.0-performance-findings.md`
- `docs/releases/v100.0.0-invariant-register.md`
- `docs/releases/v100.0.0-commit-history.md`

This lock keeps `@receiz/sdk` beneath proof authority and durable proof memory as **first admission only, then append forever**.

## Candidate Identity

- [x] Baseline is annotated tag `v99.0.0` at `17a4c5d13056ae3752cc1b08c2727e09d14db838`.
- [x] Reviewed pre-lock range is `v99.0.0..e4780d5e7d6f48a7f180f1aca3ab1b10ebe6c125`.
- [x] All 42 pre-lock commits are recorded mechanically.
- [x] Theme is Proof Becomes Alive / The Living Proof Release.
- [x] Strict SemVer governs compatible patch/minor and breaking major releases after 100.
- [x] Runtime cache epochs and numeric payload schema versions remain independent.

The baseline above names the reviewed Receiz app candidate. This standalone offline-verifier repository starts from its own annotated `v99.0.0` tag at `21cf59d1b74993d2f0a70d22e266df0692ce4e0b`; that repository boundary is mechanics beneath the app proof range, not a competing source of product truth.

## Offline Verifier Repository Gates

- [x] `node scripts/check_release_lock.mjs 100.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 100.0.0`
- [x] `git diff --check`

## Local Gates

- [x] `node scripts/check_release_lock.mjs 100.0.0`
- [x] `RELEASE_EXPECTED_VERSION=100.0.0 pnpm test:sdk-release-contracts`
- [x] `pnpm test:visual-evidence-contract`
- [x] `pnpm test:primitive-binding-contract`
- [x] `pnpm test:repo-only-reasoning-boundary`
- [x] Twin character, atlas, backfill, and World contracts.
- [x] Profile and Market continuity contracts.
- [x] Sports Arena slate and unresolved-reward contracts.
- [x] `pnpm test:page-first-paint`
- [x] `pnpm test:deterministic-surfaces`
- [ ] `pnpm test:release-freeze`
- [ ] `git diff --check`

## Visual Evidence

- [x] Canonical public profile first viewport preserves proof-bound identity, settled shell, and Live Twin presence (`output/playwright/v100-profile-receiz-viewport.png`).
- [ ] Owner profile preserves the canonical public base and appends owner controls/backfill behind first paint.
- [x] World surface preserves the public Twin stage and proof-bound public identity (`output/playwright/v100-world.png`).
- [x] Market paints the settled deterministic public proof object (`output/playwright/v100-market.png`).
- [x] Sports Arena paints the verified slate and proof-object lineup (`output/playwright/v100-sports-arena.png`).
- [x] Browser console evidence has no application/runtime/resource failure on the fresh v100 server; WebKit only reports that it ignores the `interactive-widget` viewport metadata key.

The first port-3001 browser attempt hit an older server and returned stale-chunk HTTP 500 errors. That run and `output/playwright/v100-profile-receiz.png` are rejected as release evidence. The accepted evidence above comes from the exact `100.0.0` build served on clean port 3100. Authenticated owner-mode/backfill and an unresolved-reward interaction were not exercised because no authenticated owner/reward fixture was available; their automated contracts passed.

## Governance And Attestation

- [ ] `pnpm governance:check`
- [ ] `pnpm governance:evidence-check`
- [ ] `pnpm governance:verify-artifact-signature`
- [ ] `pnpm release:attest`
- [ ] `pnpm release:verify-attestation <path>`
- [ ] Attestation is produced from the same candidate that passed the final release freeze.

## Manual Promotion Actions

- [ ] Create annotated tag `v100.0.0` from the verified release-lock commit.
- [ ] Push the release commit and tag.
- [ ] Publish `@receiz/sdk@100.0.0`.
- [ ] Publish `@receiz/mcp-server@100.0.0`.
- [ ] Deploy Receiz `v100.0.0`.
- [ ] Run production smoke evidence on affected surfaces.
- [ ] Verify published package and deployed release identity.

## No-Downgrade Audit

- [x] No sealed Twin identity is replaced by atlas, route, DB, session, SDK, or MCP state.
- [x] No deterministic proof media is renamed as optional decoration.
- [x] No existing Twin is replaced to obtain the new atlas; continuity is an append.
- [x] No SDK route or escrow response becomes proof, ownership, custody, or settlement authority.
- [x] No durable Profile, Market, Sports, identity, or public proof truth is treated as disposable cache.
- [x] No deterministic first paint is replaced by loading theater or foreground backfill.

Unchecked evidence or promotion actions are incomplete and must not be described as complete.
