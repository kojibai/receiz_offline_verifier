# Receiz v104.0.0 Release Lock Checklist

Related evidence:

- `docs/releases/v104.0.0-product-truth.md`
- `docs/releases/v104.0.0-process.md`
- `docs/releases/v104.0.0-regression-lessons.md`
- `docs/releases/v104.0.0-performance-findings.md`
- `docs/releases/v104.0.0-invariant-register.md`
- `docs/releases/v104.0.0-commit-history.md`

The app baseline is `dfac9863b` as recorded by the supplied release history. This standalone offline-verifier repository starts from its own annotated `v103.0.0` tag at `f6f03e6c12cd67a1b5481c658ced083a1e34bedd`; that repository boundary is mechanics beneath app compiler behavior and sealed proof truth.

## Offline Verifier Repository Gates

- [x] `node scripts/check_release_lock.mjs 104.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 104.0.0`
- [x] `git diff --check`

- [x] `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` source and packed manifests align at 104.0.0.
- [x] MCP dependencies are exact 104.0.0 and no `workspace:` dependency remains.
- [x] Empty external-project tarball installation passes.
- [x] SDK, MCP, CLI, and AI-skill focused tests pass.
- [x] Uncached lint passes with zero warnings and zero errors.
- [ ] Release lock, governance, application build, and complete release freeze pass.
- [ ] Publish AI skills, SDK, then MCP through pnpm workspace-aware commands.
- [ ] Verify registry manifests and external registry installation.

Durable proof memory remains **first admission only, then append forever**.
