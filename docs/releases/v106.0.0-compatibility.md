# Receiz v106.0.0 Compatibility

The coordinated public package set is `@receiz/sdk@106.0.0`, `@receiz/mcp-server@106.0.0`, and `@receiz/ai-skills@106.0.0`. MCP dependencies are exact, not ranged. The independent verifier is pinned separately at `106.0.0` and is not public proof authority.

v105 applications migrate through `receiz migrate v105-v106 --dry-run`, explicit plan-digest confirmation, `--apply`, and `--verify`. Migration is forward-only, resumable, idempotent, ambiguity-blocking, and cannot rewrite verified history. The compatibility law remains **first admission only, then append forever**.
