# Receiz v124.0.1 Migration — Reality Becomes Infrastructure

**The Production Runtime for Verified Civilization.** No new database schema migration, proof-object rewrite, identity reissue, or artifact backfill is required for the `124.0.1` corrective package release. The three V124 durable-runtime migrations remain the production prerequisite established by `v124.0.0`; this patch does not replace, repeat, or elevate that subordinate database infrastructure.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk` and all migration mechanics remain below the admitted sealed history.

## Adoption actions

1. Upgrade `@receiz/ai-skills`, `@receiz/sdk`, and `@receiz/mcp-server` together to `124.0.1` after each exact package publication is independently confirmed.
2. Keep application compatibility at `>=124.0.0 <125.0.0` and keep `RECEIZ_RULESET_VERSION` bound to `124.0.0`.
3. Issue a new delegated access token only when an application needs newly selectable lawful scopes; token issuance grants capability and does not create identity or proof authority.
4. Refresh the Receiz Docs frozen lock from the exact published package bytes after publication.
5. Preserve the already-applied V124 durable-runtime schema and verify its existing production evidence; do not rerun it as a `124.0.1` migration.

Sealed proof objects, Receiz identity artifacts, and verified local continuity remain authority. Database rows, package objects, delegated tokens, MCP projections, and Academy records remain subordinate synchronization, execution, recovery, or presentation state.

The standalone repository contains no database runtime and performs no migration. Advancing its visible release identity and cache epoch does not create database or deployment evidence.
