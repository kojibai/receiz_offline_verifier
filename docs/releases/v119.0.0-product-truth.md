# Receiz v119.0.0 Product Truth

Developer surface: `@receiz/sdk@119.0.0`. Durable proof-memory law: first admission only, then append forever.

V119 is additive from the immutable v118 constitution registry digest
`c284bd39a891c1a828b532523bd548507570819c32e307d79b8043f06d2d3360`.
It does not reinterpret, replace, or mutate v118 artifacts.

## Frozen Primitives

V119 freezes the enclosing Receiz proof object, the verified
`receiz.account.state.v3` Reserve source, qualified irreversible custody, the
whole-value Offline Note successor, canonical verification, Kai causal heads,
and durable local admission as authority-bearing primitives.

## Authority

- The enclosing verified Receiz artifact is the strongest truth boundary.
- A projection digest proves deterministic projection; it does not authorize
  admission by itself.
- Verified durable local truth paints before network, database, session, SEO,
  or discovery work.
- The highest valid Kai causal coordinate is the active head.
- Merkle state commits exact admitted appends.
- Fibonacci checkpoints provide sparse ancestry and recovery. They are not a
  substitute for Kai head selection or Merkle membership.
- A global or domain composite admits verified child proof-object heads. It
  never becomes stronger authority than the admitted child artifacts.

## Recursive continuity

Every admitted proof object has stable identity, an immutable version digest,
an exact parent, a Kai head, a Merkle-committed append history, sparse
Fibonacci ancestry, and an external admission binding. A successor appends;
it does not rewrite its predecessor.

The global economy carrier is one compact recursive proof object containing
verified references to domain proof heads. A domain remains independently
verifiable. Projection windows and chunk indexes make any admitted event
available at the edge without replaying the whole history.

## Offline Notes close the transferable-custody loop

V119 binds transferable Offline Notes to qualified irreversible custody. The
Note remains a sealed whole-value proof object; the custody adapter is a narrow
subordinate boundary that answers whether this exact verified head can
authorize one successor transition.

Genesis begins from an implemented stronger primitive, not a server balance:
the verified `receiz.account.state.v3` portable state carried by the Receiz
Key/Identity proof. Before funding is accepted for Offline Note issuance, the
external funded event is sealed into `receiz.offline-reserve.funding-binding.v1`
against that exact account head and a preselected qualified Reserve-custody
commitment. Unbound or legacy Reserve cannot enter the Offline Note path. Local
issuance composes one Reserve-debit account-state
successor and one equal whole-value qualified held-bound Note genesis,
canonical-verifies both successors, then activates that exact Note genesis in
qualified custody. Supabase, server, database, session, and publication may
project those verified successors later. They never gate or release the Note,
authorize issuance, establish Settlement, or replace the carried Reserve proof.

The Cash Send path is exact:

```text
canonical-verify exact held head
-> prove current qualified custody controls that head
-> verify one anonymous qualified next-custody commitment
-> commit the complete whole-value successor basis
-> irreversibly consume the exact predecessor once
-> seal and canonical-verify one successor
-> receiver verifies and activates that exact successor locally
-> Settlement is complete
```

Identity, username, Receiz ID, PBI, account, session, server, database, and
publication are not required for Cash Send authority. An optional identity
lock may restrict one selected handoff, but it remains subordinate: it cannot
create value, qualify custody, consume a predecessor, choose the lawful head,
permit another successor, or persist into later handoffs unless selected
again.

The predecessor remains authentic history after Send. Its qualified custody
position is permanently consumed, so copied, renamed, wrapped, restored, or
reverified predecessor bytes cannot regain authorized Send. The successor
preserves the whole value, exact predecessor digest, Kai order, one-use
consumption receipt, custody handoff, and complete verified history.

The receiver's exact local activation is Settlement. A server append, public
status projection, database row, or later global sync may distribute that
already-settled history. None of those weaker layers creates ownership,
selects a branch, or makes the transfer valid, and the parties never have to
publish.

## Binary device qualification

Every installation is automatically invited to prove the same custody law.
No device-model allowlist or manual profile registration gates the attested
path. A device qualifies only when its platform authenticator supplies
vendor/platform attestation, single-device non-backup state, user verification,
an advancing strict counter, and the executable rollback-conformance sequence;
Receiz binds that evidence to the exact origin, RP ID, custody key, and current
state head. Qualification therefore proves once-only exact-head consumption,
receiver and whole-value binding, rollback rejection, crash-safe retirement,
and signed portable evidence. Generic WebAuthn support, platform branding,
browser name, device age, account state, or a profile label does not qualify a
device.

Unqualified installations remain useful. They can inspect and canonical-verify
Notes, preserve the proof object and history, publish already-verified
evidence, and use separately governed connected Send. They cannot create a
transferable Offline Note, create a receiving-custody commitment, activate
transferable offline custody, or expose Offline Send.

Receiz Certification names the same binary law for purpose-built or
manufacturer-integrated hardware. Certification may cover integration,
testing, attestation compatibility, firmware review, and maintained compatibility;
commercial participation never purchases a passing result. A qualified
consumer installation and a Receiz Certified device produce the same canonical
Note transition verdict.

## Developer and agent rails

The V119 SDK exports the portable qualification, anonymous receiving-custody,
WebAuthn consumption/activation, and custody-conformance schemas and strict
verifiers. Those functions verify evidence; they cannot infer platform
qualification from caller-shaped data.

The CLI has no Offline Note qualification, activation, consumption, or Send
command. HTTP may verify or publish beneath the settled Note. MCP may invoke
the canonical artifact verifier but has no separate Offline Send authority. AI
skills may guide the exact verification sequence but model text cannot create
custody, ownership, Settlement, or a valid successor.

## Durable edge law

- Closing the PWA does not clear proof objects, append history, active heads,
  immutable segments, or admission evidence.
- Installing new application code does not clear those objects.
- Service-worker activation may rotate HTTP/code caches only.
- Active projection records may advance to a verified successor; immutable
  predecessor artifacts remain stored.
- Known counts, card rates, live scores, current values, profile heads, and
  settled history heads render from the admitted local head on first paint.
- Later discovery may append a higher-Kai successor. Equal/lower-Kai discovery
  cannot downgrade the held head.

## Sports card proof state

The V119 sports card state head is a projection inside the sports proof object.
It carries current rate, projected score, live score, current Phi/USD value,
history event count, append Merkle root, append proof digest, parent state
digest, predecessor artifact digest, Kai head, and the append proof's exact
sparse Fibonacci ancestry.

The enclosing card proof object remains authority. The state head cannot admit
itself. A changed digest-checked registry projection is discovery; it becomes
durable current truth only through the verified card/vault successor path.
Unchanged discovery creates no append and performs no work.

## Release-blocking failures

- Default/base card rate or score paints before a higher held state head.
- A card shows a partial history count and later hydrates to a count already
  admitted on-device.
- Arrival order replaces highest-Kai selection.
- PWA/code update deletes an immutable user proof object.
- Active-store replacement is the only copy of an artifact.
- A dense generic list is labeled Fibonacci ancestry.
- A count is used as proof lineage or readiness authority.
- A global projection is treated as child proof authority.
- An unqualified installation creates, receives, activates, or Offline Sends a
  transferable Note.
- A profile qualifies from generic WebAuthn support, device branding, account
  state, or caller-provided metadata instead of exact conformance evidence.
- Cash Send requires username, Receiz ID, PBI, account, session, server,
  database, or publication authority.
- Genesis waits for Supabase, server, database, session, or network state to
  deduct Reserve or release the Note.
- A Note genesis exists without an equal canonically verified Reserve-debit
  successor of the exact verified `receiz.account.state.v3` head.
- A successor is released before exact-head consumption succeeds and the final
  sealed successor canonical-verifies.
- A consumed predecessor regains Send after copy, restore, reinstall, upload,
  crash, missing state, or verification.
- A transfer changes, divides, recreates, or partially carries the Note value.
- Receiver activation accepts a successor not bound to its exact qualified
  receiving-custody commitment.
- Server arrival order, public status, or database state completes Settlement
  or selects the lawful owner.
- SDK, CLI, HTTP, MCP, or AI output is described as Offline Note authority.

## Frozen Invariants

The authority, continuity, Offline Note, qualification, developer-rail, and
durable-edge laws above are frozen. A successor may append verified truth; no
server, database, session, projection, tool, or arrival order may replace the
stronger carried proof.

## Release Gates

The coordinated v119 identity lock, package contracts, Offline Note custody
contracts, canonical verifier and conformance suites, Academy archive checks,
governance controls, build, visual-evidence contract, and full release freeze
must pass for the same candidate tree before attestation.

## Residual Risks

No production custody profile is inferred from a browser label or generic
WebAuthn support. An installation remains unable to create or Offline Send a
transferable Note until its exact adapter profile passes physical rollback,
clone, backup, restore, crash, and once-only conformance and is admitted to the
production registry. Package publication, deployment, production smoke, and
device qualification remain separate evidence.
