# Receiz v124.0.2 — Reality Becomes Infrastructure

Release date: August 23, 2026

**The Production Runtime for Verified Civilization.** `v124.0.2` is the coordinated portable-proof-media and exact-Kai patch for the V124 runtime. A sealed proof object can carry its exact native material through a content-bearing URL, be verified and reconstructed locally, and be displayed on Receiz or an independent developer domain without requiring a database row, media upload, or server response to make the object true.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk` verifies and projects beneath that admitted proof history.

## Release outcomes

- The SDK verifies both fully inline `rma2` material and compact `rmc1` proof-object append segmentation, reconstructs exact image, audio, video, PDF, text, or binary bytes locally, and returns the canonical Receiz `/v` coordinate.
- MCP exposes the same material-opening boundary and exact sealed-pulse projection without introducing model, server, or database authority.
- AI Skills publish the matching SDK and MCP operations so agents can implement portable proof-object playback end to end.
- Identity glyph projection derives from the account's exact sealed creation pulse. Device clock, PBI time, enrollment time, process time, and session time remain separate evidence and cannot redefine the glyph's creation coordinate.
- Sports pack issuance removes redundant blocking work while preserving the existing reveal stage, proof issuance, ownership, custody, and card UX.

## Compatibility and authority

The pre-existing V124 registry digest and `RECEIZ_RULESET_VERSION = "124.0.0"` remain unchanged. Compatibility remains `>=124.0.0 <125.0.0`. The `124.0.2` SDK, MCP, and AI packages are additive, subordinate package representations beneath sealed proof objects and exact authenticated heads.

No database migration is introduced. No historical artifact bytes are rewritten. The full head-relative monotonic continuation runtime remains scoped to V125 Temporal Continuity and is not claimed by this patch.

## Authority documents

Release law is frozen in [product truth](v124.0.2-product-truth.md), regression prevention is frozen in [regression lessons](v124.0.2-regression-lessons.md), and repository provenance is bounded in [commit history](v124.0.2-commit-history.md).

Canonical paths: `docs/releases/v124.0.2-product-truth.md`, `docs/releases/v124.0.2-regression-lessons.md`, and `docs/releases/v124.0.2-commit-history.md`.

## Standalone Repository Boundary

The standalone repository carries the canonical offline verifier and Offline Studio bytes observed in the application candidate, plus its public labels, service-worker identity, release law, and mirrored release evidence. It does not vendor the SDK, MCP server, AI Skills, application routes, database, or package-publication mechanics.

The application repository was read without modification. The observed local candidate is `7e7c4297b5ecf95236ace3eb87bb0b97cbc9cd8f`; the observed pushed `origin/main` boundary is `eed426781b6dbb4138e29c9cbcbcf896ec890b11`. The standalone predecessor is `86bd34c7ebb9c4b1e0db5d5020b9443c085fc275` at annotated tag `v124.0.1`.

Package and visible release identity advance to `124.0.2`. Ruleset `124.0.0`, registry digest `d02429151b0bcebdaeb89485792e377afc55130f9a25e07982c1c88221314247`, application-operation matrix digest `540d1c1bf39f1b288b257c79a6e020bdcc5e587fc9b7dbf6b7aaa5d082e20ad5`, and compatibility `>=124.0.0 <125.0.0` remain frozen.

No application-repository write, coordinated package publication, production deployment, database mutation, production smoke, remote push, or signed attestation is performed or inferred by this standalone release.
