# Receiz v97.2.0 Commit History

Release date: 2026-06-30

Baseline: `v97.0.0`

Commit count: `release brief evidence plus the v97.2.0 release-lock edits in this checkout`

This `v97.2.0` release-lock state captures the enterprise app runtime SDK work after the public app-state projection rail:

- Added tenant-scoped customer session helpers and customer portal helpers to the SDK release surface.
- Added merchant onboarding helpers for Commerce Cloud grade app builders.
- Expanded commerce primitives for refunds, subscriptions, shipping, tax, discounts, gift cards, access passes, inventory, fulfillment, and payouts.
- Added enterprise rails for RBAC, audit logs, risk checks, compliance exports, store portability, notifications, search, release pinning, media transforms, jobs, event replay, and offline proof queues.
- Required idempotency material on new write helpers so retries and offline replay remain bounded.
- Added `@receiz/sdk/react` hooks and hosted component factories for identity, checkout, app-state, media, domain connection, wallet balance display, and Twin writer surfaces.
- Documented App Runtime integration paths for app-state, checkout, media, events, jobs, RBAC, audit, search, compliance, offline proof replay, and release pinning.
- Locked the offline verifier checkout's release docs, version markers, service-worker release truth, and local release-lock surfaces to `v97.2.0`.

The release lesson from `v97.0.0` remains intact: public developer contracts must never understate internal proof truth. `v97.2.0` extends that lesson into enterprise app runtime composition. Developers receive typed convenience. Receiz truth remains proof-native, append-only, locally durable, and governed by Kai/Klok state. The durable proof memory law remains first admission only, then append forever.

The user-facing release brief compares SDK capability since `v97.1.0`; this checkout's release-lock baseline is `v97.0.0` because that is the previous locked state present in this repository.

## Release-Lock Requirement

`pnpm release:lock 97.2.0` must require this document for `v97.2.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the v97.0 baseline, enterprise app runtime SDK rails, React subpath, idempotent write replay, offline proof queue behavior, package evidence, and repo-local release-lock edits would not be explicit release truth.
