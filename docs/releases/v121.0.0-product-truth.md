# Receiz v121.0.0 Product Truth

Registry digest: `29a793a5bcc0195ab41d30614d37ac51df66023af354fa4335460764eb0af413`.
Immutable v120 predecessor: `0728651789b26e1d10c1991ec1c06c1ea4a576f0c6520537b250b171f8857073`.
Application-operation matrix digest: `208553829ba78a5536524b864577ce59989e2d0a994fad9598d39ae3d557c4f5`.

## Frozen Primitives

Proof objects, identity continuity, public proof surfaces, deterministic proof media, durable local truth, Settlement, and Reserve remain implemented Receiz primitives.

## Frozen Invariants

1. Representations never outrank their source.
2. A verified sealed proof object is usable immediately; server or database confirmation cannot gate it.
3. Public proof publication must become globally discoverable through a verified composite append while the proof object remains authority.
4. A settled profile cannot reload, remount, duplicate, or lose verified fields during background publication.
5. Known verified identity, Settlement, Reserve, media SHA-256, PBI, and showcase head paint before discovery.
6. Weaker partial payloads may append verified additions but cannot downgrade stronger known truth.
7. Once lawful hydration is verified and stored, it is the next cold-load head rather than work repeated on every visit.
8. Stable Receiz ID is identity; username is mutable presentation bound to that identity.
9. An accepted signed-in identity can create its new proof-object policy immediately; a missing ownership projection cannot veto creation, and an existing conflicting owner remains binding.

## Result

The same architecture now produces both lawful authority and the fast experience: local proof projection is immediate, global publication is definite and durable, and databases and servers are maximally useful without becoming the proof boundary.

`@receiz/sdk` preserves the same durable rule: **first admission only, then append forever**.

## Release Gates

The v121 release lock, deterministic-surface contracts, primitive-binding contracts, compatibility suites, coordinated package tests, production build, and full release freeze must pass on the final tree.

## Residual Risks

Package publication, production deployment, production smoke, and signed attestation are separate evidence boundaries and cannot be claimed until their credentials and commands complete successfully.
