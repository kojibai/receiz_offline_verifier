# Receiz v127.0.0 Product Truth

**Reality-Grade Infrastructure — Proof Creation Runs Locally. Intelligence Runs Locally.**

## Frozen Primitives

Sealed artifact truth, canonical Signature V4, offline verification, Receiz ID/PBI identity continuity, ownership, Settlement and append-only proof history remain implemented primitives. The proof object carries source and continuity; applications, indexes, SDK/MCP results and generated answers project or act beneath that source.

Developer sealing composes the existing canonical file sealer and verifier. Receiz Mind composes local observation, sealed Memory Crystals, exact source inspection, identity admission and local-model intelligence. MCP and AI instructions invoke the implementation; neither creates proof authority.

## Local execution and signer custody

The SDK exposes `@receiz/sdk/offline` and `@receiz/sdk/offline/node`. Explicit one-time online enrollment certifies a device-generated signing key. Once custody and canonical proof resources are installed, sealing and verification run locally, including after restart. A seal call never silently enrolls the device. Missing enrollment is an explicit failure.

Public verification roots, proof WASM and proving resources may ship. Root signing keys and server credentials do not. Node device keys remain in private local custody; browser keys remain in origin-scoped IndexedDB custody. Enrollment on one browser origin does not enroll another origin. The existing HTTP `assets.createProofObject()` operation is distinct from the explicit offline entry point.

The certificate authorizes canonical artifact signing only. It does not create a Receiz ID, establish another person's ownership, admit a transfer or execute Settlement. Existing identity, ownership and Settlement primitives remain their respective authority boundaries. The enclosing proof must verify; a self-hashed projection cannot substitute for it.

## Local temporal execution and portable history

The local V120 subject host reuses admitted Identity Record, enclosing artifact verification and the existing subject runtime. Full KaiSigil coordinates bind each new transition to its causal predecessor with strictly advancing micro-pulses. Bare pulse values, synthetic increments and sealed historical timestamp fields cannot establish current temporal authority. The ordinary file sealer carries its canonical document proof; a subject temporal envelope separately carries the full KaiSigil chain. These proof domains are not interchangeable.

Durable state retains exact source artifacts. Portable export carries those sources, the exact V120 snapshot and temporal evidence; import verifies the enclosing artifact, original sources and all bindings. Historical V120 history remains exact and is not retroactively proved. Multi-owner historical bearer hosting requires its existing shared custody domain; copying snapshots does not establish an atomic cross-owner claim domain.

Current HTTP implementations and historical compatibility methods are explicitly classified in the SDK, MCP and AI distribution. Historical Connect methods reject before HTTP unless an implementing custom host is explicitly configured. The local subject host and offline sealer need no historical HTTP host. Checkout-session creation does not complete payment, and proposed wallet funding cannot claim Settlement without its execution primitive.

## Receiz Mind and verified memory

Mind retains independent version 1.1.0/build 2026092205 and its exact qualified macOS artifact. V127 includes the full source history since v126 without relabeling that installer. The [Mind qualification ledger](../../packages/standalone/receiz-business/docs/releases/1.1.0-verification.md) records its separate deployment, notarization, installation, preservation and complete public-byte evidence.

Recording and model inference run locally. Explicit per-identity sync transports selected artifacts and connected question/answer objects privately; web access to the Mac's model requires a connected, running Mac. Sync, source indexes and model context cannot outrank the enclosing artifacts. Retrieval reads bounded exact evidence and does not claim to consume every byte of an account in one model context.

Recent conversational context remains scoped to the connected identity. Completed AI-response sealing into Memory Crystal is deferred to Mind's v2 plan. Signed subscription terms use their existing signer, owner binding and Kai interval; billing notifications trigger verification and do not confer entitlement. New billing events require connectivity; held proof and term verification remain local.

## Frozen Invariants

- No root key distribution, server signing after enrollment, custom verifier root or detached payload authority.
- No account-entry changes, first-paint rediscovery, history rewriting or replacement of complete held proof with partial transport.
- Known identity, Showcase, memory and public-history heads remain available before background discovery. Verified additions append behind settled surfaces.
- Export, save and restore preserve exact carried source and proof bytes. Transport failure is not evidence that held proof is absent or invalid.
- Mind keeps its independent artifact identity and qualification record. A standalone release does not certify the coordinated main release.

## Release Gates

Run scoped offline qualification, SDK/MCP tests, installed-package examples, coordinated package release checks, release-freeze, account-entry freeze, governance controls and attestation verification. Record the exact tested source state and artifacts. A version label, tool listing or historical pass is not current release evidence.

## Residual Risks

V127 remains under qualification until its [checklist](v127.0.0-checklist.md) records completed evidence. All three npm publications and the production registry successor are verified in the linked evidence. Final attestation and deployed visual qualification remain separate checks. Published MCP declarations require the exact TypeScript alias workaround documented in the migration guide. Synthetic HEIC qualification does not establish physical Camera Roll preservation. Browser origins retain independent key custody. Cloud AI inference requires its own connectivity; local tools and local AI can seal without cloud inference. Mind's live paid transaction delivery remains unexercised in its existing qualification ledger.

The `@receiz/sdk` v127.0.0 continuity contract remains **first admission only, then append forever**: reuse admitted proof immediately, and accept only verified additions behind the held head.

## Standalone Projection

The standalone public runtime uses the exact committed v127 verifier
(`f353850dfc535943878bd775f7e2bafcd39204657b48a7975de9f100d2601261`)
and Offline Studio
(`2f39214a9ba7ef5b8127ad26bab08e9eb3747e69401b724bbc3a3efbe5965dad`)
from the read-only main release boundary. Local creation and verification
remain beneath the enclosing sealed proof object. A release label, browser
shell, service worker, SDK result, MCP response, AI answer, server row, package
receipt, or archive record cannot replace carried artifact truth.
