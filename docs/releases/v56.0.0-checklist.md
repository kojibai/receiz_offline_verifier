# Receiz v56.0.0 Checklist

Release date: April 17, 2026
Status: Release prep

## Release Scope

- Current documented release anchor: `v55.1.0`
- This checklist governs the canonical `v56.0.0` release cut on top of the `v55.1.0` continuity baseline

## Required Release Artifacts

- `docs/releases/v56.0.0.md`
- `docs/releases/v56.0.0-product-truth.md`
- `docs/releases/v56.0.0-checklist.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`

## Database Requirements

- `supabase/migrations/20260417094500_profile_analytics_activation_event_support.sql`

## Focused Local Verification

- `pnpm exec tsc --noEmit`
- `npx tsx --test app/lib/world/__tests__/worldConciergeTaskControlsContract.test.ts`
- `npx tsx --test app/lib/profile/__tests__/profileAnalyticsTrackContract.test.ts`
- `pnpm release:lock`

## Product Gates Before Tag

- confirm fresh-account login no longer produces `500` responses from `/api/profile/analytics/track`
- confirm owner-side activation analytics insert successfully once the new constraint migration is applied
- confirm logged-in non-Automation users do not poll `/api/world/concierge/tasks`
- confirm logged-in world hydration does not adopt malformed or stale cached route snapshots
- confirm public version badges, offline verifier surfaces, service-worker defaults, and release docs all move together when the final `v56.0.0` version cut is made

## Offline Verifier Repository Gates

- root package metadata resolves to `v56.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v56.0.0`
- current release docs and indexes point to the `v56.0.0` release artifacts
- verifier trust semantics remain unchanged relative to `v55.1.0`
- no verifier proof-format or producer payload migration is implied by these repository release/documentation updates alone
