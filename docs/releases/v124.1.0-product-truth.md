# Receiz v124.1.0 Product Truth — Reality Becomes Infrastructure

**The Production Runtime for Verified Civilization.** V124.1.0 composes source-carried proof, media, identity, conversation, memory, market continuity, and developer automation beneath the unchanged V124 constitutional ruleset.

Proof-derived cognition is a first-class breakthrough in this release: a subject's proof brain can retain complete admitted history, retrieve exact primary memory objects beyond the working window, and feed private cited context into Twin response and live speech. Generated cognition remains a non-authoritative proposal until deterministic command admission, and private source text never crosses into a delivered frame without express public designation.

The public `@receiz/sdk` surface exposes these primitives directly. Durable proof memory is **first admission only, then append forever**; MCP and AI Skills remain non-authoritative projections of that SDK law.

## Frozen Primitives

- The enclosing sealed proof object and its exact bytes are authority.
- RMA2, RMC1, RMC2, RMA3/raw, manifests, segments, progressive ledgers, package routes, object URLs, and browser storage are representations beneath that object.
- Verified progressive ranges are authorized only by a complete ledger root carried inside the enclosing Signature-V4 artifact.
- A genesis progressive proof binds to its signed artifact basis and committed manifest/ledger; successor-only native Record metadata is never a prerequisite.
- Receiz-ID-bound conversation events and participant bindings are the conversation source of truth; clear message bodies and epoch secrets remain trusted-host/private-device material.
- Proof memory derives from admitted history and cites exact proof objects. Twin responses are non-authoritative proposals.
- Identity Seal, Identity Record, and Receiz Key carry portable account continuity, including exact market positions and market history.
- Portable completeness is versioned: V1 retains its original pre-market domain set, and V2 requires the market domain. Later runtime additions cannot retroactively demote an older complete seal.
- MCP and AI Skills expose bounded mechanics, never proof, identity, ownership, memory, conversation, or settlement authority.

## Frozen Invariants

- Any sealed file remains exactly reconstructable. Browser-supported material may play or render directly; unsupported binary remains exportable without mutation.
- No database, CDN, third-party media host, or institutional assertion is required to establish proof truth.
- A public material coordinate is not exposed before its complete durable public family is available.
- Raw RMC2/RMA3 bytes never enter the compressed RMA2 decoder.
- Range playback never authorizes bytes from offsets, container metadata, or transport state alone.
- Known verified local/artifact truth paints before network discovery; later sync may append but cannot replace or delay it.
- Replay retains every valid sibling branch and never elects by arrival time or last-write-wins.
- Conversation grant work may be bounded per operation, but total invited membership and history are not capped.
- Revocation rotates the conversation epoch before later messages.
- Private imported memory text never enters a delivered cognition frame unless an owner expressly made the excerpt public.
- Image publication never enters video playback-material logic. Video publication is deterministic and cannot be represented as a user retry requirement when its internal proof math is coherent.
- Transfer reseals verify the sealed original against the current receipt and verify `previousDigest` as a separate predecessor-chain condition.
- Failed enclosing-artifact settlement is revoked and evicted even after viewer unmount.
- Institutional, governmental, corporate, academic, financial, and platform identity grants no exception from exact verification.

## Release Gates

- `pnpm test:v124.1-capability-parity`
- `pnpm validate:ai-skills`
- `pnpm test:ai-skills-distribution`
- `pnpm test:packed-source-carried-exports`
- `pnpm test:mcp-release-contracts`
- `pnpm test:coordinated-package-release`
- `pnpm test:release-freeze`
- Receiz Docs release, reference-source, type, lint, build, and archive checks
- Browser evidence for exact EPUB readiness and public profile count continuity

## Residual Risks

- `@receiz/sdk@124.1.0`, `@receiz/mcp-server@124.1.0`, and `@receiz/ai-skills@124.1.0` are not public until separately published and observed.
- Repository completion does not prove production deployment, live route smoke, multi-gigabyte device coverage, scale, adoption, or signed attestation.
- Browser-native playback depends on that browser's codec/container support. Exact reconstruction and export remain available when native rendering is unsupported.
- Legacy whole-SHA artifacts require complete settlement before final artifact admission; progressive behavior requires the signed progressive commitment.

## Standalone Projection

The standalone verifier and Offline Studio consume the exact application release bytes identified by SHA-256: verifier `da389f3df9ffe181ce019975d45263da76fbf89f53edf08eafe6682ec9380d22` and Studio `c0fbcff2bfbad8d051ce034798dbb8c8737fdf634351be2329134131c24aa037`.

The implemented primitive is the immutable source family and enclosing sealed proof object. Progressive ranges, stream readers, workers, URLs, QR codes, caches, SDK objects, MCP tools, AI output, server rows, and rendered media remain weaker execution, custody, transport, automation, or presentation mechanics. Known held proof truth paints before optional playback or network enrichment, and hidden work may append only verified additions.
