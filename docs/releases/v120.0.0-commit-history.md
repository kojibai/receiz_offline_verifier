# Receiz v120.0.0 Commit History

Developer surface: `@receiz/sdk@120.0.0`. Durable proof-memory law: first admission only, then append forever.

Baseline: `62b62ecf1` on `kojib/finish-longform-narration`.

Commit count: 2 candidate commits from the baseline, comprising the core v120 living-subject release and the final release-lock alignment.

No release tag, package publication, deployment, production smoke, or signed attestation is claimed by this ledger. The final boundary commit and exact commit count must be recorded only after the candidate tree is frozen.

## Standalone archive boundary

- Immutable standalone baseline: `v119.0.0` at
  `7058530b5264703d1888764de57ba2bb2b71ef89`.
- Coordinated application source observed on pushed `origin/main`:
  `f2fd8003d5603a322376e9465f4b5c70553a44a3`.
- The standalone v120 commit and annotated tag are created only after its own
  release gates pass; application and standalone commit counts remain distinct.
