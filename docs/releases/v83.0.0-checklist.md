# Receiz v83.0.0 Checklist

Release date: May 17, 2026
Status: Release prep

## Release Scope

- Current documented release anchor in this repository before this cut: `v82.0.0`.
- Broader product trail referenced by this release: the `v82.0.0` Sports launch through the `82.9.4` stabilization line.
- Broader release base referenced by this release: commit `223b2ddbbd`, 123 commits after tag `v82.0.0`, as supplied by the release brief.
- Pre-lock arc from `v82.0.0` to `82.9.4`: 188 changed files, 28,716 insertions, and 3,485 deletions.
- This checklist governs the `v83.0.0` Stable Live Proof Release cut and the outward version-surface alignment for this offline verifier repository.

## Required Release Artifacts

- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v70.0.0-invariant-register.md`
- `docs/releases/v80.0.0.md`
- `docs/releases/v80.0.0-product-truth.md`
- `docs/releases/v80.0.0-checklist.md`
- `docs/releases/v80.0.0-process.md`
- `docs/releases/v81.0.0.md`
- `docs/releases/v81.0.0-product-truth.md`
- `docs/releases/v81.0.0-checklist.md`
- `docs/releases/v81.0.0-process.md`
- `docs/releases/v82.0.0.md`
- `docs/releases/v82.0.0-product-truth.md`
- `docs/releases/v82.0.0-checklist.md`
- `docs/releases/v82.0.0-process.md`
- `docs/releases/v83.0.0.md`
- `docs/releases/v83.0.0-product-truth.md`
- `docs/releases/v83.0.0-checklist.md`
- `docs/releases/v83.0.0-process.md`
- current-release pointers updated in `README.md`, `docs/README.md`, `RELEASE_NOTES.md`, `CHANGELOG.md`, `docs/governance/README.md`, and `docs/FORMAT.md`
- product-truth reference status markers updated for `v83.0.0`
- root package metadata updated in `package.json`
- shipped version markers updated in `site/index.html`, `apps/offline-verifier.html`, `apps/offline-record-seal.html`, and `apps/offline-settlement.html`

## Canonical Product Gates

- Sports first paint shows the truthful shell and projects known game state before hydration.
- Main Event and single-game selection share the same scoreboard selection path.
- First selected single game receives the same immediate score path as scoreboard-clicked games.
- Live score and lock state stay scoped to the selected game.
- Active entry lock state does not rewrite unrelated field or scoreboard state.
- Known zero scores remain visible legitimate zero scores.
- Card reveal click, popover, drag, drop, selection, deselection, reward-card, and 1-card through 10-card spread behavior remain stable.
- Saved cards keep issue number, pack origin, card identity, proof object, current owner, append history, vault truth, and market history together.
- Pick 5 exposes starter, bench/role, backup, injured/out, Main Event, and single-game player availability with consistent language.
- Live-player sharing from iMessage works, and canonical public profile truth remains the first-paint source.
- SEO enrichment does not gate first paint for profile or market public routes.
- Public profile remains canonical at `/<username>`, and `?manage=1` remains an owner permission overlay.
- Explore paints an immediate deterministic shell without white-screen dependency on an in-flight build.
- `/market` keeps the fast entry path, and conformance checks transport proof truth instead of stale hot-path JSON-LD.
- Market proof, provenance, value, asset surfaces, and conformance stay governed.
- Payment and embed callback policy share one centralized allowlist path with Vercel trace inclusion.
- OTC and Cash Image proof objects support image-native Receiz ID wallet creation, private-key import, connected-wallet add, wallet bundle sealing, Cash Image proof, offline pulse transfer, proof export/import, and wallet-panel UI placement.
- Account Brain, Calendar, profile, account feedback, bottom navigation, owner/manage hydration, showcase, analytics, and one-viewport contracts avoid all-or-nothing surface failure.
- Market, Sports, interoperability, profile first-paint, service-worker version coherence, release freeze, page first paint, TypeScript, release lock, and release attestation conformance gates remain release boundaries.
- Release surfaces move together to `v83.0.0`.

## Focused Platform Verification

Release-prep commands expected in the broader platform workspace:

- `pnpm lint`
- `pnpm build`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm test:release-freeze`
- `pnpm test:page-first-paint`
- `pnpm test:value-loop-guard`
- `pnpm test:contacts-release-contracts`
- `pnpm test:signature-v4-server-remote`
- Sports first-paint, selected-game score, zero-score, lock-domain, and next-game hydration contracts
- Main Event and single-game scoreboard selection contracts
- card reveal, viewer, popover, drag/drop, reward reveal, deselection, saved-card issue, proof, owner, vault, and market continuity suites
- player availability language and lineup intelligence contracts for Main Event and single-game play
- iMessage live-player open, canonical public profile first paint, newly posted profile original navigation, structured proof, manifest, and owner/manage hydration contracts
- Explore deterministic shell and no-white-screen race contracts
- Market fast-entry, transport proof conformance, provenance, asset, value, and route hot-path contracts
- callback allowlist and Vercel trace inclusion contracts
- OTC wallet/Cash Image proof creation, import, export, offline pulse transfer, optional reconciliation, and wallet-panel UI contracts
- Account Brain, Calendar keyset pagination, profile showcase, profile analytics, navigation, and account feedback contracts
- service-worker version coherence, release lock, release attestation, and conformance history gates

## Offline Verifier Repository Gates

- root package metadata resolves to `v83.0.0`
- current shipped verifier, studio, and settlement entrypoints display `v83.0.0`
- release docs and indexes point to the `v83.0.0` release artifacts
- product-truth reference docs are linked from the public docs map and carried forward for `v83.0.0`
- broader platform validation for `v83.0.0` is captured in the canonical release note, product-truth document, process document, and checklist
- verifier trust semantics remain unchanged relative to `v60.0.0`
- no verifier proof-format or producer payload migration is implied by these repository release/documentation updates alone
- `pnpm lint` has zero warnings/errors
- `pnpm build` completes
- `pnpm release:lock` confirms governed release surfaces
- `pnpm release:attest` emits a signed attestation for the final commit before production shipment
- governed surfaces all show `v83.0.0`

## Release Rule

Do not ship `v83.0.0` unless the release gates in this checklist pass from the exact release candidate, `pnpm release:lock` confirms the release surfaces, and `pnpm release:attest` emits a signed attestation for the final commit.
