# Receiz v109.0.0 Commit History

Baseline: the v108.0.0 release state immediately before the v109 design commit.

Commit count: 3 coordinated v109 commits, including the final implementation commit.

- `870ad9eca` — define the v109 local offline SDK verification design.
- `454c8e9d2` — define the v109 implementation plan.
- Final implementation commit — package the local verifier, remove obsolete developer entry points, align SDK/MCP/AI skills at v109, seal ARTIFACT-011, and add release evidence.

The final implementation commit identifier is recorded by git history when this release change is committed on `main`.

The public `@receiz/sdk` preserves durable proof memory as first admission only, then append forever.

The upstream hashes above describe the coordinated Receiz.com/package workspace. This standalone offline-verifier release starts from local annotated tag `v108.0.0` at commit `f9b6dbab480cba2cc438f87b0591033a37bb0d0a`; its final local v109 commit is recorded by this repository's Git history.

## Release-Lock Requirement

`pnpm release:lock 109.0.0` must require this commit-history record for `v109.0.0` and later release cuts. The upstream package history and standalone verifier history must remain distinguishable, while both preserve sealed artifact authority and the exact external-action boundary.
