# Receiz v81.0.0 Product Truth

Release date: May 10, 2026

Canonical invariant references:

- `docs/literal-product-law.md`
- `docs/experience-first-engineering.md`
- `docs/deterministic-surfaces.md`
- `docs/verified-history-first-principles.md`
- `docs/offline-verified-register.md`
- `docs/pbi-recovery-receiz-id-binding.md`
- `docs/value-loop-invariants.md`
- `docs/releases/v70.0.0-invariant-register.md`

`v81.0.0` is the Signal Circuit provenance release lock for the 35-commit post-`v80.0.0` candidate ending at `23eec6d23`.

## Frozen Primitives

| Concept | v81.0.0 truth | Must not regress |
| --- | --- | --- |
| Product law | Literal doctrine, v70 profile law, v74 identity-artwork law, v75 local-proof/file-restore law, v76 World/Market/Public Proof law, v77-v78 Signal Circuit/Vault law, and v80 platform law remain binding. | Do not weaken exact law into approximation, and do not call visible regressions equivalent. |
| Release trail | v81 is measured from tag `v80.0.0` through candidate `23eec6d23`: 35 commits, 52 files, 8,289 insertions, 385 deletions. | Do not describe v81 as a label-only bump or omit the card DNA/payment/activity/mobile stabilization work. |
| Release surfaces | Package version, service-worker defaults, public badges, verifier surfaces, README pointer, release docs, and world resident worker markers move together. | Do not ship version drift. |
| Signal Card identity | A Signal Card remains a sealed Receiz asset. Its claim, art evidence, owner truth, marketplace state, transfer state, auction state, battle appends, and DNA history stay separated by role. | Do not mutate original claims, merge current owner with achievement truth, or treat cards as disposable gameplay UI. |
| Signal Card DNA | `/game/cards/history` is additive provenance around cards: origin, listing, auction, bid, sale, arena append, price point, and owner context. | Do not let history rewrite the sealed claim, require showcase publication, or reduce the route to personal vault state. |
| DNA navigation | Card pages, share-card surfaces, and Card World may link to DNA/history explicitly. | Do not auto-replace the sealed card surface with DNA or treat a remembered state as current open intent. |
| Card discovery memory | `/game/cards` may preserve same-session expanded discovery only after manual `More cards` action. | Do not trade settled continuity for automatic richer discovery, and do not collapse a user-expanded view when returning in the same session. |
| Rewarded-card seal payment | Rewarded-card sealing verifies the claim, checks eligible wallet lanes, charges idempotently, and returns precise funding deltas when short. | Do not reveal before payment authority, double charge repeated attempts, or hide funding shortfall behind generic error copy. |
| Rewarded-card seal ledger | A successful rewarded-card seal appends wallet/action history with card ID, claim hash, price, amount, balance, and already-processed state. | Do not make action-ledger append the only proof of charge, and do not omit the wallet activity trail. |
| Wallet and calendar card activity | Purchased packs, rewarded cards, rewarded-card seals, and card transfers surface in wallet/calendar activity as account history. | Do not hide card events inside game-only state or duplicate received transfers. |
| Card transfer movement | Transfer activity preserves sender, recipient, collectible ID, claim hash, card name, route, and note. | Do not create duplicate owner ambiguity or lose the card open path. |
| Mobile card chrome | Game bottom chrome, top popovers, proof manifest space, share transitions, route color scope, and stage fit stay stable on mobile/PWA. | Do not let chrome cover proof or actions, bleed dark styling into white routes, or make the surface feel like it loaded twice. |
| Explorer and discovery fit | Single-card explorer views, card backs, Find Cards entry, and discovery controls preserve readable proof and action space. | Do not squeeze the manifest into a capped box when available space exists, and do not let buttons collide with proof text. |
| Canonical profile | `/<username>` remains the canonical profile. `/<username>?manage=1` is the same profile with owner permissions layered on top. | Do not let session, workspace, owner, or manage reads redefine base identity, showcase, media, or first paint. |
| Verified truth | Verified local proof, route snapshots, deterministic manifests, ledger entries, vault records, card records, and portable state are known truth. | Do not refetch, re-prove, or rediscover stronger known truth before projecting it. |
| Identity continuity | Receiz ID, PBI/passkey recovery, Identity Record, Identity Seal, Receiz Key, email recovery, server session, and account switching converge into the same active signed-in shell after proof acceptance. | Do not require manual refresh, import completion, bind completion, wallet sync, Redis replay guard, or stale-cookie reconfirmation before the recovered account is visible. |
| Settlement and Reserve | Settlement is proof-native value; Reserve is funded action capital. Cards, shares, notes, sends, auctions, payments, transfers, card seals, and battles preserve funded causality. | Do not credit unfunded value as Reserve, double count holds, resurrect sold positions, or detach stakes from ledger/wallet truth. |
| Runtime coherence | Service workers, runtime build IDs, stale-session recovery, headers, app shell scripts, offline verifiers, public labels, and worker release markers are release surfaces. | Do not strand users on stale shell state, fake recovery UI, mismatched labels, or stale service-worker defaults. |

## Frozen Invariants

- A settled visible shell is immutable unless the user explicitly interacts.
- Truthful first paint beats richer rediscovery.
- Stronger known deterministic truth is reused before any network rediscovery.
- Weaker partial payloads never downgrade stronger known truth.
- Authenticated/private enrichment may append only; it may not slow or replace the base public shell.
- `/<username>` remains canonical profile truth.
- `/<username>?manage=1` remains canonical profile truth plus owner permissions.
- Verified stored truth is permanent known truth, not stale mutable cache.
- Sync asks only for verified additions after the held prefix.
- PBI/passkey recovery proves a legacy account; Receiz ID proves a device; binding is an idempotent append, not session issuance.
- `/api/auth/receiz-id/bind` must not depend on Redis or replay-guard storage.
- Identity Record, Identity Seal, and Receiz Key restore complete visible login at proof admission.
- Import, bind, wallet refresh, global mirror, and portable-state sync are enrichment jobs after the active account projects.
- Settlement, Reserve, wallet holds, notes, certificates, shares, auctions, transfers, card seals, and battle stakes must preserve funded causality.
- Market and wallet private state is same-actor only.
- Sold or transferred-out local truth cannot be resurrected by older snapshots.
- Signal Cards are Receiz assets, not mutable gameplay UI.
- Signal Card achievement truth, generated art evidence, transfer appends, battle appends, auction state, market state, current owner truth, and DNA history remain separated.
- Signal Card DNA may inspect card state, but it may not rewrite original card truth.
- Card discovery may remember explicit same-session expansion, but background discovery may not replace the settled visible surface.
- Rewarded-card sealing must verify before charge and charge before reveal.
- Rewarded-card seal attempts must be idempotent for repeated client nonce/card/claim attempts.
- Wallet/calendar activity for card events must not duplicate receive events.
- Vault Card export starts only after explicit user action.
- Battles append requests, settlements, ledger rows, transfer state, and card appends without mutating original claims.
- AI twin defense requires explicit owner authorization.
- Public Circuit, Exchange, Proof, Status, and conformance surfaces must match real route behavior and avoid private route exposure.
- Runtime stale recovery is hidden coherence work, not a visible second shell.
- Service-worker and app-version defaults must match the release version.
- World resident worker manifests must preserve numeric schema versions and carry the app release through a release marker.
- Release gates must catch visible behavior regressions, not merely match the latest code.

## Release Gates

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```

The full release-freeze script also runs product inventory, deterministic surfaces, page-first-paint, artifact binding, document seal, document-seal ZK, world trail package, golden path, contact contracts, game contracts, signature, API security, chaos failure, value-loop, economy, market, verification, world, Signal Circuit conformance, profile, chat, performance, and governance gates.

## Residual Risks

- The release is not fully locked until `pnpm lint`, `pnpm build`, `pnpm release:lock`, `pnpm test:release-freeze`, and `pnpm release:attest` pass after the `v81.0.0` surface bump.
- `pnpm release:attest` depends on release attestation signing environment outside the codebase.
- Target Supabase environments must apply the rewarded-card seal wallet charge migrations, ending on compact idempotency from `20260509193000_signal_run_rewarded_card_seal_compact_idempotency.sql`.
- External services including Supabase, Redis, Stripe, Resend, OpenAI, storage, Vercel, GitHub, Vercel Sandbox, media/render providers, wallet settlement rails, and browser/PWA download behavior remain deployment concerns, but they must not become permission to downgrade already-held deterministic truth.
- Mobile Safari, installed PWA, and in-app browser card/payment/download paths should continue to receive manual device verification before public rollout.
- Public status probes must remain curated as new proof, creator, card, exchange, battle, auction, history, and conformance routes are added.
