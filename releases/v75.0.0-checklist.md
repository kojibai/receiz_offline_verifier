# Receiz v75.0.0 Release Lock Checklist

Release date: May 2, 2026

Product truth: `docs/releases/v75.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `75.0.0`.
- Public release badges show `v75.0.0`.
- Service worker defaults use `75.0.0`.
- Offline verifier surfaces show `v75.0.0`.
- README manual release checklist points at `docs/releases/v75.0.0-checklist.md`.
- Receiz ID login admits local device truth before network propagation and starts server session publication in the background.
- Server Receiz ID continuation cannot downgrade a stronger local account binding with a stale or weaker server session for another account.
- The local account-binding register stores Receiz device, Receiz Key, PBI, email, and server-switch account authority without erasing unrelated bindings.
- Identity Record, Identity Seal, and Receiz Key restore project the recovered account immediately across account, profile, wallet, upgrade, bottom chrome, and switcher state.
- Receiz Key restore treats import, local publish, server binding, and wallet refresh as enrichment after proof admission, not as permission for the signed-in shell.
- First-time offline Receiz Key restore requires verified portable state, while later admitted local key truth can project without server rediscovery.
- Unsupported Receiz ID crypto routes to `/signin/receiz-id-fallback` with sanitized return paths, and primary sign-in surfaces remain Receiz ID pass-through.
- The backup sign-in route owns PBI/passkey, magic-link, and Identity Record / Seal / Receiz Key file restore without replacing the primary Receiz ID sign-in surface.
- Account, wallet, upgrade, and managed profile routes preserve the locally restored account over stale old-cookie or anonymous session echoes.
- Profile identity artwork and signed original-download flows preserve deterministic profile truth, purchase context, owner authorization, and PBI-gated download proof.
- Plans and Upgrade keep immediate payment / upgrade parity and old-browser fallback access parity.
- Market snapshots preserve stronger wallet, owned-item, prediction-history, and share-trade truth, but refuse cross-viewer private-state preservation.
- Sold share positions remain closed when local sell history is stronger than incoming market payloads.
- Market internal navigation, verify actions, live player launches, record-card links, and mobile press handling use stable navigation paths without double-fire behavior.
- Market mobile input and certificate surfaces keep no-zoom viewport behavior and 16px editable controls.
- Explore remains a card-first, dock-safe mobile surface with bounded swipe rendering, deterministic preview fallbacks, local-first route entry, and in-card read / play / save / share / proof actions.
- Product doctrine documents include Identity Record, Identity Seal, and Receiz Key restore parity with PBI recovery.
- HPL continuity review and Receiz identity continuity one-pager artifacts are present as release evidence.
- `pnpm release:lock` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm release:lock
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:release-freeze
pnpm release:attest
```
