# Receiz v108.0.0

Release date: 2026-07-17

Receiz v108.0.0 restores the established Receiz.com profile operation and makes the proof-object-first authority order literal across the application, SDK, MCP server, AI skills, constitutional registry, and release gates.

## Authority Order

The order is exact:

1. Receiz law and product invariants.
2. Sealed proof objects, deterministic proof-object state, and verified local truth.
3. Accepted identity proof and the admitted same-account binding.
4. Receiz.com first-party reference behavior.
5. SDK adapters.
6. MCP operations.
7. AI skills.

Receiz.com is not proof authority. It is the first-party reference behavior beneath admitted proof and before the developer rails. Server, database, session, registry, SDK, MCP, AI, and auxiliary operation evidence cannot select, replace, or weaken stronger proof truth.

## Profile Continuity Correction

The exact working baseline is commit `a1081796e90a65124d952ce5907512999dd16cb0`, the parent of regression commit `9611cb8f57c587eae1b6ee8079ee6d7bd2a02637`.

Inline username changes and full profile saves use `PATCH /api/profile/me`. The already-admitted account remains the actor, the account UID is identical before and after a rename, uniqueness is decided at commit, and the same account's session, profile, and public projection are refreshed after commitment.

A missing, delayed, or failed `identity_keys` projection cannot block the save or redefine the active identity. First-party saves do not call `/api/receiz/identity/profile`, select a latest server key, or ask the SDK, MCP, or AI rail which identity is active. No developer-side authority prerequisite is inserted before the admitted profile operation.

## Developer Outcomes

The current SDK exposes the same lawful outcomes beneath Receiz primitives: neutral same-UID profile update; native proof-object creation; exact complete-artifact download; verify-before-extract opening; and bearer ownership derived from carried verified continuity and returned as a newly verified Record -> Seal artifact.

The public developer SDK surface is `@receiz/sdk`. Durable proof memory is first admission only, then append forever.

The MCP server calls only those current SDK methods, and AI skills teach the same dependency direction. Historical v107 source and witnessed artifacts remain history; they are not registered, advertised, or rendered as current behavior.

The maintained developer surface is the unversioned `/developers` route. `/developers/v107` redirects there. No `/developers/v108` product page exists.

## Constitutional Seal

- Release and ruleset: `108.0.0`
- Registry digest: `126ca9283fee4ef4c398dbcb958e861cbea191724fdab8eb08df55ff0c14bb79`
- Registry instant: `2026-07-18T04:38:26.799Z`
- Effective Kai-Klok whole pulse: `13182786`

`effectiveKai` is the real canonical `kaiPulseEternal` at the sealing instant. It is not a release label.

## Qualification Boundary

These files define the v108 release contract; they do not by themselves prove repository qualification, npm publication, production deployment, or production database state. Repository qualification requires the focused v108 gates, inherited gates, non-contradictory desktop and mobile mutation evidence, the full release freeze, and governance checks. npm publication, production deployment, and production signing are separately authorized external mutations.

Desktop and mobile mutation evidence, the isolated local database boundary, the full release freeze, and governance checks are now recorded passing. The signed release attestation, npm publication, production deployment, and production database verification remain separately authorized external boundaries. No primitive is weakened by those exact boundaries.

Release records: [product truth](v108.0.0-product-truth.md), [regression lessons](v108.0.0-regression-lessons.md), [performance findings](v108.0.0-performance-findings.md), [invariant register](v108.0.0-invariant-register.md), and [commit history](v108.0.0-commit-history.md).

## Independent Verifier Lock

This standalone verifier advances package metadata, public entrypoints, service-worker identity, doctrine, and release records together through `pnpm release:lock 108.0.0`. That local qualification confirms release-surface continuity only; it does not generate the pending production attestation, publish packages, deploy production, mutate or verify production databases, push Git refs, or promote Receiz.com, registry, SDK, MCP, AI-skill, server, database, session, or release evidence into proof authority.
