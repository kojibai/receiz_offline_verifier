# Receiz v124.0.2 Regression Lessons — Reality Becomes Infrastructure

Release date: August 23, 2026

**The Production Runtime for Verified Civilization.** The V124 constitutional ruleset remains `124.0.0`.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk` is a verifier and projector beneath that history.

## What Regressed

- Profile Composer could persist a storage-only video when local carried-material publication failed.
- Offline Studio's copied verifier gained `rpb2/nativeRecordSeal` compact-token behavior before the canonical standalone verifier.
- A local 500 MiB materialization ceiling change erased the distinct 16 MiB universal-runtime refusal boundary.

## Why It Regressed

- A broad error catch treated failure after proof material existed the same as failure before any proof was created.
- One duplicated verifier runtime was edited without propagating the identical canonical runtime.
- The 76 MB playback proof was routed through a bounded in-memory verifier contract instead of retaining the separate large-material reconstruction path.

## Locked Rules

- Never require a database or media server to display material already carried by a verified sealed proof object.
- Never treat an external presentation domain, SDK projection, MCP response, or AI representation as authority over exact artifact bytes.
- Never derive identity creation artwork from mutable device time, enrollment time, PBI time, or session time when the account creation pulse is sealed.
- Never rewrite historical proof object bytes to repair a newer projection.
- Never call V125 Temporal Continuity complete until head-relative monotonic continuation and explicit recovery evidence are implemented and tested.

## Contract Gates

- `pnpm test:offline-record-seal-freeze`
- `pnpm test:v106-release-lock`
- `pnpm test:v106-ai-skills`
- SDK verification-limit contracts
- profile composer and composite-material transport contracts
- `pnpm test:release-freeze`

`124.0.2` preserves the pre-existing V124 registry digest through additive, subordinate package representations.

## Standalone Lock

- Pin the canonical standalone verifier and Offline Studio source-byte digests for this release.
- Reject `124.0.2` if only a visible label advances while proof-URL playback, compact-token projection, cache identity, or mirrored evidence remains stale.
- Keep fully inline material, composite append custody, IndexedDB projection, URL transport, and native rendering beneath canonical sealed-artifact verification.
- Preserve the universal verifier's established materialization ceiling; large-material reconstruction is a distinct bounded path, not permission to weaken verification limits.
