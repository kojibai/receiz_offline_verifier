# Receiz v126.0.0 Product Truth

## Frozen Primitives

Receiz ID, sealed proof objects, ownership, provenance, settlement, public proof, and offline verification retain their implemented authority.

## Frozen Invariants

Known complete source paints immediately. Verified additions append after the held head. Creation pulses and historical proof bytes remain immutable. First sealing records the creator; transfer changes current ownership through a verified append. Server projections cannot replace sealed truth.

## Release Gates

Run `pnpm test:release-freeze`, coordinated package checks, current release drift, compiler contracts, SDK/MCP tests, Academy validation, and governance controls. A version label is not a pass.

## Residual Risks

Live publication and deployed browser behavior require separate verification. Historical reports retain their original versions and outcomes. No unrun gate is certified here.

Coordinated package: `@receiz/sdk@126.0.0`. Durable proof memory law: first admission only, then append forever.

## Standalone Projection

The standalone distribution projects the exact v126 verifier bytes at SHA-256 `822b558ceeeef3db7b005346f6992580fe01ab378b95d8b64284d6844abf55b8` and exact Offline Record and Seal Studio bytes at SHA-256 `51003369c3e2ff70bc35013b55443c04f878563b05e4509b489cceea89e6f956`, imported from committed application source `d69d6bdfa8277a7933d16015346f38ba4aa37420` without modifying it.

The enclosing sealed proof object remains the strongest available truth. Known complete source paints immediately; server, route, session, SDK, MCP, AI, cache, and generic display state remain subordinate and may only append verified additions. No standalone surface downgrades a proof object to a card, identity to a preference, ownership to a badge, Settlement to a balance, public proof to a share page, verified history to an activity feed, or deterministic first paint to loading state.
