# Receiz v124.0.3 Compatibility Matrix — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** Every compatibility statement keeps the sealed proof object above package, tag, segment, server, database, session, model, URL, QR, preview, and UI representations.

| Subject | V124.0.3 compatibility statement |
| --- | --- |
| Coordinated package identity | Application, `@receiz/ai-skills`, `@receiz/sdk`, `@receiz/mcp-server`, and the constitutional verifier are prepared at `124.0.3`; publication is not claimed by the manifest. |
| Application compatibility | Application and SDK compatibility remains exactly `>=124.0.0 <125.0.0`. |
| Constitutional registry | V124.0.3 inherits the byte-identical `124.0.0` registry at digest `d02429151b0bcebdaeb89485792e377afc55130f9a25e07982c1c88221314247`; no V124.0.3 registry is created. |
| Ruleset identity | `RECEIZ_RULESET_VERSION` remains `124.0.0`; package identity `124.0.3` does not reinterpret constitutional law. |
| Historical proof objects | Historical sealed proof-object bytes, schemas, signatures, Groth16 verification, payload binding, ownership continuity, append law, and verification semantics remain unchanged and require no reseal. |
| Exact-byte verification | SDK, Workbench, `/verify`, Offline Studio, and standalone verification reject current-basis/native-ownership drift with the same law. |
| PBI authorship | Existing valid PBI descendants remain valid. New Workbench exports release the exact descendant and add no export-only verifier exception or ownership transfer. |
| Fully inline material | `rma2` remains a fully inline exact proof-object carrier for image, audio, video, PDF, text, and binary material. |
| Large-material transport | `rmc1` remains a bounded head over exact ordered committed segments; a fresh viewer retrieves those segments before complete proof-object verification and playback. |
| Offline verification | Held artifacts verify from their own exact bytes without account, session, database, or network authority. |
| Compact proof links | Existing `rpb1` and `rpb2` meanings remain stable. A carried `?c=` proof remains additive and never replaces an existing `/v` proof coordinate. |
| Profile playback | Working image/audio publication is unchanged. Video playback remains an explicit presentation beneath the exact admitted Profile proof object. |
| Public QR | Offline Studio carries its completed compact `?c=` proof URL in a clickable 92×92 QR without changing recorded source bytes. |
| V125 boundary | Closed external-source-free reconstruction for arbitrarily large files remains a V125 Kai-Turah/HRC objective and is not complete in V124.0.3. |

The pre-existing V124 registry digest remains immutable. All V124.0.3 mechanics are corrective or additive beneath the unchanged proof law.
