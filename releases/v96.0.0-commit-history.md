# Receiz v96.0.0 Commit History

Release date: 2026-06-29

Baseline: `v95.0.0`

Commit count: `pending release-lock commit`

This `v96.0.0` release-lock state captures the SDK A+ work after `v95.0.0`:

- Added `receiz` CLI to `@receiz/sdk`.
- Added local SDK fixture conformance with zero network and zero DB authority.
- Added proof payload inspection and local-first starter generation.
- Added passive `ReceizSdkObservation` events for proof memory admission and persistence.
- Added copy-paste integration docs for React, browser admission, append-sync routes, webhook receivers, and Sports card rendering.
- Added `scripts/test_sdk_release_contracts.ts`.
- Wired `test:sdk-release-contracts` into `test:release-freeze`.
- Bumped public app and SDK release identity to `v96.0.0`.
- Updated this offline verifier checkout's release docs, version markers, and local release-lock wrapper for `v96.0.0`.

The release lesson from `v95.0.0` remains intact: public developer contracts must never understate internal proof truth. `v96.0.0` extends that lesson into release evidence. The SDK is now tested and packaged as a proof-native developer surface, while the law remains first admission only, then append forever.

## Release-Lock Requirement

`pnpm release:lock 96.0.0` must require this document for `v96.0.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the v95 baseline, SDK CLI work, local conformance, passive observation, copy-paste integration docs, package evidence, release-freeze wiring, and repo-local release-lock edits would not be explicit release truth.
