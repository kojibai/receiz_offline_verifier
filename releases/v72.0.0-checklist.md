# Receiz v72.0.0 Release Lock Checklist

Release date: April 28, 2026

Product truth: `docs/releases/v72.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `72.0.0`.
- Public release badges show `v72.0.0`.
- Service worker defaults use `72.0.0`.
- Offline verifier surfaces show `v72.0.0`.
- Account-switch continuity contracts pass.
- Market private-state continuity refuses cross-viewer preservation.
- World twin mobile overlay fit contracts pass.
- `pnpm release:lock` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:release-freeze` passes before tagging.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm release:lock
pnpm test:continuity-release-contracts
pnpm test:world-release-contracts
pnpm test:release-freeze
```
