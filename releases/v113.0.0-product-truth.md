# Receiz v113.0.0 Product Truth

Production Global Reconciliation is acceptance by the explicitly named `receiz.com/global/v1` coordination domain. It is not universal consensus and does not outrank sealed artifact truth.

## Frozen Primitives

The proof object is the portable subject. Exact sealed bytes establish artifact truth. Independently verified carried history establishes causal predecessor and ownership truth. Identity admission supplies actor and key evidence. The reconciliation plan binds the verified candidate to one tenant, actor, artifact, expected head, registry, matrix, effects ceiling, idempotency identity, attempt, and named coordination domain. The Identity Seal capability authorizes only that exact completed plan.

## Frozen Invariants

Private immutable storage preserves neutral candidate bytes. The atomic transaction coordinates the named domain’s accepted head. Receipts and projections report that acceptance and never become authority. Connect authenticates delegated API access only. Supabase/Postgres and object storage remain provider-private mechanics.

`ownership.transition` follows verified causal order and retains every earlier owner. A later revocation cannot retroactively erase an earlier lawful handoff. Chronos cannot select a winner. Divergent siblings remain divergent.

Known artifact truth paints before any remote coordination starts. Resolution may append verified additions behind the settled proof object; it cannot remount, replace, or downgrade it.

## Release Gates

The v113 release lock, reconciliation suite, coordinated packed-package qualification, full release freeze, governance controls, production build, and release attestation must pass from this exact tree.

Release records: [main note](v113.0.0.md), [checklist](v113.0.0-checklist.md), [process](v113.0.0-process.md), [regression lessons](v113.0.0-regression-lessons.md), [performance findings](v113.0.0-performance-findings.md), [invariant register](v113.0.0-invariant-register.md), and [commit history](v113.0.0-commit-history.md).

## Residual Risks

The additive private migration remains unapplied in this commit. Package publication, deployment composition, migration application, and production smoke verification are separate authorized release operations; until performed, the new production coordination routes remain fail-closed.
