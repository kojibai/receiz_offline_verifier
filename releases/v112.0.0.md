# Receiz v112.0.0

Release date: 2026-07-19

Public developer surface: `@receiz/sdk` v112.0.0. Durable proof memory remains **first admission only, then append forever**.

v112 makes the artifact authority hierarchy executable. Exact bytes establish artifact truth; admission establishes profile membership; verified identity admission supplies actor evidence; verified history supplies predecessor truth; a transition digest identifies portable successor meaning; a plan binds actor, resource, registry, derived effects, idempotency identity, and commit domain; a verified capability authorizes that exact plan; neutral staging preserves sealed candidate bytes; independent commit verification atomically advances one named domain; and the receipt reports acceptance without becoming authority.

The package exports the recursively frozen current registry and detached loader, identity-key capability signing for Ed25519 and P-256, the async admission engine, and an IndexedDB-backed browser admission ledger. Durable local admission truth is not cache. The store never contains proof objects or sealed artifact bytes, so known artifact truth paints first.

The deterministic unsigned registry lock is `docs/releases/v112.0.0-constitution-registry.json`, digest `1356f8122d0b5fcbe891d7e6ed1e75faca827f15d63d1ed5d950664e11c146ee`, pinned to the v111 predecessor chain and generated into `@receiz/sdk`. No native `.receiz` or signed-wrapper ceremony is required or claimed for v112.

v111 exact bytes remain verifiable and eligible for current profile admission. Historical runtime objects do not gain current mutation authority. v112 adds no product HTTP route and no database migration. Remote global admission and offline-to-global reconciliation are outside v112.0.

Evidence: [product truth](v112.0.0-product-truth.md), [checklist](v112.0.0-checklist.md), [process](v112.0.0-process.md), [regression lessons](v112.0.0-regression-lessons.md), [performance findings](v112.0.0-performance-findings.md), [invariant register](v112.0.0-invariant-register.md), [compatibility matrix](v112.0.0-compatibility-matrix.md), and [commit history](v112.0.0-commit-history.md).

Release evidence paths: `docs/releases/v112.0.0-product-truth.md`, `docs/releases/v112.0.0-regression-lessons.md`, and `docs/releases/v112.0.0-commit-history.md`.

Release evidence explicitly covers exact-byte artifact truth; verification/admission separation; verified actor/history; transition-plan-command separation; namespace preservation/reducers; the pinned registry chain; neutral staging; independent commit verification; named-domain atomicity; receipt non-authority; v111 compatibility; no new product HTTP route; no migration; focused, package, and full-release checks; visual evidence; and exact uncertainty boundaries. Task 14 records the full release freeze and desktop/mobile browser evidence passing on 2026-07-19. The signed governance attestation remains post-push so it can bind the final commit SHA.

Standalone qualification uses:

```bash
node scripts/check_release_lock.mjs 112.0.0
pnpm lint
pnpm build
pnpm release:lock 112.0.0
git diff --check
```

These records do not publish packages, deploy Receiz.com, mutate production data, emit the GitHub Environment attestation, or push Git refs.
