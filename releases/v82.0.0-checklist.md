# Receiz v82.0.0 Release Lock Checklist

Release date: May 14, 2026

Product truth: `docs/releases/v82.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `82.0.0`.
- Public release badges show `v82.0.0`.
- Service worker defaults use `82.0.0`.
- Offline verifier surfaces show `v82.0.0`.
- README manual release checklist points at `docs/releases/v82.0.0-checklist.md`.
- World resident surface-worker manifests preserve numeric schema `version` and expose `releaseVersion: "v82.0.0"`.
- Main release notes describe the exact `v80.0.0` to `v82.0.0` arc: release base `edb39fa11a`, 174 commits before release-lock edits, 242 changed files including evidence artifacts, 125,626 insertions, 508 deletions.
- Main release notes describe the code/documentation arc excluding QA artifacts: 185 changed files, 56,576 insertions, 508 deletions.
- Sports Arena routes `/game/sports` and `/game/sports/baseball` load as first-class game surfaces.
- `/game` exposes a Sports icon and route path without replacing Signal Run.
- Sports bottom navigation and global navigation do not cover gameplay panels.
- MLB registry, player image, card issue, pack, entry, appends, market, trade, tournament, worker, and conformance routes are present.
- Base-set cards do not show fake card rarity.
- Owned rarity filters show only owned issued rarity variants.
- Card viewer opens the highest rarity first, then lowest issue number.
- Users can switch owned rarity versions from the card viewer ownership list.
- Issue numbers render as plain numbers, not padded/comma-formatted labels.
- Downloaded Sports cards include pack ID, issue number, rarity when issued, provenance, KAI pulse, claim, and card memory metadata.
- Pack odds separate player scarcity from card rarity.
- Pack simulator returns published odds and observed odds.
- Pack health does not hide bad spread, bad duplicate pressure, or over-available elite/player/rareness outcomes.
- Starter, premium, reward, and chase pack odds remain deterministic and auditable.
- Pack reveal sync includes issuance data without requiring Retry Sync for normal opens.
- Free first card disappears after use.
- Pick 5 selected cards rise to the top.
- Per-game Pick 5 accepts one to five eligible owned matchup cards.
- Main Event Pick 5 requires the slate lineup and stays separate from per-game play.
- Main Event button text fits on mobile and routes to paid tournament entry.
- Final games cannot be entered as live games.
- Final games remain selectable only when reward/proof claim requires user action.
- Active entries cannot overwrite live scoreboards.
- Active entries cannot overwrite live field text.
- Selected field data must match selected game.
- Selected final games must not borrow the first live game's field.
- Pregame field state shows selected matchup, venue, first-pitch readiness, and both teams' star player cards.
- Pregame field state never labels a scheduled game as a live play.
- Live cockpit appears only for the selected context and submitted lineup.
- Main Event cockpit appears only when Main Event is selected.
- Single-game cockpit appears only when that single game is selected and entered.
- Cockpit totals match the sum of visible scoped cards.
- Negative cockpit totals render as negative movement, not green gain.
- Leaders show scoped live scores, not zero while ranking.
- Pick 5 tile scores, leaders, and cockpit scores use the same scoped source.
- Claim reward copy appears only after finality.
- Live proof copy appears during live games before claim is available.
- Claimed proof buttons do not re-enter old games.
- Old finals clear after rewards are opened.
- Main Event finality closes, pays, records result, and allows next event entry.
- Single-game finality closes, records reward/proof state, and allows upcoming games after claim.
- Sports card career appends persist entry, rank, reward, source date, game key, lineup scope, score, and append hash.
- Card viewer live number uses the same scoped current score as Sports gameplay surfaces.
- Sports sales page uses wallet-first funding and Stripe delta recovery without internal dev copy.
- Sports purchase buttons use readable contrast and compact premium layout.
- Value panel is opaque/frosted enough that the card behind it does not fight the panel.
- Value panel uses compact market rows and stock-style line charts.
- Sports trading supports proposals, basket trades, accept, decline, cancel, and counterproposal-ready state.
- Trade actions return conflict when the trade is no longer open.
- Unexpected trade lookup failures return server failure, not missing-trade success.
- Sports conformance route, API route, badge, history, and rollups are accessible.
- Sports conformance can run rarity scoring and display official rarity/odds evidence.
- Sports conformance hot fanout check passes.
- Sports checkout delta recovery conformance passes.
- Sports signed history payload shape is accepted.
- Sports public hot snapshots are warmed by worker/cron.
- Stream fanout reads warmed public snapshots before fresh live/tournament work.
- Signed-in account overlays are throttled away from every stream tick.
- Scale guard contracts cover snapshot/cache/cron/stream behavior.
- `/game/sports` login behavior matches bottom-bar login behavior and keeps the signed-in state.
- Installed PWA and Safari hydrate card catalog and entered state without requiring a manual Arena tap.
- Profile original signing projects signed state after first PBI signature.
- Newly posted profile originals can navigate to live player without leave-and-return.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:game-release-contracts` passes.
- `pnpm test:signal-circuit-conformance` passes.
- `pnpm test:economy-conformance` passes.
- `pnpm test:market-conformance` passes.
- `pnpm test:verification-conformance` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:economy-conformance
pnpm test:market-conformance
pnpm test:verification-conformance
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```

Minimum fast release confidence for the current edit set:

```bash
pnpm release:lock
npx tsx --test app/game/__tests__/sportsArenaRouteContract.test.ts
npx tsx --test app/game/__tests__/sportsArenaScaleModel.test.ts
pnpm exec tsc --noEmit --pretty false
pnpm run build
```
