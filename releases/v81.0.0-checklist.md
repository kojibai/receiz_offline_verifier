# Receiz v81.0.0 Release Lock Checklist

Release date: May 10, 2026

Product truth: `docs/releases/v81.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `81.0.0`.
- Public release badges show `v81.0.0`.
- Service worker defaults use `81.0.0`.
- Offline verifier surfaces show `v81.0.0`.
- README manual release checklist points at `docs/releases/v81.0.0-checklist.md`.
- World resident surface-worker manifests preserve numeric schema `version` and expose `releaseVersion: "v81.0.0"`.
- The main release notes describe the exact post-`v80.0.0` candidate: 35 commits, 52 changed files, 8,289 insertions, 385 deletions, ending at `23eec6d23`.
- Signal Card DNA at `/game/cards/history` keeps sealed origin, listings, auctions, bids, sales, and verified arena appends additive to the original card claim.
- Card DNA links from card pages, share-card surfaces, and Card World remain explicit navigation, not automatic replacement of the sealed card surface.
- `/game/cards` keeps manual `More cards` behavior and may remember same-session expansion only after explicit user action.
- Rewarded-card seal payment verifies the card claim before charging.
- Rewarded-card seal payment spends eligible wallet lanes before opening checkout funding.
- Rewarded-card seal payment remains idempotent for repeated client nonce/card/claim attempts.
- Rewarded-card seal funding failures return the precise missing funding delta and Stripe-safe checkout amount.
- Rewarded-card seal action history appends to wallet activity without blocking the seal response if action-ledger append has a transient failure.
- Wallet and calendar activity include purchased packs, rewarded cards, rewarded-card seals, and card transfers without duplicate receive rows.
- Card transfer activity keeps sender, recipient, collectible ID, claim hash, card name, and open path coherent.
- Card history search and filters must not drop stronger card truth or reduce the route to a personal vault-only read.
- Explorer and discovery card layouts keep proof manifests, bottom actions, top popovers, and stage content readable on mobile.
- Game bottom chrome must not cover top-bar popovers.
- Dark game route styling must not bleed into white product routes.
- Card share animation, Find Cards entry, and short share links remain stable.
- Proof manifest lists on card backs use available space without crowding the description or back actions.
- World mobile bottom chrome remains contract-bound.
- Market navigation and profile route continuity remain contract-bound.
- `/<username>` remains canonical profile truth and `?manage=1` remains owner permission overlay only.
- Local proof, Receiz ID, PBI/passkey, Identity Record, Identity Seal, Receiz Key, email, and server-session paths converge on the same signed-in shell after proof acceptance.
- File restore projects the recovered account before import, bind, wallet refresh, or global sync jobs complete.
- `Receiz ID -> account` binding remains idempotent and does not depend on Redis or replay-guard storage.
- Settlement and Reserve value paths preserve funded causality across sends, notes, certificates, shares, auctions, packs, battles, payments, transfers, card seals, and wallet holds.
- Runtime stale recovery stays hidden; it must not show a root interstitial, fake loader, or automatic visible route takeover.
- Service-worker registration defaults, runtime build IDs, update checks, private-browser behavior, and PWA header behavior move with the release.
- Public/developer Status excludes private vault writes, pack purchase internals, worker routes, admin routes, webhooks, privileged issuer routes, and legacy keyring internals.
- Value-loop copy remains contract-bound to reserve, settlement, ownership, wallet, and proof truth.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:game-release-contracts` passes.
- `pnpm test:signal-circuit-conformance` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```
