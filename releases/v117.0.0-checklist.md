# Receiz v117.0.0 Release Lock Checklist

Release records: [product truth](v117.0.0-product-truth.md), [regression lessons](v117.0.0-regression-lessons.md), and [complete commit history](v117.0.0-commit-history.md).

Canonical paths: `docs/releases/v117.0.0-product-truth.md`, `docs/releases/v117.0.0-regression-lessons.md`, and `docs/releases/v117.0.0-commit-history.md`.

## Identity and history

- [x] Preserve immutable `v116.0.0` predecessor and all historical v116 exports.
- [x] Generate additive v117 registry and canonical digest.
- [x] Promote current registry, ruleset, SDK matrix, CLI, runtime, service worker, and offline surfaces.
- [x] Coordinate app, SDK, MCP, verifier, AI skills, workspace pins, and lockfile at `117.0.0`.
- [x] Move unimplemented orchestration planning to v118 without claiming shipment.

## Product law

- [x] Keep deterministic first paint free of SEO, metadata, DB, session, and network blockers.
- [x] Preserve held Contacts/Profile/Wallet/Sports proof and settlement truth against weaker projections.
- [x] Keep Sports Arena behavior identical while source remains below 500,000 bytes.
- [x] Keep `/t/` transaction and `/v/` adaptable moment share surfaces masked, deterministic, and non-overlapping.
- [x] Download an existing transaction Moment without resealing.
- [x] Run release gates after an unavailable isolated DB fixture and aggregate real failures.

## Required evidence

- [x] `pnpm test:v117-release-lock`
- [x] `pnpm test:v117-skill-contracts`
- [x] `pnpm test:coordinated-package-release`
- [x] `pnpm test:release-freeze` (all runnable gates completed; isolated profile DB explicitly unavailable because no isolated DB URL was supplied)
- [x] `pnpm governance:check`
- [x] `pnpm governance:evidence-check`
- [x] `pnpm build`
- [x] browser screenshots confirm Sports and share surfaces
- [ ] `pnpm release:attest`
- [ ] verify generated attestation
- [ ] clean working tree, release commit, annotated `v117.0.0` tag

The required evidence above is the coordinated application/package release
boundary. It is not converted to passing evidence by mirroring the records into
this standalone repository.

## Standalone repository evidence

- [x] `node scripts/check_release_lock.mjs 117.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 117.0.0`
- [x] mirrored v117 archive and v118 planning records are byte-identical
- [x] all relative links in the current standalone release archive resolve
- [x] v117 registry version, 79-law inventory, predecessor, and digest record verified
- [x] `git diff --check`
- [ ] clean working tree, local release commit, annotated `v117.0.0` tag

Unchecked items must be replaced with exact evidence before the release is declared complete.

The `@receiz/sdk` v117 release must preserve durable proof memory as **first admission only, then append forever**.
