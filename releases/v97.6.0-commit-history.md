# Receiz v97.6.0 Commit History

This release document summarizes the working set since `v97.5.0`. Git commit hashes should be finalized after the release commit is created.

Baseline: `v97.5.0`

Commit count: pending final release commit

## Major Working Set

- Added signed merchant public-store publish helpers to `@receiz/sdk`.
- Added signed public-store envelope verification and append-coordinate response to the public proof rail.
- Added contracts for signed publish, token suppression, tamper rejection, and server route/store behavior.
- Added `/proof-graph` as a live public proof graph surface.
- Added proof graph network data, mobile graph views, and live `☤KAI` pulse behavior.
- Added proof graph links to `/exchange`.
- Bumped visible app/offline labels to `v97.6.0` and SDK, MCP, and service-worker package/runtime identities to `97.6.0`.
- Corrected Sports saved-card base issue probability language.
- Added release documents and SDK/MCP authority-boundary guidance.
- Preserved durable proof memory law: first admission only, then append forever.

## Expected Release Commit

```txt
v97.6.0 signed merchant public-store, proof graph, MCP/SW law release
```

## Release-Lock Requirement

`pnpm release:lock 97.6.0` must require this document for `v97.6.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the `v97.5.0` baseline, signed merchant public-store rail, public proof graph surface, SDK/MCP package alignment, service-worker release identity, Sports saved-card language, and repo-local release-lock edits would not be explicit release truth.
