# Receiz v124.0.3 V125 Horizon — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** V124.0.3 preserves ruleset `124.0.0`, restores exact source authority across every verifier/export surface, and keeps the playable-proof URL primitive intact. V125 begins only after this boundary is live-qualified.

## Settled foundation entering V125

- The proof object, not its playback or export representation, is authority.
- Record/Seal/Verify/Append/PBI operate on exact source and descendant bytes with one canonical verifier law.
- Offline held-artifact verification is primary and account-free.
- Small material that fits practical browser URL limits can remain carried and playable without network access.
- Large `rmc1` playback remains additive and may retrieve exact committed segments on a fresh viewer; custody never becomes proof authority.
- Profile image/audio behavior and playable URLs are settled reference implementations for source-first video publication.

## Temporal continuity

V125 is the native temporal-continuity release: verified state continues monotonically from exact authenticated heads; additions append without rewriting settled history; recovery is explicit; and Kai/Chronos ordering preserves causality across local and coordinated surfaces.

## Closed Kai-Turah/HRC reconstruction

V125 may apply existing Kai-Turah sigil definitions, Harmonic Resonance Computing relations, harmonic coordinates, deterministic graph operations, recursive composition, exact residuals, and proof-object glyph roots to reconstruct large material without external retrieval.

The requirement remains absolute:

```text
decode(encode(X)) == X
```

No approximation, semantic regeneration, remote dictionary, database lookup, object-store dependency, server authority, or weakened verification is permitted. The closed package must contain the law, canonical parameters, graph, exact residual, and irreducible authority required to reproduce source bytes offline.

## Acceptance boundary

1. Disconnect the network and remove database, cache, server, object-store, and remote-dictionary copies.
2. Begin with only a conforming decoder and the closed reconstruction package.
3. Resolve the root sigil and deterministic graph.
4. Inject the exact residual and compose the artifact.
5. Require byte length, byte equality, SHA-256 equality, and complete Receiz proof verification.
6. Reject any candidate that is approximate, depends on external material, or exceeds its literal source without a justified authority requirement.

V124.0.3 does not claim this closure. It contributes the prerequisite discipline: representation can never outrank source, and every future reconstruction must return to exact bytes before proof admission.
