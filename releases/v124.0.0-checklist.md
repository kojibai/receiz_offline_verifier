# Receiz v124.0.0 Release Lock Checklist — Reality Becomes Infrastructure

**The Production Runtime for Verified Civilization.** Every checked item must preserve sealed proof object authority, exact authenticated heads, and durable proof memory as first admission only, then append forever.

## Repository candidate

- [ ] `pnpm test:v124-release-lock` passes.
- [ ] Coordinated release identity tests pass for `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` at `124.0.0`.
- [ ] Focused V124 SDK adversarial and typecheck gates pass.
- [ ] Focused AI Skills and MCP metadata gates pass.
- [ ] OpenAPI generation and contract tests pass.
- [ ] Receiz Docs V124 archive and evidence tests pass.
- [ ] Package dry-run and current-version cleanliness checks pass.
- [ ] Release-freeze focused contracts pass.
- [ ] `git diff --check` passes.
- [ ] Historical V123 release, registry, digest, and generated-registry bytes remain exact.

The standalone Academy lock remains publication-dependent. After all three coordinated V124 packages exist in the configured registry, run `pnpm --dir receiz-docs install --lockfile-only --no-frozen-lockfile`, review the resolved V124 package integrities, then run `pnpm --dir receiz-docs install --frozen-lockfile`. Do not relabel V123 integrity hashes or local tarballs as published V124 registry resolutions.

## External operations

- [ ] NPM packages published for the coordinated V124 package set.
- [ ] Production migration deployed and independently verified.
- [ ] Production deployment completed with exact release identity.
- [ ] Production smoke verified and recorded.
- [ ] Scale evidence captured and bound.
- [ ] Adoption evidence captured and bound.
- [ ] Signed release attestation generated with `pnpm release:attest`.
- [ ] Git tag `v124.0.0` created and verified.

Unchecked external items are not pending proof claims; they explicitly mean not performed by this repository candidate.

Release authority documents: [product truth](v124.0.0-product-truth.md), [regression lessons](v124.0.0-regression-lessons.md), and [commit history](v124.0.0-commit-history.md).

Canonical paths: `docs/releases/v124.0.0-product-truth.md`, `docs/releases/v124.0.0-regression-lessons.md`, and `docs/releases/v124.0.0-commit-history.md`.

## Standalone repository evidence

- [x] Standalone release surfaces align exactly at `124.0.0`.
- [x] All 17 canonical records are mirrored byte-for-byte in `docs/releases/` and `releases/`.
- [x] Registry digest, immutable predecessor, 124-law count, 53-operation matrix digest, compatibility range, and immutable source boundary are locked.
- [x] Offline Studio preserves local Receiz ID and canonical Signature V4 preparation without inserting enrollment into deterministic first paint.
- [x] Standalone lint, build, release lock, archive parity, relative-link, repository-boundary, and whitespace checks pass on the final candidate.
- [x] Final candidate is prepared for the detailed standalone release commit and local annotated tag `v124.0.0`; Git verifies the post-commit identity.
- [ ] Standalone release commit and tag pushed.
