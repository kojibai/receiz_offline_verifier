# Receiz v103.0.0 Commit History

Baseline: `v101.0.0` production behavior, with `v102.0.0` as the rejected verifier regression.

Commit count: 1 corrective release commit planned.

The v103 release commit covers the corrective shared verifier, authenticated native SDK Record-before-Seal composition, legacy portable admission closure, offline native compatibility, coordinated `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` versioning, and release evidence.

The release preserves existing Profile Composer, Record, and Seal creation behavior while removing the v102 post-creation rejection.

Durable proof memory remains **first admission only, then append forever**.

## Release-Lock Requirement

`pnpm release:lock 103.0.0` must require this corrective app history in the standalone offline-verifier checkout. The verifier records the upstream correction as evidence; it does not replace v101 production rollback truth, native Record/Seal coordinates, or the sealed proof bundle.
