# Receiz v107.0.0 Commit History

Baseline: `477713ae2` (v106.0.0)

Commit count: 20 release commits after the baseline

The v107.0.0 release branch starts from v106.0.0 commit `477713ae2` and records the Receiz.com and `@receiz/sdk` implementation milestones below:

- `a2d66850f` closes inherited v106 implementation gaps.
- `05a5d2c9a` adds v107 operation contracts.
- `b89af1aba` exposes canonical identity/profile operations.
- `97484bb26` adds portable account continuity.
- `55694e3f5` adds generic bearer artifact ownership.
- `11eaa98a7` exposes v107 SDK operations.
- `cd3bd08f3` adds the typed offline command queue.
- `94541a096` adds MCP parity.
- `f215c4d5d` adds executable v107 AI skills.
- `a1081796e` adds the emulator and integration generator.
- `9611cb8f5` wires proof operations into profile surfaces and captures public first-paint evidence.
- `8bc7eea29` prepares v106→v107 migration, registry, coordinated packages, runtime, service worker, and external installation.
- `0be68345d` binds the release candidate to complete documentation, threat model, visual evidence, package gates, and release-lock wiring.
- `2c90ee7e1` locks the release qualification evidence and the canonical v107 public-profile publication contract.
- `ebb29364d` records the successful full freeze and the production-attestation boundary.

The final release-evidence commit records the successful full freeze and governance results while leaving the signed attestation explicitly pending for the production signing environment. Commit identity documents repository mechanics only; it does not outrank sealed proof objects. The history model is first admission only, then append forever.

## Release-Lock Requirement

`pnpm release:lock 107.0.0` must require this document for `v107.0.0` and later release cuts. A release that omits the reviewed implementation boundary cannot claim that Receiz.com, SDK, MCP, AI-skill, emulator, generator, migration, registry, visual evidence, package, and release-lock slices were coordinated beneath sealed proof authority.
