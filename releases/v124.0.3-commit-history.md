# Receiz v124.0.3 Commit History — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** This ledger preserves sealed proof object chronology instead of relabeling V124.0.2 after the fact.

Durable proof memory remains first admission only, then append forever. The `@receiz/sdk` release coordinate records the implementation boundary; it does not rewrite the historical chain or outrank exact proof-object bytes.

Baseline: public V124.0.1 release at `b4efb56c72780bd9b5013ef466fdea998701b96b`.

Published V124.0.2 runtime boundary: `eed426781b6dbb4138e29c9cbcbcf896ec890b11`.

V124.0.3 pre-release implementation boundary: `52d97cb4614ab5f180c0faa6948d5532adf5511d`.

Commit count: 50 commits from the V124.0.1 baseline through the V124.0.3 pre-release implementation boundary.

## Portable-material introduction

Commits from `5c9663a24` through `eed426781` introduced portable proof material, inline and bounded-head playback, Profile publication, verifier synchronization, and related UI work. The proof-material breakthrough remained valid; optional representation work was not kept outside every source execution path.

## Rollback and reconstruction

- `ce7a97147` and `b25d66836` exposed copy amplification and QR drift.
- `3ca10e52c` and `6063005b6` began isolating playback and canonical held bytes.
- `84e03350b` restored V124.0.1 Workbench proof behavior as the safe baseline.
- `157f2b8d5`, `ce693fa1b`, `855c286dd`, and `440331a0c` established streaming trailer sealing and verification.
- `37848050e` restored the known-good large `/seal` and `/verify` projection and repaired the Docs build root.
- `f28760b0d`, `cfc535baa`, and `fda1eb060` synchronized the Workbench runtime, native successor fields, and release gates.
- `f260540c1` closed source-first large-media Profile publication, Workbench append/PBI, offline verifier streaming, typed bundle validation, and coordinated build failures.
- `52d97cb46` added the final compact `?c=` Offline Studio QR projection and restored bounded grid-media loading beneath settled Profile shells.
- `8c52fceba` introduced a canonical PBI badge into exported PNG pixels; `60d4eef40` isolated that rewrite to export but also introduced the exception that let Workbench accept a representation the standalone verifier rejected.
- The release reconciliation commit removes that representation path, exports the exact verified PBI descendant, moves exact-byte/continuity acceptance into the SDK, synchronizes Workbench to that SDK gate, and advances coordinated package identity to `124.0.3`.

There are 50 commits between the V124.0.1 baseline and the V124.0.3 implementation boundary. The V124.0.2 tag and publication evidence remain historical coordinates and are never moved to the V124.0.3 boundary.

## Release reconciliation boundary

The V124.0.3 version, package distributions, institutional documents, and generated Academy archive are committed after the pre-release implementation boundary. The commit carrying this ledger is the final repository release boundary. Governance evidence and attestation are recorded only if their commands complete; package publication, Git tag, deployment, smoke, mobile, scale, and adoption remain later independent evidence. None modifies registry `124.0.0`, historical proof bytes, or the earlier V124.0.2 public tag.

## Standalone Archive Boundary

The standalone repository already carries three source-first large-file commits after its historical `v124.0.2` tag:

- `6986d0a` — `Large-file trailer verification & streaming SHA256`
- `e7b99fb` — `Support large trailer media; add SHA256 clone`
- `d699451` — `Reuse SHA-256 w buffer to avoid allocations`

`d699451cc61fc80136b45d9d4d6925fdcf7fdb68` is the standalone predecessor for this release. The application V124.0.3 release commit and observed `origin/main` are `8cdd19d7e7c8a81610cc03f42606fdf6160c039e`; its committed release source is additionally bound by exact digests.

The standalone `v124.0.3` commit and annotated local tag are established by the release sequence after qualification. They do not imply coordinated package publication, an application tag, deployment, remote push, production smoke, mobile evidence, scale evidence, or signed attestation.
