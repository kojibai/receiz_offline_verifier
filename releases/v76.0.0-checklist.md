# Receiz v76.0.0 Release Lock Checklist

Release date: May 6, 2026

Product truth: `docs/releases/v76.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `76.0.0`.
- Public release badges show `v76.0.0`.
- Service worker defaults use `76.0.0`.
- Offline verifier surfaces show `v76.0.0`.
- README manual release checklist points at `docs/releases/v76.0.0-checklist.md`.
- World Concierge always replies through the direct Responses streaming path unless the user is explicitly asking the agent to perform agentic work.
- Agents SDK use remains additive: tool execution, handoffs, tracing, and orchestration cannot break normal concierge reply streaming.
- The World agent toolbelt exposes real business tools and fails gracefully without suppressing the user-visible reply.
- Public-safe agent trace shows product-level steps instead of raw model traces.
- World missions produce visible artifacts: leads, contacts, drafts, sends, follow-ups, revenue attribution, payment/booking path, and next revenue action.
- Follow-up campaigns default to email unless the user explicitly configured a valid connected-app webhook destination.
- Legacy invalid app-webhook campaigns can heal to email delivery when email is configured.
- Market Twin first-paints from cached market truth and does not wait on saved mandate hydration before showing the existing shell.
- Market Twin saved mandate settings hydrate into the existing console without remounting, resetting, or visually redefining the surface.
- Market Twin exposes brief, why-this-move, guarded action, value ledger, daily brief, and next return action without fake yield claims.
- Market Twin replay remains deterministic and local to the market snapshot.
- Market Twin authority tables and repair migration persist mandate columns and append-only intent audit rows.
- Public Proof observe, lookup, claim, verify, seal, dispute, rights, identity, OAuth, creator, and registry-feed surfaces are available behind the public proof product boundary.
- Public Proof public reads are status-visible; signed-in mutation routes remain protected.
- Wallet reserve/payment credit settlement handles non-subscription value without double counting or losing running reserve balance truth.
- Public/developer Status displays high-value public endpoints and excludes admin, webhook, internal-worker, private session-only, and privileged issuer surfaces.
- Status dynamic probes use safe synthetic identifiers for username, Receiz ID, note, proof, creator, and proof-record routes.
- Account menu frames operating surfaces and exposes Market Twin and Public Proof.
- World mobile pill and popover layouts remain compact, dock-safe, and no-zoom on mobile controls.
- Profile, auth, Receiz Key, passkey, PBI, and bridge continuity preserve v75 local-proof law.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```
