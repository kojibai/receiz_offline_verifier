# Receiz v101.0.0 Release Lock Checklist

Release documents:

- `docs/releases/v101.0.0-product-truth.md`
- `docs/releases/v101.0.0-process.md`
- `docs/releases/v101.0.0-regression-lessons.md`
- `docs/releases/v101.0.0-performance-findings.md`
- `docs/releases/v101.0.0-invariant-register.md`
- `docs/releases/v101.0.0-commit-history.md`

This lock keeps `@receiz/sdk` beneath sealed artifact authority and durable proof memory as **first admission only, then append forever**.

## Candidate Identity

- [x] Baseline is annotated tag `v100.0.0`.
- [x] `101.0.0` is a deliberate major because manifest-only public asset admission is removed.
- [x] App, SDK, MCP, runtime, service worker, verifier labels, and release records move together.

The baseline above names the reviewed Receiz app candidate. This standalone offline-verifier repository starts from its own annotated `v100.0.0` tag at `7f17e33c7f8e25134122307dd3d735b2bb624f93`; that repository boundary is mechanics beneath the app proof range, not a competing source of product truth.

## Offline Verifier Repository Gates

- [x] `node scripts/check_release_lock.mjs 101.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 101.0.0`
- [x] `git diff --check`

## Local Gates

- [x] `node scripts/check_release_lock.mjs 101.0.0`
- [x] `RELEASE_EXPECTED_VERSION=101.0.0 pnpm test:sdk-release-contracts`
- [x] Complete proof-object unit, server admission, migration, OpenAPI compatibility, and offline-verifier contracts.
- [x] `pnpm test:primitive-binding-contract`
- [x] `pnpm test:verification-conformance`
- [x] `pnpm test:release-freeze`
- [x] `git diff --check`

## Visual Evidence

- [x] `/verify` retains its existing single verified verdict on the exact v101 build (`output/playwright/v101-verify.png`).
- [x] Public home identifies `v101.0.0` on the exact build (`output/playwright/v101-home.png`).
- [x] No screenshot, recording, or DOM evidence contradicts offline verification or continuity law.

## Governance And Attestation

- [x] `pnpm governance:check`
- [x] `pnpm governance:evidence-check`
- [x] `pnpm governance:verify-artifact-signature`
- [ ] `pnpm release:attest`
- [ ] `pnpm release:verify-attestation <path>`

## Promotion Actions

- [ ] Apply `20260715190000_sdk_portable_asset_continuity.sql` to the linked Supabase project.
- [ ] Publish `@receiz/sdk@101.0.0`.
- [ ] Publish `@receiz/mcp-server@101.0.0`.
- [ ] Deploy Receiz frontend/runtime/service worker `v101.0.0`.
- [ ] Commit, push, and create annotated tag `v101.0.0`.
- [ ] Verify production and registry release identity.

Unchecked gates or promotion actions are incomplete and must not be described as complete.

Local attestation generation remains pending until the exact release commit exists and the high-risk separation-of-duties inputs plus active Ed25519 attestation key are available.
