# Receiz v77.0.0 Release Lock Checklist

Release date: May 8, 2026

Product truth: `docs/releases/v77.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `77.0.0`.
- Public release badges show `v77.0.0`.
- Service worker defaults use `77.0.0`.
- Offline verifier surfaces show `v77.0.0`.
- README manual release checklist points at `docs/releases/v77.0.0-checklist.md`.
- Signal Circuit `/game` first-paints a dedicated product surface with no fake loader shell, no arcade framing, and no visible remount before gameplay truth.
- Signal Cards remain sealed Receiz collectibles with separated achievement truth, transferable ownership truth, rarity, mechanics, art, lineage, battle profile, and verification paths.
- Signal Card downloaded fronts use `☤KAI` steward/valuation copy and do not regress to old Pulse framing.
- Signal Card issue flow does not call success before the sealed card is rendered, issued, stored, and recoverable.
- Purchased or recovered Signal Cards do not downgrade deterministic stored artwork with stale local cache or weaker transport payloads.
- Signal Pack purchase uses exact wallet settlement, pending opening recovery, completed-card IDs, and durable account-side completion.
- Signal Vault is account-scoped, paginated, retryable, and append-only for card, transfer, listing, auction, market, and battle records.
- Signal Vault hydration does not leak cards across accounts or hard-stop at one visible batch.
- Card public pages, share images, share actions, proof panels, listings, auctions, and purchase panels render sealed card truth without liquid loader replacement.
- Card purchase remains reserve-first and does not redefine wallet value or profile truth.
- Signal Arena battle requests require authenticated owners where needed, use opt-in AI twin defense, and append battle/progression records without mutating original claims.
- Signal Card discovery reads verified discoverable card truth for swipe, world-map, market, auction, and battle surfaces.
- Signal Pack Foundry campaigns publish sealed manifests with tier ladder, visible odds, supply caps, pack limits, campaign records, and verification isolation.
- Signal Circuit conformance exposes requirements, claim-to-code checks, source digests, scarcity proof, bounded simulation, badge, history, rollups, and status inventory.
- Public/developer Status includes public Signal Circuit endpoints and excludes private vault writes, pack purchase internals, worker routes, admin routes, webhooks, privileged issuer routes, and legacy keyring trust surfaces.
- Creator Workspace is a first-class `/creator` route and does not reuse `/make`.
- Creator Workspace first-paints a deterministic local workspace before signed-in AI, wallet, asset, publish, or launch enrichment.
- Creator output uses Streamdown and creator activity UI without fake production claims.
- Creator AI run, asset, publish, workspace, storage, and worker APIs require the right signed-in or worker authority boundary.
- Creator OS migrations include durable projects, brand kits, runs, messages, events, outputs, assets, versions, usage ledger, publish jobs, and safety reviews.
- Creator paid AI runs, asset jobs, and publish jobs debit wallet value idempotently before production continues.
- Creator shortfalls open embedded wallet funding and do not pretend paid jobs are free.
- Creator managed launch queues GitHub and Vercel targets, records proof, and keeps users inside Receiz.
- Verified Data Vault downloads linked bytes before sealing, exports verifiable ZIP/vault packages, restores originals, and previews restored image/audio/video/PDF/native files.
- Vault copy and UI use Proof Map / Vault Library framing instead of Key Map or block jargon.
- Old Safari online document sealing uses Signature V4 fallback; old Safari offline tells users to upgrade Safari 17+ or go online.
- Runtime stale recovery no longer mounts an automatic root interstitial, fake loading copy, stale-guard redirect, or visible replacement shell.
- Market share quote bootstrap batches long asset ID lists before Supabase `in` filters.
- Wallet reserve, card purchase, and creator production settlement preserve deterministic value state.
- Account, global chrome, bottom chrome, game chrome, Creator, Vault, Status, Developers, Payments, Public Proof, Seal, and verified player surfaces remain dock-safe, no-zoom where needed, and consistent with settled first-paint law.
- Profile, auth, Receiz Key, passkey, PBI, bridge, and local Receiz ID continuity preserve v75 local-proof law.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:game-release-contracts` passes.
- `pnpm test:signal-circuit-conformance` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```
