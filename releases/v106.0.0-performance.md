# Receiz v106.0.0 Performance Budget

Constitutional evaluation is bounded by an explicit operation budget. Command admission, causal replay, and independent verification are deterministic and must not add network or database calls to local conformance. Known verified proof objects, PBI authorship, Showcase, and Live Player state paint from stronger known truth; background reconciliation may append only.

The existing `perf:budgets`, performance-route, page-first-paint, deterministic-surface, and full release-freeze gates remain mandatory. `@receiz/sdk` conformance must report zero network and database calls. Proof memory remains **first admission only, then append forever**.
