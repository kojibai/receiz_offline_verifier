# Receiz v80.0.0 Release Lock Checklist

Release date: May 9, 2026

Product truth: `docs/releases/v80.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `80.0.0`.
- Public release badges show `v80.0.0`.
- Service worker defaults use `80.0.0`.
- Offline verifier surfaces show `v80.0.0`.
- README manual release checklist points at `docs/releases/v80.0.0-checklist.md`.
- World resident surface-worker manifests preserve numeric schema `version` and expose `releaseVersion: "v80.0.0"`.
- The main release notes document the full release trail from `v70.0.0` public alpha through the `v78.9.1` stabilization candidate.
- `/<username>` remains canonical profile truth and `?manage=1` remains owner permission overlay only.
- Local proof, Receiz ID, PBI/passkey, Identity Record, Identity Seal, Receiz Key, email, and server-session paths converge on the same signed-in shell after proof acceptance.
- File restore projects the recovered account before import, bind, wallet refresh, or global sync jobs complete.
- `Receiz ID -> account` binding remains idempotent and does not depend on Redis or replay-guard storage.
- Market, wallet, account, profile, card, and vault private state remains actor-scoped.
- Settlement and Reserve value paths preserve funded causality across sends, notes, certificates, shares, auctions, packs, battles, payments, transfers, and wallet holds.
- World Concierge direct replies remain reliable while agentic tools enrich missions, traces, Sales Brain, briefs, campaigns, and revenue attribution.
- Public Proof observe/claim/verify/seal/dispute/rights/registry behavior remains governed and public reads remain visible in public/developer assurance.
- Creator Workspace first-paints locally and keeps wallet-backed production, publish, safety, launch, and worker boundaries durable.
- Signal Cards remain sealed Receiz assets with separate achievement truth, owner truth, art evidence, append records, battle state, auction state, and transfer state.
- `/game/cards` reads local verified card truth plus synced owner rows without showcase publication and without account bleed.
- Card share pages, proof popovers, Vault Verify state, flip gestures, battle queue, and game-front copy stay settled and mobile/PWA safe.
- Signal Arena append proof manifests expose appends without mutating original card truth.
- Signal Vault Card export starts only from explicit user action and embeds archive bytes, manifest, hash, chunking, Merkle integrity, KAI timing, paths, and restore rules.
- Mobile/PWA card downloads do not navigate away, refresh the app shell, collide with the header, or start from hidden panel-open side effects.
- Transfer packages move ownership through explicit evidence and avoid duplicate owner ambiguity.
- Card auctions preserve wallet-hold and settlement truth.
- `/signal-circuit` and `/exchange` continue to match real route behavior, conformance, ownership, settlement, wallet, card discovery, and proof paths.
- Signal Circuit conformance source inventory, source digests, badges, history, public route list, and status catalog match real public code paths.
- Public/developer Status excludes private vault writes, pack purchase internals, worker routes, admin routes, webhooks, privileged issuer routes, and legacy keyring internals.
- Dashboard route first-paint behavior remains stable after the post-v78 dashboard experiment/reversion sequence.
- Runtime stale recovery stays hidden; it must not show a root interstitial, fake loader, or automatic visible route takeover.
- Service-worker registration defaults, runtime build IDs, update checks, private-browser behavior, and PWA header behavior move with the release.
- Bottom chrome, global auth chrome, chat dock, composer host, card popovers, footers, profile/card CSS, discovery controls, and Explore toolbar clearance remain mobile/PWA stable.
- Value-loop copy remains contract-bound to reserve, settlement, ownership, wallet, and proof truth.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:game-release-contracts` passes.
- `pnpm test:signal-circuit-conformance` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```
