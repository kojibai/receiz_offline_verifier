# Receiz v74.0.0 Release Lock Checklist

Release date: April 30, 2026

Product truth: `docs/releases/v74.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `74.0.0`.
- Public release badges show `v74.0.0`.
- Service worker defaults use `74.0.0`.
- Offline verifier surfaces show `v74.0.0`.
- README manual release checklist points at `docs/releases/v74.0.0-checklist.md`.
- Receiz Key restore verifies the embedded Identity Record key-payload signature and active key registration without requiring PBI/passkey restore proof.
- Account Receiz Key controls list active/revoked keys, download private Identity Record PNGs with the embedded key payload, support optional protected export, support Identity Record PNG import plus legacy JSON import compatibility, and revoke only through explicit owner action.
- First-time offline Receiz Key projection requires verified portable state, while later admitted local key truth can project without server rediscovery.
- Deterministic profile identity artwork derives from stable account-creation Kai truth without runtime randomness or storage upload timing.
- Identity seal/record chrome is seeded by proof and Kai moment data, while the KaiSigil glyph itself keeps the exact canonical geometry, color math, and size math from the creation sigil.
- PBI-signed identity seal and identity record artifacts append signed state through the real seal primitive, keep the base profile shell stable, render signed witness state in trust-blue chrome, and require owner PBI before any identity seal/record download.
- Public profiles do not expose downloadable identity seal/record actions; Identity Record import/restore remains Receiz Key proof and does not require PBI.
- Profile creation pulse seeds remain durable and cannot move forward across remounts, route hydration, or workspace hydration.
- Long-form profile originals preserve full text and hash while the profile card layout keeps URL, Read, and market value controls in separate rows.
- Attached Receiz URLs preserve the exact href and render compact domain labels without silent truncation.
- Profile video playback derivatives use canonical playback media sources and keep original proof downloads intact.
- Sold share-position continuity prevents closed local positions from resurrecting through route snapshots or market hydration.
- Portable state import preserves profile media/state fields and reports integrity-skipped ledger entries instead of silently weakening ledger proof.
- `pnpm release:lock` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm release:lock
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:release-freeze
pnpm release:attest
```
