# Receiz v122.0.0 Migration

The proof object is authority; migration creates only append, indexing, synchronization, and recovery storage.

Apply `20260821225747_receiz_v122_remote_subject_world_runtime.sql` once before deploying v122 API routes. Confirm the linked production project exactly, run schema/security advisors, then use authenticated read and zero-write conflict probes. Do not create a production subject without a designated proof fixture.

The migration adds append-only remote subject admission, public subject access-key bindings, world/access events, exact execution outcomes, remote mandates, multi-world commit coordination, and Phi intent recovery. Only the subject-scoped public access binding reaches the server; the private access kit and private plaintext remain at the edge. Existing app tables and user-facing routes are not reinterpreted as v122 authority.

Rollback is allowed only before public v122 writes. After admission or world/value appends, preserve immutable history and forward-fix; never drop accepted proof, private-envelope, outcome, mandate, Settlement, Reserve, or head records.
