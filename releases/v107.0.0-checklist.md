# Receiz v107.0.0 Release Lock Checklist

This checklist qualifies Receiz.com and the public `@receiz/sdk`, MCP, and AI-skill surfaces as one release.

- [x] v106 closure inventory has `unresolved: []`.
- [x] Canonical v107 contracts, stable errors, proof heads, and receipt verification are implemented.
- [x] Same-identity rename and commit-time global username uniqueness are implemented.
- [x] Raw and prepublished profile media use one atomic operation.
- [x] Portable identity restore, namespaced app-state append, and continuity reconciliation are implemented.
- [x] Generic bearer ownership verifies enclosing artifacts and commits one owner.
- [x] Offline queues sign proposals, preserve per-object FIFO, and never claim queued work is committed.
- [x] SDK, MCP, and seven v107 AI skills expose exact operations and registry binding.
- [x] Eleven emulator scenarios and the complete Next.js integration generator are implemented.
- [x] Receiz.com frontend and service worker align to v107 without deterministic first-paint regression.
- [x] v106→v107 migration preserves artifacts, receipts, heads, and witnessed history.
- [x] App, SDK, MCP, AI skills, verifier, runtime, and service-worker identities are v107.0.0.
- [x] Three coordinated packages pack and install into a clean external project.
- [x] Final eight-capture visual evidence index passes.
- [x] Full `pnpm test:release-freeze` passes.
- [x] Governance controls, evidence, and artifact-signature checks pass.
- [ ] Production release governance supplies the change class, approver, approval reference, risk owner, active signing-key ID, and private signing key, then generates and verifies the signed release attestation.
- [x] Final repository diff and release evidence are committed.

npm publication and production deployment are intentionally not part of repository qualification. Receiz proof memory remains first admission only, then append forever.

Release evidence: [product truth](v107.0.0-product-truth.md), [regression lessons](v107.0.0-regression-lessons.md), [performance findings](v107.0.0-performance-findings.md), [invariant register](v107.0.0-invariant-register.md), and [commit history](v107.0.0-commit-history.md).

## Independent Verifier Local Qualification

- [x] `node scripts/check_release_lock.mjs 107.0.0`
- [x] `pnpm lint`
- [x] `pnpm build`
- [x] `pnpm release:lock 107.0.0`
- [x] Supplied JSON validation, registry-digest parity, and canonical/archive byte comparison
- [x] Current-surface stale-narrative scan, v107 link scan, and `git diff --check`

These checks qualify this standalone verifier checkout only. They do not generate the pending production attestation, publish packages, deploy production, mutate a database, push Git refs, or establish authenticated production-browser evidence.
