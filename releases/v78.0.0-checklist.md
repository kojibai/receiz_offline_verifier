# Receiz v78.0.0 Release Lock Checklist

Release date: May 9, 2026

Product truth: `docs/releases/v78.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `78.0.0`.
- Public release badges show `v78.0.0`.
- Service worker defaults use `78.0.0`.
- Offline verifier surfaces show `v78.0.0`.
- README manual release checklist points at `docs/releases/v78.0.0-checklist.md`.
- World resident surface-worker manifests preserve numeric schema `version` and expose `releaseVersion: "v78.0.0"`.
- Signal Card downloaded fronts, QR payloads, share actions, share page, OG image, and Twitter image routes carry sealed card truth without detaching proof from owner state.
- Signal Card World reads verified local cards and synced global owner rows without requiring showcase publication.
- Signal Card World discovery preserves account scope, hidden state, showcased state, listings, battle appends, and transferred-out state.
- Global Vault sync publishes server-known owner rows in bounded batches and does not rewrite original card claims.
- Signal Vault Card export embeds archive bytes, archive hash, chunk count, Merkle integrity, manifest, KAI timing, card paths, and restore rules.
- Opening the Vault panel does not start sealing, export, hidden downloads, timers, or route navigation.
- Card export and download actions are explicit clicks and remain stable in Safari, installed PWA, and in-app browsers.
- Transfer downloads carry explicit transfer evidence and do not leave duplicate owner ambiguity.
- Card restore path recognizes the sealed Vault Card payload and restores card/archive truth from embedded bytes.
- Signal Arena battle requests require the right authenticated actor and owner scope.
- AI twin defense is opt-in and never inferred from public card visibility.
- Battle settlement appends battle history, action-ledger rows, card append records, and transfer state without mutating the original card claim.
- Battle stake modes preserve wallet/ledger truth for money, XP, keeps, and fun modes.
- Auction routes and database migration preserve wallet holds and settlement state.
- `/signal-circuit` publishes the public Circuit standard and matches the implemented card ownership, discovery, transfer, market, and arena behavior.
- `/exchange` connects Circuit, Market, settlement, Wallet, Payments, Transfers, Ledger, Offline notes, `/game/cards`, and conformance without splitting proof from value.
- Signal Circuit conformance badge, source inventory, source digests, public route list, and status catalog match real public code paths.
- Public/developer Status excludes private vault writes, pack purchase internals, worker routes, admin routes, webhooks, privileged issuer routes, and legacy keyring trust surfaces.
- Dashboard route first-paints through the simplified route/client/API split and does not reintroduce a heavy route-level blob.
- Service-worker registration defaults, runtime build IDs, update checks, and PWA header behavior move with the release and do not strand stale app shell state.
- Bottom chrome, global auth chrome, chat dock, composer host, card popovers, footers, profile/card CSS, and discovery controls remain mobile/PWA stable.
- Value-loop copy remains contract-bound to reserve, settlement, ownership, wallet, and proof truth.
- Profile, auth, Receiz Key, passkey, PBI, bridge, and local Receiz ID continuity preserve v75 local-proof law.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:game-release-contracts` passes.
- `pnpm test:signal-circuit-conformance` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:value-loop-guard
pnpm test:release-freeze
pnpm release:attest
```
