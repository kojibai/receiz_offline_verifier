# Receiz v124.0.0 Compatibility Matrix — Reality Becomes Infrastructure

**The Production Runtime for Verified Civilization.** Every compatibility statement keeps the sealed proof object and exact authenticated head above package, server, database, session, and UI representations.

| Subject | V124 compatibility statement |
| --- | --- |
| Coordinated package identity | `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` are current at `124.0.0`; current application compatibility is `>=124.0.0 <125.0.0`. |
| V123 proof objects | Historical V123 sealed proof object bytes, schemas, registry bytes, digests, signatures, and whole-pulse `issuedAtKai` semantics remain byte-compatible and unchanged. |
| Micro-pulse precision | Explicit `issuedAtKaiUPulse` / `expiresAtKaiUPulse` or structured micro-pulse coordinates require a newly versioned schema; existing fields are never reinterpreted. |
| Stable operation identifiers | Existing v122 and v123 SDK/MCP identifiers remain stable protocol names in the V124 distribution. |
| Application admission | Existing application/principal admission remains unchanged; V124 creates no second identity or consumer-specific admission path. |
| Proof authority | Sealed proof-object verification and exact authenticated heads remain authority; runtime responses must bind to the originating request and stronger evidence. |
| Hosted persistence | Journal, checkpoint, plan, execution, session, and database records are sync/coordination/recovery beneath proof authority. |
| Replay | Only fully verified additions and namespace metadata can enter application callbacks; unknown discriminators and statuses fail closed. |
| Private state | Private additions hydrate only after access verification; private coordinates and recipient membership are not projected to unauthorized callers. |
| Atomic domains | World, inventory/material, Settlement, and Reserve transaction effects commit together or produce zero accepted writes. |
| Recipient resolution | Username resolution returns a privacy-safe transfer coordinate only under consent and rate limits; it never exposes directory enumeration. |
| Capability evidence | A dependency is operational only when its declared evidence passes; configured or present is not equivalent to operational. |
| Durable memory | Durable proof memory remains first admission only, then append forever. |

No compatibility row claims publication, migration, deployment, production smoke, scale, or adoption evidence. Those stages are independently recorded in the V124 evidence status.

The current 53-operation matrix digest is
`540d1c1bf39f1b288b257c79a6e020bdcc5e587fc9b7dbf6b7aaa5d082e20ad5`.
The 124-law registry digest is
`d02429151b0bcebdaeb89485792e377afc55130f9a25e07982c1c88221314247`,
chained to immutable v123 registry digest
`945a581d1fc49c2dc18fbe8c129771ef464b8a58b96188bce561e88ae8b6ceeb`.
The standalone archive preserves these identities as compatibility evidence; it
does not itself ship or publish the coordinated packages.
