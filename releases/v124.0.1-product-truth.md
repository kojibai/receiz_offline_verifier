# Receiz v124.0.1 Product Truth — Reality Becomes Infrastructure

Release date: August 23, 2026

**The Production Runtime for Verified Civilization.** This patch extends the deployed V124 runtime without rewriting its constitutional ruleset, registry, existing schemas, proof hierarchy, or historical `v124.0.0` package evidence. Additive `.v124` MCP carrier schemas version new subordinate package representations; they do not alter any pre-existing constitutional schema or field meaning.

Durable proof memory remains first admission only, then append forever. `@receiz/sdk`, MCP, AI, server, database, session, and UI representations remain subordinate to the admitted sealed history.

## Frozen Primitives

| Primitive | Locked truth | Primary enforcement |
| --- | --- | --- |
| Proof object storage authority | Exact sealed bytes, verified history, and authenticated heads remain stronger than SDK, MCP, database, session, or UI representation. | `packages/receiz-sdk/src/v124Replay.ts`, `packages/receiz-mcp-server/src/v124Operations.ts` |
| Identity primitive | Identity Record, Identity Seal, and Receiz Key import prove continuity directly; a delegated bearer remains only a short-lived capability. | `receiz-docs/lib/workbench/identity-runtime.ts`, `app/lib/oidc/scopeCatalog.ts` |
| Delegated capability catalog | Every lawful OIDC scope is available through one universal catalog across client creation, discovery, token creation, and MCP setup. | `app/lib/oidc/scopeCatalog.ts`, `packages/receiz-sdk/src/capabilities.ts` |
| MCP projection | All current MCP operations are discoverable, schema-bound, and represented to AI/Academy without MCP becoming authority. | `packages/receiz-mcp-server/src/index.ts`, `ai-skills/skills.json`, `receiz-docs/lib/workbench/developer-projection.ts` |
| Portable receipt coordinate | A valid saved receipt coordinate remains a public proof surface without requiring a database row; altered coordinates fail closed. | `app/lib/receiptCoordinateProof.ts`, `app/lib/receiptView.ts` |
| V124 constitutional identity | V124 registry bytes, registry digest, ruleset `124.0.0`, pre-existing constitutional schemas, and pre-existing `.v124` law IDs are immutable. Package/application patch identity is `124.0.1`; additive MCP carrier IDs remain subordinate representations. | `docs/releases/v124.0.0-constitution-registry.json`, `packages/receiz-sdk/src/releaseIdentity.ts` |

## Frozen Invariants

| Invariant | What must not regress | Locked by |
| --- | --- | --- |
| One complete scope catalog | No OIDC route, developer selector, or MCP setup helper may silently carry an older partial capability list. | `app/lib/__tests__/v124OidcScopeExposure.test.tsx`, MCP login contracts |
| Full current MCP parity | AI and Academy machine projections must equal the complete current MCP runtime inventory; removed tools may not remain admitted. | AI distribution validation and Academy developer-rails contracts |
| Reference custody | Raw identity artifacts, sealed source bytes, sessions, execution handles, private additions, and replay proof objects cannot be reconstructed from model JSON. | `packages/receiz-mcp-server/test/v124Operations.test.ts` |
| Replay export is not pre-sealed | Export returns exact non-authoritative candidate bytes; restore accepts only a canonically sealed replay proof object reference. | V124 MCP map and replay export/restore contracts |
| Database-independent receipt landing | Missing database or anchor projection cannot 404 an otherwise valid saved coordinate; a bad coordinate cannot use that fallback. | `app/lib/__tests__/receiptViewDatabaseIndependent.test.ts` |
| Patch cannot rewrite V124 law | Package `124.0.1` remains compatible with ruleset `124.0.0`; no registry digest, historical tag, existing constitutional schema, or existing `.v124` identifier changes. Additive MCP carrier schemas cannot become law or proof authority. | `scripts/test_v124_release_lock.ts` |

## Release Gates

- `pnpm test:v124-release-lock`
- `pnpm test:coordinated-release-identity`
- `pnpm test:ai-skills-distribution`
- `pnpm test:sdk-release-contracts`
- `pnpm test:mcp-release-contracts`
- focused OIDC, receipt, SDK V124, MCP V124, and Academy developer-projection contracts
- SDK/MCP typecheck and build
- `pnpm test:primitive-binding-contract`
- `pnpm test:source-authority-contract`
- `pnpm test:repo-only-reasoning-boundary`
- `git diff --check`

## Residual Risks

- NPM publication, production deployment, production smoke, Git tag, and signed attestation are external operations and remain unverified until their exact evidence is recorded.
- Receiz Docs cannot prove registry installation of `124.0.1` before the packages exist in NPM; its frozen lock must bind the exact published tarball integrity before deployment.
- No production database migration is introduced by this patch.

## Standalone Projection

The standalone verifier carries the patch release identity but does not implement or claim the application-side OIDC, MCP, Academy, or receipt-route changes. Its preserved primitive is exact-file verification: sealed bytes and verified history remain above route, database, session, SDK, MCP, AI, and display state. Verifier semantics remain unchanged from `v60.0.0`.
