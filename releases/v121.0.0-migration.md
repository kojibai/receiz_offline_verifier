# v121.0.0 Migration

No proof-object migration or rewrite is required.

Applications adopting the coordinated developer release should update `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` together to `121.0.0`. Continue using compatible v120 HTTP protocol routes. Do not transform historical sealed objects, usernames into identity, or server snapshots into proof authority.

For profile projections, merge by strongest verified field and persist a newly verified composite head as durable cold truth. For Settlement and identity surfaces, paint known verified state first and reconcile lawful additions in the background.
