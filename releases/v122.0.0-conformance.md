# Receiz v122.0.0 Conformance

The proof object is authority. Release conformance requires deterministic subject IDs, idempotent replay, zero-write conflicts, authenticated state, zero-network edge verification, ciphertext separation, viewer-scoped additions, append-only access revocation, exact execution outcomes, exact participant/head/byte validation, mandate revocation, atomic multi-world writes, and Phi rail identity.

Settled Receiz application primitive binding, deterministic surfaces, first paint, and visual evidence remain release blockers. Package typechecks, tests, packed contents, migration contracts, governance controls, and attestation must agree with the exact v122 registry digest.

Institutional conformance requires the same authority order at every surface: enclosing proof object, verified edge state, authenticated append history, remote coordination/recovery, then SDK/MCP/AI/UI projection. A lower layer cannot satisfy conformance by recreating, renaming, delaying, or overriding a stronger layer.

Phi conformance requires exactly one named movement rail per intent—Settlement or Reserve—and rejects USD as moved authority. The accepted deterministic quote and price-basis digest bind the live USD display to the Phi movement without collapsing the two rails.

Required release evidence:

- `pnpm test:v122-release-lock`
- `pnpm test:coordinated-release-identity`
- `pnpm test:coordinated-package-release`
- `pnpm --dir packages/receiz-sdk test`
- `pnpm --dir packages/receiz-mcp-server test`
- `pnpm --dir ai-skills test`
- `pnpm --dir receiz-docs test:foundation`
- `pnpm test:primitive-binding-contract`
- `pnpm test:page-first-paint`
- `pnpm test:visual-evidence-contract`
- `pnpm test:settlement-conformance`
- `pnpm test:verification-conformance`
- `pnpm test:release-freeze`
- `pnpm governance:check`
- `pnpm governance:evidence-check`

These commands qualify repository and package behavior only when they pass on the final committed tree. Migration, deployment, production smoke, package publication, Git tag, and signed attestation retain independent evidence states.
