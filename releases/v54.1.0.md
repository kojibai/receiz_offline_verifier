# Receiz v54.1.0

Release date: April 15, 2026

## Overview

`v54.1.0` is the world/profile continuity patch release. It keeps the broader `v54.0.0` operating-system baseline intact, then closes two route-continuity gaps: a signed-in user could briefly see the public visitor shell while `/world` was loading, and the market bottom-bar profile action could still force a browser-level page load instead of using the same client-side navigation path as the rest of the signed-in app.

The product-truth freeze for this release is defined in `docs/releases/v54.1.0-product-truth.md`.

## What Defines v54.1.0

- `/world` loading now reads session identity before the full world bootstrap resolves, so authenticated users keep the signed-in shell instead of flashing the logged-out visitor view.
- The loading path still renders through `WorldRouteSnapshot` and deterministic bootstrap data instead of bringing back a bespoke loading skeleton that can drift from the real page shell.
- Bottom-bar profile navigation from market now follows the same client-side prefetch + route transition path as wallet instead of forcing a full document reload.
- The world route hydration contract now locks this authenticated loading behavior in source.
- The profile continuity contract now locks the client-side market-to-profile navigation behavior in source.
- Release-governed version surfaces move together to `v54.1.0` across package metadata, service worker defaults, public badges, offline verifier surfaces, and release docs.

## Targeted Release Outcomes

### 1. Signed-In World First Paint Looks Signed In

The world route should stop visually denying session truth during load:

- a valid session cookie now produces an authenticated loading snapshot
- signed-in users no longer see public-visitor copy or auth CTA chrome while the real world bootstrap is still in flight
- the route can still reconcile into the richer `/api/world/me` payload as soon as it lands

### 2. The Loading Shell Stays On The Real World Snapshot Path

This patch does not reintroduce a fake shell:

- `/world/loading.tsx` still renders `WorldRouteSnapshot`
- deterministic bootstrap data remains the fallback graph for first paint
- the loading shell and the real page continue sharing the same world-shell contract instead of splitting into unrelated UIs

### 3. Market-To-Profile Navigation Behaves Like The Rest Of The App

The market route should not have its own browser-reload exception:

- bottom-bar profile navigation now uses the same client-side route behavior as wallet and the other signed-in tabs
- market no longer hard-switches the browser to `/profile` with `window.location.assign`
- profile continuity from the bottom bar now matches the rest of the app’s signed-in navigation model

### 4. Release Surfaces Stay Canonical

This release also performs the version move cleanly:

- package metadata, service worker defaults, public badges, offline verifier surfaces, and release docs all move together to `v54.1.0`
- the release-lock gate remains the source of truth for that surface inventory

## Verification Notes

Validated against the repository state prepared for `v54.1.0` on April 15, 2026:

- `pnpm exec eslint app/world/loading.tsx app/lib/world/__tests__/worldRouteHydrationContract.test.ts`
- `npx tsx --test app/lib/world/__tests__/worldRouteHydrationContract.test.ts`
- `npx tsx --test app/lib/profile/__tests__/profileRouteContinuityContract.test.ts`
- `pnpm test:page-first-paint`
- `pnpm exec tsc --noEmit --pretty false`
- `pnpm release:lock`
- `pnpm test:release-freeze`

## Deployment Notes

This patch does not add new database migrations.

Before tagging `v54.1.0`, production should still be checked for:

- a signed-in `/world` hard load never flashing the logged-out visitor shell
- an anonymous `/world` load still presenting the public preview state
- the world route still reconciling into the full signed-in bootstrap after first paint
- bottom-bar profile navigation from `/market` moving to `/profile` without triggering a browser-level page load
- public version badges, service worker version, and offline verifier surfaces all showing `v54.1.0`

## Internal Provenance Note

This release note is based on the repository state prepared for `v54.1.0` on April 15, 2026, plus the committed world-loading continuity fix, market-to-profile navigation continuity fix, contract coverage, and release-governed version-surface verification performed against the local repository state.
