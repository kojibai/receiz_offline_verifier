# Receiz v99.0.0 Commit History

Baseline: annotated `v98.0.0` tag at `844807ec180ea1a850491ccabbded0eead7d8f19`

Commit count: `39 committed changes after v98.0.0 before the v99.0.0 release-lock edits`

Reviewed range: `v98.0.0..924343a3c`

Range summary: `184 files changed, 17,733 insertions, 2,084 deletions`

`@receiz/sdk` remains a convenience rail beneath the proof-native artifact system. Durable proof memory remains **first admission only, then append forever**.

## High-Level Release Delta

- Portable proof memory: complete Receiz Key/Proof Vault export and ledger-head visibility.
- Sports proof state: append-only vault admission, normalized game IDs, dated Fight archives, and finalized-entry replay prevention.
- Public proof first paint: deterministic live-player and full-width profile continuity.
- Whole-Market truth: complete durable membership register, current tombstones, and full original/media projection.
- Explore: canonical complete view, settled-entry continuity, bounded grid rendering, and background verified additions.
- Identity: strongest local projection, local publication outbox, session recovery, and Receiz Key offline restore/export parity.
- PBI authorship: local verifier, durable append outbox, exact custody resolution, and one-ceremony send/sign state.
- Runtime continuity: service-worker PBI proof-persistence epochs and package-aligned runtime identity.
- Kai causality: proof moment resolution from sealed pulse state.

## Commit-By-Commit Recap

| Commit | Recap |
| --- | --- |
| `c683eaf0a` | Prevented finalized Sports entries from replaying and added the v98 architecture report evidence assets carried into this range. |
| `016a10aea` | Added local Proof Vault export and ledger-head UI so held settlement/proof memory remains portable and inspectable. |
| `493fbbbba` | Expanded portable account export and bounded long-running route mechanics. |
| `061e0b23e` | Made Sports vault admission append-only and prevented a private vault write from becoming an unauthorized public projection. |
| `0421ed17f` | Added dated Fight arena snapshots and archive lookup. |
| `a533be26d` | Normalized Sports game primary keys and matching across proof routes. |
| `f3d134418` | Added deterministic live-player first paint, exact-route/service-worker continuity, public proof media handling, durable vault migration, and stronger player/profile prefixes. |
| `536f9a47b` | Used canonical current originals for owner fields. |
| `3e65e765a` | Included Market public-proof release identity in player revisions. |
| `4f8da2458` | Corrected test typing and guarded property access without changing primitive authority. |
| `1126e1e6f` | Restored full-width profile shell continuity and locked it in contracts. |
| `c10bcdfc7` | Kept bottom-bar inner width aligned under normalized viewport scale. |
| `553df7fc2` | Merged the deterministic live-player first-paint and hydration work. |
| `a3e3eb8c6` | Rejected tombstoned Market profiles from current public projection. |
| `e779d1156` | Tightened the policy that permits a tombstone to replace held membership. |
| `b66704cda` | Bound tombstone replacement to stronger held route truth. |
| `964c8d55b` | Added canonical Explore API/projection, settled-entry merge continuity, grid windowing, offline append projection, and local publication/session policy. |
| `efd045d36` | Recorded Explore observation time before loading the visible view. |
| `f905e049f` | Added short-lived mechanics caching for canonical Explore assets beneath held truth. |
| `f34712f2d` | Carried `observedAtMs` through Explore mechanics. |
| `093c2e615` | Added immediate identity-key session recovery and conditional publication payloads. |
| `6fad6e7df` | Added session truth to the relevant recovery test fixture. |
| `fb594f378` | Corrected an offline identity contract regex without weakening the identity law. |
| `37c22abf6` | Locked that local publication cannot silently upgrade append truth into a server-issued session. |
| `313beecd1` | Stabilized credential handling by extracting the PBI credential once. |
| `1dfd5eb66` | Advanced interim app/runtime deployment identity to `98.0.1`; this was not a complete release lock. |
| `8880dfc30` | Preserved pending tombstone state consistently across rendering and writes. |
| `32963243c` | Merged identity replay and Market tombstone corrections. |
| `f88fc4b9a` | Attempted PBI signature-history preservation and runtime revalidation. |
| `db227aaad` | Reverted the prior combined approach so the proof-preservation boundary could be rebuilt exactly. |
| `922eedb3f` | Made PBI proof appends permanent durable proof memory. |
| `26ab4288e` | Preserved PBI signatures when writing canonical profile snapshots. |
| `2f3516fc8` | Advanced interim app/runtime deployment identity to `98.0.2`; this was not a complete release lock. |
| `51f847761` | Restored the settled baseline while retaining PBI proof appends. |
| `39a48522b` | Restored the proven global PBI signing fallback beneath held local proof. |
| `1760707e8` | Moved the service-worker runtime epoch to the PBI proof-persistence namespace and aligned interim runtime identity. |
| `cf6dded03` | Aligned one-ceremony PBI append behavior with the visible Send/sign state. |
| `a682f87f6` | Advanced the PBI proof-persistence cache epoch to `v3` so deployment assets refresh without redefining proof truth. |
| `924343a3c` | Resolved Kai moment from sealed pulse truth, preserving Kai causality above Chronos display mechanics. |

## File-Scope Review Summary

- `app/lib/market/*`, `app/market/*`: complete public membership register, tombstones, public profile assets, Market continuity, and settlement-aware execution context.
- `app/lib/profile/*`, `app/[username]/*`, `app/u/[username]/*`: deterministic profile/live-player first paint, hydration ranking, share/media continuity, public projection, and owner-shell parity.
- `app/lib/pbi/*`, `app/api/pbi/*`: local authorship verification, custody, append, durable outbox, and reconnect publication.
- `app/lib/auth/*`, `app/api/auth/*`, `app/account/*`: portable Receiz Key proof memory, strongest local identity projection, publication/session separation, and immediate recovered-account continuity.
- `public/sw.js`, `app/components/pwa/*`: exact-route truth, durable actor vault, verified append broadcasts, public-player revocation, runtime coherence, and cache-epoch mechanics.
- `app/explore/*`, `app/lib/discovery/*`: canonical Explore truth, bounded rendering, merge continuity, and additive refresh.
- `app/game/*`, `app/lib/game/*`, `app/api/game/*`: Sports vault/arena append, archive, identity, and finality corrections.
- `app/lib/ledger/*`, `app/wallet/*`: portable settlement proof memory and ledger-head projection.

## Required Release Evidence

```bash
node scripts/check_release_lock.mjs 99.0.0
pnpm test:local-truth-release-contracts
pnpm test:page-first-paint
pnpm test:deterministic-surfaces
pnpm test:visual-evidence-contract
pnpm test:primitive-binding-contract
pnpm test:repo-only-reasoning-boundary
pnpm test:release-freeze
pnpm release:attest
pnpm release:verify-attestation <path>
```

## Release-Lock Requirement

`pnpm release:lock 99.0.0` must require this reviewed commit boundary in the standalone offline-verifier checkout. The verifier release label records this history as evidence; it does not promote repository or transport mechanics above the implemented proof primitives.
