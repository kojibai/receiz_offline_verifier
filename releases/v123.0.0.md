# Receiz v123.0.0 — Intent Becomes Lawful Action

The coordinated public package surface is `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills`. Durable proof memory remains first admission only, then append forever.

Release date: August 22, 2026

V123 completes the universal path from a held proof object to a lawful remote outcome. The object is authority. The edge verifies and retains it. A short-lived grant expresses explicit application-scoped consent; the SDK produces canonical bytes; the remote runtime performs one exact mutation; and recovery returns committed, zero-write, or unknown before any retry.

## Release outcomes

- `client.value.executeSettlement(intent, authority)` executes canonical Phi Settlement.
- `client.value.executeReserve(intent, authority)` executes canonical Phi Reserve.
- `client.value.executionByIdempotencyKey(key)` recovers an ambiguous execution outcome.
- `client.world.planCommandV122(input)` and `client.world.planTransactionV122(input)` produce canonical public world command bytes, authority and plan digests, transaction identity, and transaction digest. The v122 suffix remains the stable protocol identifier.
- Settlement and Reserve have authoritative OAuth/OIDC scopes plus granted- and missing-scope introspection.
- `client.subjects.resolveNamespaces({ subjectId, atHead, names })` resolves exact current subject namespaces at one authenticated head.
- `client.identity.exchangeProofAuthority({ artifact, challenge, applicationId, scopes })` exchanges independently verified artifact proof and explicit in-application consent for short-lived, non-refreshable, minimum-scope authority without redirecting the person to receiz.com.
- SDK, MCP, AI Skills, registry, operation matrix, OpenAPI, receiz.com developer surfaces, and Receiz Docs advance together to v123.

## Value law

Phi is the moved value. Settlement and Reserve are separate rails with separate scopes and execution paths. USD is display-only from the canonical deterministic price projection. It never becomes the transferred unit or collapses the two rails into a generic balance.

## Authority law

The database is projection, indexing, sync and recovery. The server independently validates the exact proof-bound request and performs the scoped remote write; it does not make the proof object true. Tokens, receipts, MCP results, AI prose, rows, and UI state remain beneath sealed artifact truth.

## Compatibility

The coordinated package line is exactly `123.0.0`, with supported current range `>=123.0.0 <124.0.0`. Historical artifacts remain verifiable. Existing v122 wire schemas and method suffixes remain stable where they are protocol identities; applications do not rename them.

## Release boundary

This repository candidate includes code, migration, package, documentation, and local qualification work. Package publication, production migration application, deployment, tag creation, release attestation signing, and live smoke evidence are separate authorized actions and are not claimed by this record.

Intent Becomes Lawful Action is the release theme and the exact system sequence: proof object → edge verification → explicit consent → narrow authority → canonical plan → exact execution → exact outcome.

## Standalone repository boundary

This standalone verifier archive binds the canonical v123 records to the latest
committed application candidate `1b3d9ad2a7739f5bc2ffff28103ffd3bc4f30e59` and advances from standalone
v122 commit `64ca5dc3884234f1e1c146f042819eca05ff95c3`. It advances verifier-visible
release identity and preserves existing verification behavior; it does not add
the production application API, SDK, MCP, AI Skills, or database implementation.

The registry digest is
`945a581d1fc49c2dc18fbe8c129771ef464b8a58b96188bce561e88ae8b6ceeb`,
its immutable v122 predecessor is
`ed65956a16dd5f0d76d04db2f4a651fc43eb0a71cef64afd53576aa782dc9896`,
the application operation matrix digest is
`e08cec3e3ad22c20ddd6c08169ece19f094c366214d6d6b4dc432cd97558e2c5`,
and SDK/MCP compatibility is `>=123.0.0 <124.0.0`.

This repository record does not claim npm publication, application deployment,
production migration execution, production smoke, signed release attestation,
upstream application tagging, or push. Those evidence dimensions remain separate
until their exact external identifiers are observed.

At archive qualification, application `origin/main` remains at
`fc6ef96cedd3ed6b30da7576a53a5c2977645f82`; the later v123 MCP qualification fix
is committed locally but is not described as pushed by this record.
