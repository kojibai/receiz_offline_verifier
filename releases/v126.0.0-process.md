# Receiz v126.0.0 Release Process

## Goal

Align all current release surfaces and packages at 126.0.0 while retaining exact historical proof and protocol compatibility.

## What Worked

Reuse the canonical release manifest, implemented registry successor preparation, compiler contracts, SDK inventory and generated skill manifests.

## What Did Not Work

Package-only version edits omit compiler admission, skill requirements, packed documentation and Academy projections.

## Practical Rule For Future Work

Change canonical inputs, regenerate their projections, and verify actual compiler and runtime behavior before claiming release completion.

Coordinated package: `@receiz/sdk@126.0.0`. Durable proof memory law: first admission only, then append forever.

## Registry admission migration

Apply `supabase/migrations/20260908021500_v126_lawful_action_registry_successor.sql` before directing v126 issuance to production. It admits the exact v126 registry alongside the preserved v125 registry in the existing derivative SQL append functions. It does not rewrite historical receipts or replace canonical sealed-source verification. The isolated PostgreSQL integration covers retries, ownership appends, tampering and large sources. Production application remains a separate release step.

## Standalone Qualification Boundary

1. Inspect the application source read-only at committed local `HEAD` `d69d6bdfa8277a7933d16015346f38ba4aa37420`.
2. Import the exact canonical verifier, Offline Studio, and ten v126 release records.
3. Bind verifier, Studio, source-book, v126 registry, v125 predecessor-registry, and standalone-predecessor identities.
4. Advance only this repository's package, app labels, doctrine pointers, service-worker epochs, documentation, and release-lock contract.
5. Run release locks, syntax and JSON checks, mirror and digest parity, browser smoke, and Git checks on the intended tree.
6. Commit and create a local annotated `v126.0.0` tag; leave remote push to the user.

The standalone predecessor is `eb70e44e85c17620668d1c055323838ac188488d`, also targeted by annotated tag `v125.0.0`. The source checkout's unrelated dirty Academy lockfile remains outside this release boundary.
