# Receiz v104.0.0 Product Truth

Related evidence:

- `docs/releases/v104.0.0-regression-lessons.md`
- `docs/releases/v104.0.0-performance-findings.md`
- `docs/releases/v104.0.0-invariant-register.md`
- `docs/releases/v104.0.0-commit-history.md`

## Frozen Primitives

The sealed Receiz proof object remains stronger truth than `@receiz/sdk`, the
app compiler, `@receiz/mcp-server`, `@receiz/ai-skills`, repository files,
database, cache, server, model, or UI state. Native proof creation remains an
authenticated real Receiz ID Record before Seal sequence.

## Frozen Invariants

- Inspection and planning never equal artifact verification.
- Automatic writes require the exact reviewed digest and remain root-confined.
- Existing witnessed history is never rewritten.
- Missing truth appends; incorrect projections rebuild from stronger truth.
- MCP/SDK/AI packages align at exact 104.0.0 with no published `workspace:` dependency.
- Durable proof memory remains **first admission only, then append forever**.

## Release Gates

SDK compiler/CLI tests, MCP tools/resources tests, AI skill validation, exact
package alignment, packed artifact inspection, empty-project installation,
historical proof compatibility, continuity conformance, lint, build, governance,
and complete release freeze.

## Residual Risks

Registry publication and post-publication external installation remain unproven
until the packages are published. No production database mutation is required.
