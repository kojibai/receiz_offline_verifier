# Receiz v96.1.0 Commit History

Release date: 2026-06-30

Baseline: `v96.0.0`

Commit count: `pending release-lock commit`

This `v96.1.0` release-lock state captures the Twin and World SDK work after `v96.0.0`:

- Added first-class World convenience clients to `@receiz/sdk`.
- Added delegated Twin convenience clients to `@receiz/sdk`.
- Added Connect Twin routes for delegated market mandate, market intents, Twin Mind export/import, and approval flows.
- Added `receiz:twin.read` and `receiz:twin.write` scopes to Connect discovery and developer configuration.
- Added Twin and World OpenAPI/status catalog visibility.
- Added Twin and World SDK documentation.
- Added SDK tests for the new Twin and World client surface.
- Bumped public app and SDK release identity to `v96.1.0`.
- Updated this offline verifier checkout's release docs, version markers, service-worker release truth, and local release-lock surfaces for `v96.1.0`.

The release lesson from `v96.0.0` remains intact: public developer contracts must never understate internal proof truth. `v96.1.0` extends that lesson into Twin and World capability. Developers receive typed convenience. Receiz truth remains proof-native, append-only, locally durable, and governed by Kai/Klok state. The durable proof memory law remains first admission only, then append forever.

## Release-Lock Requirement

`pnpm release:lock 96.1.0` must require this document for `v96.1.0` and later release cuts when the release brief includes a commit-history artifact. A release that omits the commit-history boundary is incomplete because the v96.0 baseline, Twin and World SDK clients, Connect scopes, OpenAPI/status visibility, SDK docs, package evidence, and repo-local release-lock edits would not be explicit release truth.
