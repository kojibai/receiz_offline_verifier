# Receiz v107.0.0 Product Truth

## Frozen Primitives

Receiz v107.0.0 is one proof-native operation language across Receiz.com, `@receiz/sdk`, HTTP, MCP, AI skills, emulator, generator, frontend, service worker, and migration.

The key is the identity. A username is a globally unique mutable projection of that identity, not a replacement identity and not an availability reservation. A rename preserves the same key and releases the old name only after canonical commitment.

Profile text, username, display name, optional raw or prepublished avatar proof, and proof head commit atomically. A verified canonical receipt matching the returned head is required before the public proof projection changes.

Receiz Keys, Identity Records, Identity Seals, and Vaults carry portable identity and app state. Verified carried truth projects immediately. Network or database state may append verified additions; it cannot redefine first paint or erase stronger local truth.

Bearer ownership is generic across qualifying artifacts. The enclosing proof object is verified before embedded material. Ownership changes append a new sealed artifact and terminate the prior owner projection only after the append commits.

## Frozen Invariants

Offline commands are signed proposals. Queued is not committed. Service-worker and IndexedDB mechanics warm and retain known truth without turning caches into proof authority.

Known verified truth paints immediately. Username release follows canonical commitment. Profile/media effects are atomic. Cross-app state is namespace isolated. Enclosing proof outranks embedded bearer material.

## Release Gates

The v107 operation, frontend, service-worker, SDK, MCP, AI-skill, emulator, generator, migration, package-install, visual-evidence, governance, build, conformance, and release-freeze gates must pass against registry digest `4d0caa6172a69c3bf5817c1c35db5630e555b5d6d824091d45a90fb426b86ef6`.

The regression boundary is recorded in [v107.0.0-regression-lessons.md](v107.0.0-regression-lessons.md), and the reviewed implementation boundary is recorded in [v107.0.0-commit-history.md](v107.0.0-commit-history.md).

## Residual Risks

Repository qualification does not verify production database state, production Redis availability, npm publication, or production deployment. Those are separate external mutations and evidence boundaries.

The v107 registry, packages, MCP plans, AI manifests, emulator results, migration results, release lock, and attestation remain beneath sealed proof objects. Receiz proof memory obeys first admission only, then append forever.
