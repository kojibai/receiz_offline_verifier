# Receiz v106.0.0 Product Truth

`@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` are coordinated at `106.0.0`. Their active constitutional registry digest is `bf851c209e807309672c0f466411baa5607ce6b3195fe4eb16755edfeb7f5a1a`.

## Application Change Boundary

The sole new v106 main-application capability is owner-local admission for newly published Showcase proof objects: the logged-in owner retains accepted PBI authorship and can open the same newly admitted proof object in Live Player. Existing global visibility is not allowed to short-circuit that local append. No other application feature or redesign is part of v106; the other v106 capabilities are SDK, MCP, independent-verifier, AI-skill, migration, and release-lock capabilities. The Sports viewport correction only restores an inherited 140/140 continuity contract that already required visible bottom navigation and safe-area clearance.

## Frozen Primitives

- Sealed proof objects and verified local truth outrank MCP, AI, server snapshots, sessions, databases, projections, and release evidence.
- PBI is an identity primitive. Its locally accepted signature is durable proof admission, not temporary display state.
- Showcase and Live Player are public proof surfaces projecting the same admitted profile original.
- The SDK owns constitutional evaluation, command admission, deterministic replay, capability boundaries, receipts, causal history, and migration.

## Frozen Invariants

- Proof memory is **first admission only, then append forever**.
- A canonical row cannot prevent a new locally accepted proof object from being admitted, signed, and projected.
- Older owner snapshots cannot defeat a newer verified append.
- MCP and AI outputs are never proof authority.
- Direct state mutation, history rewrite, authority bypass, and registry-digest drift fail closed.

## Release Gates

The v106 release lock verifies coordinated versions, canonical registry digest parity, independent verifier separation, machine-readable skill contracts, migration evidence, the exact profile-only application change boundary, profile continuity, PBI signing, Live Player projection, packed tarball installation, performance budgets, governance controls, build, and the full release freeze.

The regression boundary is recorded in [v106.0.0-regression-lessons.md](v106.0.0-regression-lessons.md), and the reviewed implementation boundary is recorded in [v106.0.0-commit-history.md](v106.0.0-commit-history.md).

## Residual Risks

Publication, deployment, production database state, and authenticated production-browser state are outside a local release qualification. They require separately authorized evidence and do not weaken the implementation contract.
