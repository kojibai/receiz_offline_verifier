# Receiz v124.0.3 Migration — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** No migrations are required. V124.0.3 reconciles mechanics beneath constitutional ruleset `124.0.0`; it does not rewrite sealed proof objects or create a new authority.

## Preserved state

- No database schema migration.
- No proof-object rewrite, normalization, reseal, or artifact backfill.
- No identity reissue or PBI reenrollment.
- No ownership-history or append-history rewrite.
- No registry, signature, Groth16, payload-binding, operation-name, stable schema, `rpb1`, `rpb2`, `rma2`, or `rmc1` semantic change.
- No network, account, session, server, database, or package authority added to offline verification.

## Coordinated upgrade

Applications should upgrade application mechanics, `@receiz/sdk`, `@receiz/mcp-server`, and `@receiz/ai-skills` together to `124.0.3`, rebuild the SDK distribution, synchronize the exact Workbench verifier runtime, and rerun coordinated identity and conformance gates. This is a mechanics upgrade; historical artifact verification remains exact-byte compatible across `>=124.0.0 <125.0.0`.

Any PBI export produced by the removed badged-pixel path that fails canonical offline verification remains failed evidence. Do not normalize or bless it. Return to the last canonically verified predecessor and create a new exact PBI authorship descendant through the corrected append path.

Rollback must preserve source files and verified predecessors. It must never substitute a preview, rendered badge, server row, or cached representation for the sealed proof object.
