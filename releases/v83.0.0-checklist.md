# Receiz v83.0.0 Release Lock Checklist

Release date: May 17, 2026

Product truth: `docs/releases/v83.0.0-product-truth.md`

## Release Lock Checklist

- Package version is `83.0.0`.
- Public release badges show `v83.0.0`.
- Service worker defaults use `83.0.0`.
- Offline verifier surfaces show `v83.0.0`.
- README manual release checklist points at `docs/releases/v83.0.0-checklist.md`.
- World resident surface-worker manifests preserve numeric schema `version` and expose `releaseVersion: "v83.0.0"`.
- Main release notes describe the exact `v82.0.0` to `v83.0.0` arc: release base `223b2ddbbd`, 123 commits before release-lock edits, 188 changed files, 28,716 insertions, 3,485 deletions.
- Sports first paint remains instant and truthful.
- Main Event and single-game lock states stay scoped to their own contexts.
- Single-game tab selection uses the same score path as scoreboard selection.
- A first selected single game shows its score without requiring switch-away and return.
- Legitimate zero scores render as legitimate zero scores.
- Active entries cannot overwrite live scoreboards.
- Active entries cannot overwrite selected-game field text.
- Pick 5 identifies known starter, bench, role, backup, injured, and unavailable risk for Main Event and single-game play.
- Card reveal spread supports click-to-open, viewer close, drag/drop, selection, and deselection without degrading stage behavior.
- Reward reveal cards support the same click, drag, drop, select, and deselect behavior as pack cards.
- Users can deselect cards without automatic reselection.
- Saved card proof objects preserve issue number as issuance proof truth.
- Saved cards do not show pending issue when issuance is already known.
- Live-player route works from iMessage without a first-paint crash.
- Live-player and profile routes first-paint from canonical public profile truth.
- SEO structured data and proof manifests do not block the visible hot path.
- Explore paints a deterministic shell even when a build is already in flight.
- Explore first paint does not depend on Three.js, remote assets, or server build completion.
- Market route keeps the fast entry path.
- Market conformance checks the current transport truth without forcing stale hot-path JSON-LD.
- Interoperability conformance checks centralized callback URL policy.
- Vercel tracing includes the callback URL policy source.
- OTC wallet and Cash Image proof object flows remain offline-verifiable before optional chain reconciliation.
- Account Brain and Calendar pagination do not treat multi-page loads as all-or-nothing.
- Bottom navigation routes to canonical profile truth.
- Owner/manage profile hydration remains background overlay behavior.
- Conformance tests are updated only to match product law, not to ratify visible regressions.
- `pnpm lint` passes with no warnings.
- `pnpm build` passes.
- `pnpm release:lock` passes.
- `pnpm test:product-surface-inventory` passes.
- `pnpm test:continuity-release-contracts` passes.
- `pnpm test:profile-release-contracts` passes.
- `pnpm test:world-release-contracts` passes.
- `pnpm test:game-release-contracts` passes.
- `pnpm test:signal-circuit-conformance` passes.
- `pnpm test:economy-conformance` passes.
- `pnpm test:market-conformance` passes.
- `pnpm test:interoperability-conformance` passes.
- `pnpm test:verification-conformance` passes.
- `pnpm test:value-loop-guard` passes.
- `pnpm test:page-first-paint` passes.
- `pnpm test:release-freeze` passes before tagging.
- `pnpm release:attest` emits a signed release attestation for the final candidate when release signing env is present.

## Required Evidence

Run from the exact release candidate:

```bash
pnpm lint
pnpm build
pnpm release:lock
pnpm test:product-surface-inventory
pnpm test:continuity-release-contracts
pnpm test:profile-release-contracts
pnpm test:world-release-contracts
pnpm test:game-release-contracts
pnpm test:signal-circuit-conformance
pnpm test:economy-conformance
pnpm test:market-conformance
pnpm test:interoperability-conformance
pnpm test:verification-conformance
pnpm test:value-loop-guard
pnpm test:page-first-paint
pnpm test:release-freeze
pnpm release:attest
```

Minimum release confidence for the current lock:

```bash
pnpm release:lock
pnpm test:market-conformance
pnpm test:interoperability-conformance
npx tsx --test app/game/__tests__/sportsArenaRouteContract.test.ts
npx tsx --test app/lib/__tests__/receiptRouteContinuityContract.test.ts
pnpm test:page-first-paint
pnpm test:release-freeze
pnpm exec tsc --noEmit --pretty false
pnpm build
```

