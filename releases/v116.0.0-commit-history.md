# Receiz v116.0.0 Commit History

@receiz/sdk preserves durable proof memory: first admission only, then append forever.

Baseline: v115.0.0.

Release candidate product baseline: `0820771f7`.

Commit count: pending the final coordinated release commit.

V116 includes the committed free-core, Premium/Business AI, Creator, Live Twin,
usage-credit, settlement, profile, and continuity work between v115.0.0 and the
candidate baseline. The release promotion adds coordinated version identity,
locks, concise release records, and evidence only.

This record does not claim external publication or any production mutation
beyond the supplied fact that dispute-credit SQL is installed.

## Standalone Verifier Boundary

The product baseline above describes the upstream Receiz repository, where
`0820771f7ab929b39c8a4a5315d20474f914e307` is present. This standalone
verifier begins v116 release work from local commit `0988a5e` on `main`.
Its pre-existing Offline Studio and verifier label changes are preserved.

The standalone release commit may archive evidence, promote verifier-visible
identity, and create a local annotated tag after local qualification. It does
not claim that its history is the upstream product range, supply the missing
upstream dependencies, pass unchecked upstream gates, change Supabase
migration history, run the twelve unchecked migrations, publish, create remote
Git refs, deploy, smoke production, or implement the deferred v117 program.

## Release-Lock Requirement

`pnpm release:lock 116.0.0` must require this record so upstream product
baseline, standalone release history, and production SQL/history facts remain
separate, verifiable truths.
