# Receiz v117.0.0

Release date: 2026-08-01

Registry digest: `598ee0fa4dc31b8394fdd2b7b8fe713c8ee8c4b33e6ecdd92101a1a93d8787a8`

v117 is the release lock for all implemented work after immutable tag `v116.0.0`. It preserves the v116 constitutional law inventory and promotes one coordinated `117.0.0` identity across the app, SDK, MCP server, constitutional verifier, and AI skills.

## Shipped outcomes

- Contacts now remain one held composite proof object across deployments, hydration, PWA recovery, AI additions, session publication, and first paint. Stronger held truth cannot be erased by a partial payload.
- Installed PWA continuity was repaired. The service worker no longer causes visible refreshes, drops settled proof truth, or makes taps heavy; cache rotation is version-only and background work remains behind settled surfaces.
- Profile public and owner surfaces retain their original proof object, Twin media, scrolling, showcase order, high proof head, and deterministic first paint. Redis and partial projections remain beneath stronger carried truth.
- Sports Arena retains its full live-game, proof-save, economy, toolbar, and card experience while `SportsArenaConsole.tsx` is below the 500,000-byte release ceiling. The extracted prepared-seal hook remains synchronous and direct.
- Live sports counts, play flow, server wording, card taps, and settlement projection were repaired without granting server or UI state proof authority.
- Wallet and Glyph Settlement preserve proof-native settlement state; weaker refreshes cannot erase known workspace, reserve, or showcase truth.
- `/t/` links now publish an institutional light transaction OpenGraph/Twitter image with masked identities, amount, status, settlement coordinates, and non-overlapping layout.
- `/v/` links publish adaptable “This happened” moment images for transactions and non-transaction proof moments. Share copy is ready-to-send for Twitter, iMessage, and native sharing.
- Transaction collection from `/v/` downloads the already sealed transaction Moment directly. It does not reseal and no longer reports “failed to seal.”
- Share-image and metadata generation are outside deterministic first paint. Homepage image sizing is full-width and metadata/SEO cannot block proof surfaces.
- Release freeze now runs every gate, records unavailable isolated DB integration separately, and aggregates genuine failures at the end.
- Issuance conformance binds production custody proof. Eight stale or incorrect release contracts were corrected to enforce current product law, including market continuation, offline version identity, free-core/Business custom-domain policy, deterministic usage-credit time, Sports source size, series passport, and value-loop behavior.

## Coordinated packages

- `receiz-app@117.0.0`
- `@receiz/sdk@117.0.0`
- `@receiz/mcp-server@117.0.0`
- `@receiz/constitutional-verifier@117.0.0`
- `@receiz/ai-skills@117.0.0`
- SDK/MCP compatibility: `>=117.0.0 <118.0.0`

## Deferred planning

The unimplemented orchestration proposal formerly assigned to v117 is now the [v118 plan](./v118.0.0-planning.md). It is not represented as shipped work.

The `@receiz/sdk` v117 public surface preserves durable proof memory as **first admission only, then append forever**.

Release records: [product truth](v117.0.0-product-truth.md), [regression lessons](v117.0.0-regression-lessons.md), and [complete commit history](v117.0.0-commit-history.md).

Canonical paths: `docs/releases/v117.0.0-product-truth.md`, `docs/releases/v117.0.0-regression-lessons.md`, and `docs/releases/v117.0.0-commit-history.md`.

## Standalone repository boundary

This repository carries the constitutional verifier and an archival mirror of
the coordinated v117 release book. Its local release lock qualifies the static
standalone verifier, Record and Seal Studio, Sports verifier, settlement
surface, documentation, registry mirror, and service-worker identity only.

The application and package outcomes above are implemented and qualified in
the separate Receiz application repository. Its release commit
`bc74831885e2a69b50ecc8388ce311e75e4c6803` is present on that repository's
`origin/main`. Importing its canonical release records here does not itself run
the application, SDK, MCP, AI-skill, browser, governance, or attestation gates.
This standalone record does not independently claim npm publication, an
application tag, deployment, database mutation, production smoke verification,
or signed attestation.
