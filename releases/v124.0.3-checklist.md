# Receiz v124.0.3 Release Lock Checklist — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** Package identity advances to `124.0.3`; constitutional ruleset `124.0.0`, the pre-existing V124 registry digest, and every historical sealed proof object remain immutable.

Durable proof memory remains first admission only, then append forever. SDK, MCP, AI Skills, server, storage, and UI projections remain subordinate to exact admitted proof-object bytes.

- [x] Representation never outranks source in Record, Seal, Verify, Append, PBI authorship, Export, Profile playback, compact links, or QR projection.
- [x] Large source files use incremental hashing and range verification beneath the same canonical verifier law.
- [x] Generic streaming output is runtime-validated before typed proof-bundle projection.
- [x] Large ownership append and PBI authorship create separately verified descendants without rematerializing optional playback representations.
- [x] PBI export releases the exact verified descendant and performs no badge/canvas byte rewrite.
- [x] SDK exact-byte acceptance rejects native ownership/current-basis drift as `continuity_ownership_attestation_mismatch`.
- [x] Workbench whole-file and streaming adapters cannot report verified before the SDK continuity gate passes.
- [x] Offline Studio and the standalone offline verifier use the same canonical runtime and large-file streaming boundary.
- [x] Offline Studio projects a clickable 92×92 QR from the completed compact `?c=` proof URL.
- [x] A valid carried `?c=` bundle is named **Proof**; historical lookup remains a separate coordinate.
- [x] Profile image/audio global sync and playable URLs remain unchanged while video uses the same source-first publication law.
- [x] Native-capture images reconstruct from their self-contained carried proof URL after temporary composer blob custody ends; the held-source exception remains video-only.
- [x] Local identity fallback creates no Profile Originals; Identity Seal and Identity Record enter Showcase only through admitted publication.
- [x] Same-profile global publication preserves authenticated owner authority, so the existing signature control is present immediately; public/logged-out projections cannot replace it and signing mechanics remain unchanged.
- [x] Settled Profile shells remain immediate; bounded grid-media loading does not rediscover or replace known proof truth.
- [x] Application, SDK, MCP server, AI Skills, service worker, Academy release identity, and generated release projections align at `124.0.3`.
- [x] The release-candidate Docs build uses the last independently published `124.0.2` package pins while its frozen Workbench verifier is synchronized from local `124.0.3`; those pins advance only after exact public-registry evidence exists.
- [x] Ruleset remains `124.0.0`; compatibility remains `>=124.0.0 <125.0.0`.
- [x] No database migration, proof-object rewrite, identity reissue, artifact backfill, historical-byte change, or reseal is required.
- [x] Full local release freeze, main build, Docs build, and governance controls verified for the final tree.
- [ ] npm packages published: `@receiz/ai-skills@124.0.3`, `@receiz/sdk@124.0.3`, and `@receiz/mcp-server@124.0.3` independently observed in the public registry.
- [ ] Public Git tag `v124.0.3` independently observed and bound to the intended release commit.
- [ ] Production deployment confirmed for main and Docs with exact deployment IDs and source commits.
- [ ] Production smoke recorded for credential-free release routes and live mobile large-fixture verification.
- [ ] Signed release attestation emitted and verified through the repository-wired command.
- [ ] Scale and adoption evidence recorded without inference.

The checked implementation items are repository truth; unchecked external stages remain unclaimed. Package, tag, deployment, storage, segment, server, and UI evidence remain subordinate to the sealed proof object.

Release authority documents: [product truth](v124.0.3-product-truth.md), [regression lessons](v124.0.3-regression-lessons.md), [process](v124.0.3-process.md), [commit history](v124.0.3-commit-history.md), and [evidence status](v124.0.3-evidence-status.md).

Canonical repository paths: `docs/releases/v124.0.3-product-truth.md`, `docs/releases/v124.0.3-regression-lessons.md`, `docs/releases/v124.0.3-process.md`, `docs/releases/v124.0.3-commit-history.md`, and `docs/releases/v124.0.3-evidence-status.md`.

## Standalone Repository Evidence

- [x] Canonical standalone verifier and Offline Studio bytes match the read-only application release-commit source digests.
- [x] Large trailer verification remains incremental and resource failure remains distinct from invalid-proof verdicts.
- [x] Package, public labels, doctrine pointers, and cache epochs align at `124.0.3`.
- [x] All 15 canonical records, including the machine-readable registry binding, are byte-identical across `docs/releases/` and `releases/` after standalone boundaries are applied.
- [x] Ruleset `124.0.0`, registry digest, 124-law inventory, operation matrix, and compatibility remain frozen.
- [x] Standalone release locks, syntax checks, source-byte parity, archive parity, browser smoke, and whitespace checks pass.
- [x] Detailed standalone release commit and annotated local tag `v124.0.3` are created and verified by the release sequence.
- [x] No main `/receiz` repository file was modified by standalone qualification.

The unchecked public tag above refers to the application/package release, not the standalone local archive tag. Standalone qualification does not publish packages, deploy production, mutate a database, push a remote branch or tag, run production smoke, or emit a signed attestation.
