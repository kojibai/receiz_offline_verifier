# Receiz v124.0.3 Product Truth — Reality Becomes Infrastructure

Release date: August 25, 2026

**The Production Runtime for Verified Civilization.** Package identity is `124.0.3`. Constitutional ruleset identity and the V124 registry remain exactly `124.0.0`.

Durable proof memory remains first admission only, then append forever. The coordinated public SDK release surface is `@receiz/sdk`; it projects this law without acquiring authority over admitted proof-object bytes.

## Authority ladder

1. Receiz law and product invariants.
2. The immutable source file and enclosing sealed proof object.
3. Deterministic proof-object state, verified append history, and exact authenticated heads.
4. Verified local truth and durable local custody.
5. Server, database, session, package, segment, URL, QR, preview, and UI representations beneath those stronger layers.

A hash proves equality to source bytes. It does not elevate a generic record, a reconstructed preview, or a type assertion into a proof bundle. A QR transports an already-completed coordinate. It does not create authority. A playback URL presents verified material. It does not become the source.

## Frozen Primitives

- Receiz remains a proof-native artifact system.
- Record and Seal create proof objects from the exact held source.
- Verify admits only completed canonical verification.
- Append creates a new canonical successor after the exact verified predecessor; the predecessor remains immutable.
- PBI authorship appends presence-bound authorship without ownership transfer.
- A PBI-authored successor is the exact separately verified descendant. An export-time badge, canvas, preview, QR, or other representation cannot become that successor.
- Ownership transfer and ownership continuity remain distinct from authorship.
- Offline verification is a primary proof primitive, not a fallback mode.
- The public `/v` route is a public proof surface when it carries and verifies the proof bundle.
- The Profile Showcase is a public proof/ownership surface whose verified objects remain authoritative before network enrichment.
- Playback is an optional presentation primitive available only beneath an admitted proof object.

## Frozen Invariants

- Large native audio/video sealing hashes the source incrementally and writes a bounded trailer without first duplicating the complete source into JavaScript heap state.
- Large native trailer verification reads exact file ranges incrementally and projects the same canonical verifier result used by smaller artifacts.
- Streaming verifier output must pass a runtime bundle validator before typed proof-bundle use. Unsafe `as ReceizProofBundle` promotion is forbidden.
- Large append and PBI authorship start from the complete canonically verified predecessor and emit separately verified descendants.
- The SDK exact-byte validator requires the current bundle basis, native Record/Seal payload basis, and ownership-continuity basis to agree before any adapter may report verified.
- Workbench exports only the exact descendant returned by the canonical PBI append. It has no verifier exception for export-only pixel mutations.
- Resource exhaustion, timeout, unavailable worker mechanics, or browser reload are execution failures, never invalid-proof verdicts.
- Workbench, Offline Studio, `/seal`, `/verify`, and standalone verifiers perform no automatic playback construction.
- Profile Composer/Profile Showcase playback remains explicit and additive. Working image/audio global sync is unchanged.
- A native-capture image with a carried material coordinate must reconstruct its playable image payload from that self-contained proof URL after temporary blob custody ends. Only bounded video playback may defer URL reconstruction while its held video source remains settled.
- Identity Seal and Identity Record remain identity artifacts. They enter Profile Showcase membership only through an admitted proof-object publication; session/bootstrap projection cannot fabricate owner-only showcase rows.
- A post-publication event from the authenticated owner carries the complete owner profile projection into the already-open same-profile surface. A public/logged-out projection cannot replace that owner authority, and the existing PBI signature control must be available immediately without changing signing mechanics.
- Offline Studio's QR is projected after the final compact `?c=` URL exists. The QR remains exactly 92×92 and clickable; `?ms=` historical coordinates cannot replace it.
- A valid carried `?c=` bundle is labeled **Proof** after Signature V4 verification.
- Profile card shells remain settled and continuous. Browser-lazy grid covers are resource containment beneath held proof truth, not lazy proof admission.

## Compatibility invariants

- `RECEIZ_RELEASE_VERSION = "124.0.3"`.
- `RECEIZ_RULESET_VERSION = "124.0.0"`.
- Compatible application/SDK range remains `>=124.0.0 <125.0.0`.
- Stable historical schemas, signatures, registry bytes, `rpb1`, `rpb2`, `rma2`, `rmc1`, operation names, and method suffixes retain their existing meanings.
- No historical proof object is rewritten, normalized into new bytes, or resealed.
- No new database authority, issuer, allowlist, session requirement, or network requirement is introduced for verification.

## Release Gates

- The SDK, MCP, AI Skills, constitutional verifier, Workbench, and both offline verifier distributions must accept or reject the same exact proof-object bytes.
- `pnpm test:release-freeze`, the main production build, the Receiz Docs production build, and all governance controls must pass on the release candidate.
- Package tarballs must install together at `124.0.3` without workspace dependencies before publication can be claimed.
- Contradictory mobile or browser evidence blocks release even when source contracts pass.

## Residual Risks

The repository does not infer mobile live qualification from desktop fixtures, production deployment from a local build, publication from a package manifest, or adoption from release readiness. Large bounded-head playback may require retrieval of committed segments on a fresh viewer; those segments provide custody and availability, never proof authority. V125 closed reconstruction remains unclaimed until exact offline byte equality is demonstrated without external material retrieval.

## Standalone Projection

The standalone verifier and Offline Studio consume the exact source working-tree bytes identified by SHA-256 in the release record: verifier `cc225401704c9717de300d8c5d5b3777f0fe92c602e78a389776aea1f8602cf5` and Studio `0eae566796dc2ff812ccdb102d8c47f5da58456f4c6efcbcdd72d6933efdeb03`. The changed offline-verification surface incrementally hashes and verifies large trailer artifacts, validates streamed bundle data before typed use, preserves exact native Record/Seal and ownership-continuity bases, and keeps memory or worker exhaustion separate from invalid-proof verdicts.

The implemented primitive is the immutable source file and enclosing sealed proof object. Whole-buffer materialization, stream readers, workers, URLs, QR codes, IndexedDB segments, SDK objects, and rendered media remain weaker execution, custody, transport, or presentation mechanics. Known held proof truth paints before optional playback or network enrichment, and hidden work may append only verified additions.
