# Receiz v105.0.0 Commit History

Baseline: `v104.0.0` (`0e15313f8`)

Commit count: 12 implementation/design commits before final release preparation.

The sequence split the `@receiz/sdk` runtime/compiler boundary, added shared errors and capabilities, migrated compiler consumers, protected extensions, added the sandbox emulator and conformance, composed semantic MCP tools, and updated shipped AI skills. Proof memory remains first admission only, then append forever.

## Release-Lock Requirement

`pnpm release:lock 105.0.0` must require this app architecture history in the standalone offline-verifier checkout. The verifier records upstream runtime, compiler, emulator, MCP, and packaging evidence; those mechanics do not replace authenticated native proof creation or historical verification truth.
